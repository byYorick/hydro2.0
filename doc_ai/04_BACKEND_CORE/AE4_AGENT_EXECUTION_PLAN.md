# AE4 — план исполнения для оркестратора и агентов

**Дата:** 2026-09-22
**Статус:** `plan`, исполнимый. Это не продолжение AE3. Каталог `ae4/` — имя работы в репозитории. В проде система называется **AE 1.0.0**: одна архитектура и одна бизнес-логика, без второго runtime рядом.
**Объект:** посадка в зоне (`GrowCycle`), одна активная на зону.
**Цель управления:** растению хорошо на каждой фазе. Коридор фазы — комфорт.
**Гидравлика прода:** `clean`, `feed` и, где место управляет стоком, отдельный бак `drain`. Клапан слива рабочего бака этим баком не является.
**Неполный комплект:** несвежий датчик не получает выдуманную норму.

Compatible-With: Protocol 2.0, Backend >=3.0, Python >=3.0, Database >=3.0, Frontend >=3.0.

Пользователь явно разрешил править архитектуру, схему БД и логику, пока собирается 1.0.0. Миграции Laravel, колонки фазы, контракт каналов в `NODE_CHANNELS_REFERENCE.md` и снос `ae3lite` на волне cutover — в рамках плана. Не разрешено: новый compose-сервис, прямой MQTT из AE, агрономия в прошивке, выдуманная норма вместо пустого датчика, копирование машины стадий AE3, выдача клапана `valve_drain` за уровень бака стока.

Формулы §2 живут в этом файле, пока их волна не зелёная. В `ae4.md` они переносятся один раз на cutover, не двумя канонами с первого дня.

К исполнению готова волна 0. Её бриф — §6 и §7. Волна 1 не открывается, пока E215 и E216 не зелёные на `hydro_test`. Живые зоны на `ae4` до волны 3 не переводятся.

---

## 1. Роли

**Оркестратор один.** Он не пишет функциональный код. Он выдаёт агенту бриф волны целиком и не открывает следующую волну, пока прогон зелёный, запреты §3 целы, стиль §1.3 соблюдён и в diff нет того, что §6 пометил как «не делать». Нарушение формулы из §2 — такой же возврат, как красный тест.

**Агент волны один на волну**, если в брифе не сказано «параллельно». Чужие волны не трогает. Сомнение, которого нет в §1.1, останавливает волну. Подставлять своё число нельзя.

Агент возвращает: список файлов, команды и их результат, что скопировал и сразу удалил, риски.

### 1.1. Допущения

1. На зоне одна активная посадка.
2. День и ночь — локальное время теплицы. Поле `greenhouses.timezone` уже есть. Пустой timezone — ошибка конфигурации. UTC контейнера в формулу окна не подставляется.
3. Свежесть датчика — `RuntimePlan.telemetry_max_age_sec`. В рантайм-план оно попадает из `correction_config` `timing.telemetry_max_age_sec`. Поля в плане зоны нет — датчик протух. Своё число секунд не вводится. Дефолт схемы AE3 не подставляется.
4. Нормы только из текущей фазы этой посадки. Полоса влажности — имена `soil_moisture_min` и `soil_moisture_max` из `EFFECTIVE_TARGETS_SPEC.md`. Колонками снимка они сегодня не являются, `PhaseSnapshotCreator` их не копирует. Волна 1 добавляет эти два поля на фазу рецепта и в снимок рядом с `irrigation_interval_sec`. Вторую пару имён не заводить. Пустые поля — правила суши и перелива молчат.
5. Нормы `vpd_min`, `vpd_max`, `light_integral_per_shot` имеют одно место. Волна 1 выбирает одно: новые колонки или уже копируемое JSON-поле `extensions`. Не оба. Колонки пишутся на фазу рецепта и на `grow_cycle_phases`. `PhaseSnapshotCreator` копирует их в том же списке, что `irrigation_interval_sec`. Валидация — `RecipePhaseController`. Форма фазы, которая уже правит интервал полива, принимает эти поля. Новое меню не делать. Только колонки снимка цикла, без рецепта и без копира, не считаются местом. Пустой ключ — правило молчит.
6. Окно света новым полем не заводится. Берётся уже существующая пара `on_time` / `off_time` целей света. Пары нет — `lighting_start_time` + `lighting_photoperiod_hours`, тем же смыслом, что `LightingScheduleParser`. Оба края пустые — окна нет. Задан один край или начало равно концу — ошибка конфигурации. Длительность окна — секунды от начала до конца в часовом поясе теплицы. Через полночь длительность положительная.
7. В `telemetry_samples` лежит `metric_type`, не domain key. `TEMPERATURE` без канала не читается: воздух и раствор имеют один и тот же `metric_type`.

| Domain key | `metric_type` | Канал | К чему относится |
|------------|---------------|-------|------------------|
| `temp_air` | `TEMPERATURE` | `temperature` или `air_temp_c` | воздух зоны |
| `humidity_air` | `HUMIDITY` | `humidity` или `air_rh` | воздух зоны |
| `lux_main` | `LIGHT_INTENSITY` | `light` или `light_level` | свет в зоне |
| наружный свет | `OUTSIDE_LIGHT` | `outside_light` | запасной свет, если зонный протух |
| `temp_water` | `TEMPERATURE` | `solution_temp_c` | бак `feed` |
| EC рабочего бака | `EC` | `ec_sensor` | только `feed` |
| pH рабочего бака | `PH` | `ph_sensor` | только `feed` |
| влажность субстрата | `SOIL_MOISTURE` | `soil_moisture` | субстрат |
| `level_solution_min` / `level_solution_max` | `WATER_LEVEL_SWITCH` | то же имя | бак `feed` |
| `level_clean_min` / `level_clean_max` | `WATER_LEVEL_SWITCH` | то же имя | бак `clean` |

Единица канала и единица цели фазы должны совпадать. Lux и PPFD, мСм/см и мкСм/см в одну потребность не складываются: потребность `unseen`, дозы нет. Синонимы вроде `moisture` или `ec_value` не читаются.

8. Команда узлу только через history-logger. Успех mutating-команды — только `DONE`. Имя command plan (`irrigation_start`, `solution_topup_start`) само по себе в gateway не отправляется: это ключ словаря AE3, не payload.
9. Пока код `ae3lite` не удалён, в процессе могут быть два воркера, у каждого свой claim, чтобы старый код не исполнил чужую задачу. Это не сохранение диспетчера. В проде воркер один. Общего цикла и второго сервиса в compose нет. С волны 3 форточки будит один tick нового воркера.
10. Привязка `drain` в сегодняшнем `ZoneReadinessService` — клапан `valve_drain`, слив рабочего бака. Бак стока с лотков — новая сущность 1.0.0: свои уровень и EC, свои строки в контракте каналов. Пока этих строк нет, контур со стоком не стартует и клапан слива за уровень не принимается.
11. Окна света может не быть. Тогда ночного запрета нет, полив идёт по интервалу, окно в ограничителе `unseen`.
12. `control_mode=manual` — тик воркера новых команд не публикует. Уже опубликованный `run_pump` с `duration_ms` гаснет таймером узла. Отдельной команды «полей сейчас» через старый ingress нет.
13. Тесты с БД — `make test-ae`, база `hydro_test`. Realhw агент не запускает.
14. В состоянии посадки и в новых именах нет режима `two_tank`.

### 1.2. Ограничения

1. Только список «Сделать» своей волны.
2. Скопированная функция без вызова удаляется в той же волне. Вызов из теста волны считается вызовом.
3. Момент полива — только `water_demand.py`. Разрешение дозы — только `dose_policy.py`. Сток — только `drain_policy.py`, и только после волны, которая завела каналы бака стока. Файла `policy.py` нет.
4. Машина стадий AE3 не переносится. Handler-ы `fill`, `irrigation`, `correction`, `recovery` не копируются. Совместимость поведения с AE3 не требуется. Из старого кода берутся узкие куски: клиент history-logger, семантика `DONE`, lease, один расчёт объёма импульса.
5. Не добавлять флаг «включить позже», пустой `Protocol` на один класс и совместимость с `degraded_run`.
6. Новый `.md` — только тот, который бриф назвал по имени.
7. Логи не содержат секретов узлов.
8. Исключение не глотается. `except: pass` и `logger.debug` вместо ошибки — бракованная сдача.
9. Формула, которая не сходится в единицах, не чинится коэффициентом. Волна останавливается.
10. Один тик — одна мутация: кадр целиком (его шаги открытия и `irrigation_stop`), или долив `feed` целиком, или набор `clean` целиком, или один импульс дозы. Второй перенос в том же тике не начинается.

### 1.3. Стиль

Образец структуры — `ae3lite`: `from __future__ import annotations`, аннотации, `@dataclass(frozen=True)` для результата, ключевые аргументы у публичных функций.

1. Имена и `reason_code` — английский `snake_case`. Текст человеку — русский.
2. Комментарии и docstring — русский. Комментарий только про неочевидное ограничение: единицу, протухший датчик, запрет кадра.
3. Отступ 4 пробела, строка до 100 символов, двойные кавычки.
4. PHP волны 3, которая удаляет диспетчер, — стиль соседних классов, комментарии на русском, Pint если файл уже в наборе Pint. Ветку «пропустить только `ae4`» не писать.
5. Английская проза в комментарии, `TODO` и закомментированный старый код — возврат волны.

---

## 2. Зафиксированное поведение

Источник истины — посадка и коридор её фазы. Гидравлика исполняет переносы. Имя топологии `two_tank_drip_substrate_trays` поведение посадки не выбирает.

Смена фазы сразу подменяет коридоры. Интеграл света прошлой фазы не переносится.

### 2.1. Контур этих волн

Сосуды: `clean` и `feed`. Переносы этих волн:

| Перенос | Смысл | Шаги из профиля зоны |
|---------|--------|----------------------|
| `supply_to_clean` | набрать бак чистой воды | открыть шаги `clean_fill_start`, закрыть шаги `clean_fill_stop`. Стоп по свежему `level_clean_max = 1` или по `runtime.clean_fill_timeout_sec` |
| `clean_to_feed` | долить или разбавить рабочий бак | открыть шаги `solution_fill_start`, закрыть шаги `solution_fill_stop`. Стоп по свежему `level_solution_max = 1` или по `runtime.solution_topup_timeout_sec` |
| `feed_to_plants` | один кадр капли | шаги `irrigation_start`, после `DONE` насоса шаги `irrigation_stop` |

Состав шагов уже зафиксирован в `ZoneLogicProfileNormalizer::TWO_TANK_REQUIRED_PLAN_CHANNELS`. Процедура читает из профиля зоны готовые `channel`, `cmd`, `params` этих ключей и публикует их по порядку. Имя ключа в gateway не отправляется. Канала из этого списка нет — сбой конфигурации, насос не включается. `sensor_mode_activate` в эти шаги не добавляется.

Для кадра насос в этих шагах — `pump_main` / `run_pump`, `duration_ms = duration_sec * 1000`, не ниже 1000 и не выше уже существующего потолка history-logger. Своего потолка не вводить. Клапаны кадра — `set_relay` `{"state": true}` до насоса и `{"state": false}` в `irrigation_stop`. Кадр целиком — одна мутация тика: стоп клапанов не откладывается на следующий проход.

`1` на `WATER_LEVEL_SWITCH` — датчик сработал, уровень достиг этой точки. `0` — не сработал. Пустой бак — свежий `level_*_min` равен 0. Ключа таймаута в runtime-плане зоны нет — клапан не открывается, это ошибка конфигурации §2.4. Дефолт 900 из схемы AE3 не подставляется.

Канал уровня не привязан или последняя проба старше `telemetry_max_age_sec` — тот же сбой, не тихий долив. Проба внутри окна свежести пустым баком не считается.

Порядок одного тика, первая подходящая мутация:

1. E-STOP — команд нет, задача fail-closed. Источник — уже записанное событие `emergency_stop_activated` (`zone_events`, тип после нормализации `EMERGENCY_STOP_ACTIVATED`). Новый протокол не заводится.
2. `feed` пуст и `clean` не пуст — `clean_to_feed`. Кадра нет.
3. `feed` пуст и `clean` пуст — `supply_to_clean`. Кадра нет.
4. С волны 4: EC `feed` свежий и выше коридора — импульса нет и кадра нет. Если `clean` не пуст, этот тик делает `clean_to_feed`. Если `clean` пуст — шаг 3. До волны 4 этот шаг не исполняется.
5. Дальше решают §2.2 и §2.3. Низкий EC кадр не запрещает, но в этом тике либо импульс, либо кадр.

Во время долива или набора свежий `level_*_min`, который был 1 и стал 0 до верхнего уровня, — стоп роли, закрытие, сбой §2.4. Это утечка, не повод открыть клапан снова.

`solution_drain_*`, подтверждение оператора и подмена раствора в 1.0.0 не входят. Агент не докопирует эти стадии в суточный полив и не в волну бака стока.

Тик без мутации задачу не создаёт и lease не берёт. Повтор того же `reason_code` не пишет новое `zone_events`. Смена причины пишет одно событие. Экран читает последнее такое событие даже без активной задачи. Пока задача или lease зоны живы, следующий тик её пропускает.

### 2.2. Полив

Один кадр длится `duration_sec` фазы. Свет, температура и влажность меняют момент, не длительность.

Пробуждение делает только воркер 1.0.0. Период — уже существующий `AE_RECONCILE_POLL_INTERVAL_SEC`. Новый cron и новое число секунд не заводятся. На каждом проходе воркер обходит зоны с активной посадкой.

Диспетчер Laravel не остаётся ни для каких зон. `automation:dispatch-schedules`, `ScheduleDispatcher` и расчёт окон (`shouldRunIntervalTask`, фаза `ready`, ключ идемпотентности окна, `biz_irrigation_window_missed`) удаляются в волне 3. Ветки «зоны `ae3` по-прежнему ждут окно» нет. Тесты этих окон удаляются вместе с классом, а не переписываются на пропуск `ae4`.

`POST /zones/{id}/start-irrigation`, `start-lighting-tick`, `start-solution-topup` и вызов `start-climate-tick` из диспетчера удаляются в той же волне. Кнопка экрана, которая бьёт в этот ingress, удаляется там же. Новый маршрут на её место не ставится. Отказ «для `ae4` нельзя, для `ae3` можно» не пишется.

Пока команда диспетчера ещё лежит в репозитории (волны 0–2), её не дорабатывают. Это не контракт совместимости. С волны 3 её нет, и зона со старым runtime сама не поливается: будить её больше нечем.

`interval_sec` уходит в `water_demand` как вход. Решение «кадр или нет» принимает тик воркера. Свет с волны 5 считает тот же тик. Форточки с волны 3 будит тот же тик, один `POST /greenhouses/{id}/start-climate-tick` на теплицу. Этот маршрут остаётся. `ae4` его обработчик не импортирует из `ae3lite`: до волны 8 отвечает прежний обработчик процесса. Диспетчер `dispatchDue` больше не вызывает. Волна 8 подменяет обработчик на `ae4/leaf_climate/`, второй tick не добавляет.

Пауза и якорь интервала:

- после любого кадра следующий не раньше `last_shot_at + duration_sec`;
- плановый интервал считается от `last_planned_shot_at`. Планового ещё не было — от `started_at` текущей фазы посадки;
- внеочередной кадр (сушь, VPD выше нормы, интеграл) — один флаг на полуинтервал `[last_planned_shot_at, last_planned_shot_at + interval_sec)`;
- пауза `duration_sec` не разрешает второй внеочередной в том же полуинтервале;
- плановый кадр, когда `now >= last_planned_shot_at + interval_sec` и пауза после любого кадра прошла, флаг внеочередного не занимает.

Обе метки времени читаются из последнего `zone_events` успешного кадра этой фазы. Отдельная таблица под них не заводится. Успешный кадр пишет событие с признаком `planned` или `extra`.

Суточный потолок, секунды:

```
если interval_sec отсутствует или <= 0, либо duration_sec отсутствует или <= 0:
    ошибка конфигурации, полива нет, деления нет
если окно света задано неполно (см. §1.1.6):
    ошибка конфигурации, полива нет
если окно задано:
    planned_shots = floor(длительность_окна_сек / interval_sec)
иначе:
    planned_shots = floor(86400 / interval_sec)
если planned_shots < 1:
    ошибка конфигурации, полива нет
daily_cap_sec = planned_shots * duration_sec
```

Ночной кадр по суши входит в тот же потолок и в тот же флаг внеочередного.

Интеграл света хранится один раз: сумма `значение * секунды` по `telemetry_samples` от `last_shot_at` этой фазы до сейчас. Кадра ещё не было — от `started_at` фазы. Смена фазы обнуляет точку так же, как новый кадр. Нет проб в окне или разрыв между пробами больше `telemetry_max_age_sec` — интеграл `unseen`, не ноль. Свет берётся как `LIGHT_INTENSITY` зонного канала, если протух — `OUTSIDE_LIGHT`. Разные единицы канала и `light_integral_per_shot` — `unseen`.

VPD, только если оба датчика свежие и в фазе есть `vpd_min` и `vpd_max`:

```
svp_kpa = 0.6108 * exp(17.27 * temp_c / (temp_c + 237.3))
vpd_kpa = svp_kpa * (1 - humidity_pct / 100)
```

Температура воздуха берётся каналом из §1.1, не `solution_temp_c`. Температура вне −40…60 °C или влажность вне 0…100 — VPD `unseen`. Целей VPD нет — температура и влажность момент полива не двигают. Константу кПа не подставлять.

Порядок кадра, первое сработавшее после паузы `last_shot_at + duration_sec`, проверки потолка и после того, как тик не занят мутацией §2.1:

1. E-STOP или пустой `feed` — кадра нет. Пустой `feed` уже ушёл в долив или набор §2.1.
2. Влажность свежая и выше `soil_moisture_max` — кадра нет.
3. Влажность свежая и ниже `soil_moisture_min` — один внеочередной кадр, если флаг этого полуинтервала свободен. Ночь не мешает суши.
4. Ночь (окно задано и сейчас вне него) и суши нет — кадра нет.
5. VPD ниже `vpd_min` и суши нет — кадра нет.
6. VPD выше `vpd_max` — один внеочередной кадр, если флаг свободен.
7. Интеграл достиг `light_integral_per_shot` — один внеочередной кадр, если флаг свободен.
8. Интервал планового кадра вышел — плановый кадр.

Нет влажности, нет пригодного VPD и нет пригодного интеграла — остаётся шаг 8, в том числе когда окна света нет. Шаг 8 не запрещает сутки без окна.

### 2.3. Соли и кислота

Только в `feed`. Один импульс за тик. Отдельной функции «сколько мл» в `correction_planner.py` нет: миллилитры считаются внутри `CorrectionPlanner` из ошибки и gain. Волна 4 переносит в `dose_policy.py` только эту арифметику одного шага: ошибка до цели, делённая на gain контроллера, не выше `controllers.{ec,ph}.max_dose_ml`. Серия observe и файл planner-а не копируются. `_dose_ml_to_ms` решает только, что импульс не ниже уже существующего `min_dose_ms`; в команду узла длительность не кладётся. Команда — `cmd=dose`, `params.ml`. Следующий импульс не раньше уже существующего `min_interval` контроллера.

EC или pH `feed` протух, единица не совпала с целью — импульса нет. Полив набранным раствором при протухшем датчике можно продолжать.

EC `feed` свежий и выше коридора — импульса нет и `feed_to_plants` нет. Тик уходит в разбавление §2.1. `failed` не ставится.

EC или pH `feed` свежий и ниже цели — один импульс к цели, не к верхнему краю коридора. В этом тике кадра нет: кадр, если он ещё нужен, сделает следующий тик после `DONE`.

Пока канала EC стока нет, доза смотрит только `feed`. Когда волна бака стока добавила канал и датчик свежий, высокий EC стока запрещает соль даже если `feed` ниже цели. Разбавление `feed`, который уже выше своего коридора, к этому каналу не привязано: оно действует с волны 4.

### 2.4. Сбой и решение политики

`state_details.failed` на экране зоны означает упавшую задачу. Пауза полива, которую политика выбрала правильно, туда не пишется.

Сбой — исключение, команда не `DONE`, потеря lease, E-STOP, пустая обязательная норма, нет привязки объявленного контура `feed_only`, нет привязки `level_solution_min` или её проба старше окна свежести. Его проводит только `ae4/infrastructure/failure_report.py`:

1. `logger.error` или `logger.exception`: `zone_id`, `grow_cycle_id`, `reason_code`, русский текст.
2. `zone_events` с тем же текстом.
3. `common.biz_alerts.send_biz_alert` с `dedupe_key`.
4. `state_details.failed = true`, `error_code`, `human_error_message`. Экран зоны уже это показывает. Новый экран не делать.

Неудача шага 3 логируется и не стирает текст шагов 1 и 4.

Сушь, ночь, VPD, долив вместо полива, EC выше коридора — поля решения посадки: `reason_code` и русский текст, `failed=false`. Они лежат в том же JSON состояния зоны, который экран уже запрашивает, рядом с `state_details`. Источник текста — последнее `zone_events` решения. Новую страницу и новый маршрут не делать. Если в уже открытом блоке состояния нет строки под этот текст, волна 2 добавляет строку туда.

Алерт «полив молчит дольше одного `interval_sec` из‑за бака или конфигурации» ставит тот же тик, который получил такую паузу. Канал — `failure_report`, отдельный `dedupe_key`, `failed` не ставится. Ночь, влажность в полосе и низкий VPD этот алерт не создают.

Ограничитель этих волн — худшая потребность текущего решения, одна функция, порядок: вода (пустой `feed`, нет кадра по конфигурации), питание, кислотность, климат листа, вода без влажности, кислород корня. Длительность «кто был below дольше всех за сутки» эти волны не считают и событие на каждый тик не пишут.

Климат листа и кислород корня в этих волнах только наблюдение, без команд. Климат листа: каналы воздуха §1.1 против уже существующих `temp_air_target` и `humidity_target`. Кислород корня: `solution_temp_c` против уже существующих `solution_temp_min` и `solution_temp_max`. Нормы нет — пункт в ограничителе отсутствует. Датчик протух при живой норме — `unseen`. Форточки из этого расчёта не едут. Нагрева раствора нет.

### 2.5. Бак стока

Это часть 1.0.0, не наследник графа two-tank. Волна заводит каналы сама и в той же волне правит `NODE_CHANNELS_REFERENCE.md` и модель данных. До этой правки исполняемого контура со стоком нет. Прошивку эта волна не пишет. Положительный путь теста — вставленная телеметрия. Узел, который начнёт публиковать новый канал, — отдельное решение после контракта. Нет канала в контракте — контур не стартует.

Объём кадра — `volume_ml` фазы. Его нет — `duration_sec` и расход калибровки насоса. Нет обоих — место в баке стока не оценивается, полив держит только свежий уровень не-max, доля стока `unseen`. Секунды полива на литры сами не умножаются.

Возврат в `feed` считается только при известных ёмкостях обоих баков и свежем EC стока, и только если смесь остаётся в коридоре. Нет ёмкостей — возврат запрещён, причина называется, сток удерживается или сбрасывается. Долю объёма агент не подставляет.

Подмена рабочего раствора в эту версию не входит. Command plan слива AE3 не используется ни для суточного стока, ни для подмены.

Форточки — один `POST /greenhouses/{id}/start-climate-tick` на `greenhouse_id` из тика воркера, с волны 3. Диспетчер его больше не вызывает. До волны 8 маршрут обслуживает прежний обработчик. Волна 8 подменяет обработчик, не добавляя второй tick.

---

## 3. Границы diff

1. `ae4/` не импортирует `ae3lite`. `common.db` и `common.biz_alerts` можно.
2. Нет второго сервиса в compose и нет переключателя, который гасит один из воркеров на весь процесс, пока идёт сборка. На cutover старый воркер удаляется кодом, не флагом.
3. Пока код `ae3lite` ещё в процессе, его claim берёт только `automation_runtime='ae3'`, новый воркер — только зоны `ae4`. С волны 3 зоны будит только новый воркер. Диспетчера и его `POST` полива, света и долива после волны 3 нет. `start-cycle` волна 3 не удаляет. Чужая задача не остаётся `pending`. После cutover старого claim нет. До волны 3 живые зоны на `ae4` не переводят: значение нужно тестам, диспетчер ещё создаёт задачи.
4. Одна активная задача на зону. Смена runtime при задаче или lease запрещена. Возврат зоны на `ae3` автоматику не включает: диспетчера, который её будил, нет. После cutover возврат — предыдущий образ сервиса.
5. Схему менять можно и нужно, если место нормы или сосуда иначе врёт. Только Laravel-миграция, в той же волне правка `DATA_MODEL_REFERENCE.md`, если таблица из контракта.
6. В новом пакете нет копий handler-ов fill, irrigation, correction, recovery и нет чужого `solution_drain` как суточного стока.
7. Новые YAML — `scenarios/ae4/` и только путь команды из §5. Старые сценарии AE3 не чинятся ради совместимости. На cutover они удаляются вместе с воркером, а не переписываются под новую логику.
8. Realhw не запускается. Канон, если оркестратор решит иначе: `doc_ai/13_TESTING/REALHW_TEST_NODE_AGENT_GUIDE.md`.
9. `ScheduleDispatcher` не патчится. После волны 3 класса, команды `automation:dispatch-schedules` и её регистрации в `routes/console.php` нет.

---

## 4. Что копировать

`ae3lite` — склад узких рабочих кусков, не каркас 1.0.0. После фильтра claim его больше не развивают. На cutover каталог удаляется.

| Откуда | Куда | Правка |
|--------|------|--------|
| `infrastructure/clients/history_logger_client.py` | `ae4/infrastructure/clients/` | импорты, префикс метрик `ae4_` |
| `infrastructure/gateways/sequential_command_gateway.py` | `ae4/infrastructure/gateways/` | успех только `DONE` |
| `infrastructure/repositories/zone_lease_repository.py` | `ae4/infrastructure/repositories/` | владелец `ae4` |
| `infrastructure/repositories/automation_task_repository.py` | `ae4/infrastructure/repositories/` | claim только `ae4` |
| `infrastructure/repositories/ae_command_repository.py` | `ae4/infrastructure/repositories/` | та же таблица, если FK не тащит handler AE3 |
| каркас `runtime/worker.py` | `ae4/runtime/worker.py` | свой цикл claim, не общий с AE3. Подъём своих `claimed` / `running` / `waiting_command` после рестарта. Задачи `ae3` не трогает |
| арифметика одного `dose_ml` внутри `CorrectionPlanner` | формула в `dose_policy.py` | файл planner-а не копировать. Команда `dose` + `params.ml` |
| ветка ON/OFF света в `cycle_start_planner.py` | `ae4/domain/light_policy.py` | окно фазы §1.1.6, OFF на границе |
| tick форточек `greenhouse_climate/` | `ae4/leaf_climate/`, волна 8 | один tick на `greenhouse_id`. Пробуждение с волны 3, эта волна меняет расчёт |
| `common/biz_alerts.py` не копировать | `ae4/infrastructure/failure_report.py` | единственный выход сбоя |

Не копировать: handler-ы fill, irrigation, correction, recovery, `workflow_topology.py` целиком, `irrigation_decision_controller.py`, `generic_cycle_start`, `TaskDecisionStrategy`, `SmartSoilDecisionStrategy`.

Процедура переноса собирает payload сама по таблице §2.1 и отдаёт его в gateway.

Тесты арифметики — `backend/services/automation-engine/tests/unit/ae4/`, БД `hydro_test`:

```bash
make test-ae PYTEST_ARGS="-q tests/unit/ae4"
```

Пути относительно `/app` контейнера. YAML-контракт:

```bash
cd tests/e2e && ./venv/bin/python -m pytest -q tests/test_ae4_scenario_contract.py
```

Node-sim на арифметике политики не гоняется.

---

## 5. Проверки

### YAML, путь команды

| Id | Волна | Что обязано случиться |
|----|-------|------------------------|
| E216 | 0 | Воркер AE3 не забирает задачу зоны `ae4`, воркер AE4 не забирает зону `ae3` |
| E215 | 0 | Смена runtime при задаче или lease отклоняется тем же `ZoneRuntimeSwitchDeniedException`. Свободная зона принимает `ae4`. Проверка диспетчер не создаёт и не удаляет |
| E219 | 2 | Тестовая команда доходит до history-logger; статус не `DONE` роняет шаг. Это ещё не кадр полива |
| E238 | 2 | Исключение: журнал, алерт, `failed=true`, русский `human_error_message` |
| E239 | 2 | Не-`DONE` показывает тот же русский текст на состоянии зоны |
| E240 | 2 | Сбой публикации алерта не стирает текст на зоне |
| E218 | 3 | E-STOP из уже существующего `zone_events` обрывает полив, нового топика нет |
| E221 | 3 | `feed_only` не шлёт команд слива и возврата стока |
| E205 | 4 | Протухший EC `feed`: в командах нет `dose` |

### Без YAML

| Id | Волна | Правило |
|----|-------|---------|
| E200 | 1 | Состояние посадки без имён стадий бака |
| E237 | 1 | Смена фазы обнуляет точку интеграла и берёт цели новой фазы |
| E202 | 1 | Потолок: окно есть — `floor(окно / interval) * duration`; окна нет — база 86400; `interval_sec <= 0` и `planned_shots < 1` — ошибка |
| E228 | 1 | Ночь и влажность в полосе — кадра нет; окна нет — ночь не запрещает интервал |
| E229 | 1 | Сушь — один внеочередной кадр на полуинтервал. После паузы `duration_sec` второй внеочередной в том же полуинтервале всё ещё запрещён. Плановый кадр этого флага не занимает |
| E230 | 1 | Интеграл с прошлого кадра достиг цели — один внеочередной кадр |
| E231 | 1 | VPD ниже нормы и суши нет — кадра нет. Температура раствора в VPD не подставляется |
| E232 | 1 | VPD выше нормы — один внеочередной кадр на полуинтервал, длительность `duration_sec` |
| E233 | 1 | Разные единицы света или редкие пробы — интеграл `unseen`, не ноль |
| E213 | 1 | Ограничитель — худшая потребность текущего решения. Климат листа и кислород корня входят как наблюдение, когда их нормы в фазе уже есть |
| E203 | 3 | Тик воркера: сушь даёт кадр, хотя `interval_sec` фазы не вышел. Intent Laravel не участвует |
| E246 | 3 | Команды `automation:dispatch-schedules` и класса `ScheduleDispatcher` нет. Их тесты удалены, не переписаны. Маршрутов `start-irrigation`, `start-lighting-tick`, `start-solution-topup` нет |
| E204 | 3 | Влажность выше нормы — кадра нет, алерта нет, второй такой тик не пишет новую `ae_tasks` |
| E217 | 2 | Вторая активная задача зоны не создаётся |
| E220 | 3 | Нет привязки контура `feed_only` или нет `level_solution_min` — `failed=true` |
| E244 | 3 | `feed` пуст и `clean` пуст — `supply_to_clean`, кадра в этом тике нет. Привязки `valve_clean_fill` нет — клапан не выдумывается |
| E206 | 4 | Свежий EC ниже цели — один `dose`, кадра в этом тике нет |
| E207 | 4 | EC `feed` выше коридора — нет `dose` и нет кадра, тик доливает `feed`, `failed` не ставится |
| E208 | 4 | Протухший pH — кислоты нет, полив набранным раствором не запрещён этим фактом |
| E209 | 4 | Свежий pH вне коридора и ниже цели — один импульс |
| E210 | 5 | Нет датчика света: ON в окне, OFF на границе. Окно — §1.1.6, вторая пара часов не читается |
| E214 | 6 | Новый sample получает `cycle_id` активной посадки |
| E245 | 7 | Нет канала стока в контракте — контур не стартует. Прошивки в diff нет |
| E211 | 8 | Нет свежей температуры воздуха — список команд форточек пуст. Tick на теплицу по-прежнему один |

---

## 6. Волны

```
0 ── 1 ── 2 ── 3 ── 4 ── 5 ── 7 ── 8 ── 9
          └──── 6 (history-logger, после 1)
```

Волна 5 может идти рядом с волной 4. Волна 6 не делит файлы с волнами 3–5. Волна 7 — бак стока, после 4. Волна 8 — расчёт форточек, после 3, файлы с 4, 5 и 7 не делит. Волна 9 — cutover, после 5, 6, 7 и 8. Первая к исполнению — волна 0. Следующую не открывать, пока её id из §5 не зелёные.

### Волна 0. Два воркера и вход

**Контекст.** `claim_next_pending` в `ae3lite/infrastructure/repositories/automation_task_repository.py` берёт любой `ae_tasks.status='pending'` с `due_at <= now`, без `zones.automation_runtime`. Check `zones_automation_runtime_check` разрешает только `ae3`. `ZoneController` принимает runtime только `in:ae3`. Запрет смены при задаче или lease уже есть: `ZoneRuntimeSwitchDeniedException` через `ZoneService`.

**Сделать.**

- Пакет `ae4/` и пустые `__init__.py`. `ae4/runtime/env.py` читает тот же `AE_RECONCILE_POLL_INTERVAL_SEC`, новое число не вводит.
- `backend/services/automation-engine/main.py` всегда поднимает воркер AE3 и воркер AE4. `AE_RUNTIME` процесс целиком не переключает. Форточки и диспетчер эта волна не трогает.
- Миграция Laravel: `zones_automation_runtime_check` допускает `ae3` и `ae4`. Откат миграции возвращает прежний check. `ZoneController` принимает оба значения. `tests/Feature/Ae3LiteSchemaTest.php` обновлён под новое значение. Смена при активной задаче или lease по-прежнему кидает `ZoneRuntimeSwitchDeniedException`.
- В claim AE3 к запросу добавлен `JOIN zones` и `zones.automation_runtime = 'ae3'`. Условие `due_at <= now` остаётся. Скопированный repository AE4 оставляет тот же запрос с `automation_runtime = 'ae4'`. Задача без `due_at` или с `due_at` в будущем этим claim не берётся. Тик волны 3 пишет `due_at = now`.
- Тест, что `ae4/` не импортирует `ae3lite`.
- `doc_ai/04_BACKEND_CORE/ae4.md` только про значение runtime, два claim, `due_at`, два воркера, тик воркера как единственное автоматическое пробуждение после волны 3, FSM задачи. Формул §2 в этом файле нет.

**Не делать.** Патчить `ScheduleDispatcher`. Переводить живые зоны на `ae4`. Отказ `POST` «для `ae4` нельзя». Удалять диспетчер.

**Критерии.** E215, E216 на `hydro_test`. Оба воркера живы без переменной, которая выключает один из них. `make up` без новых флагов по-прежнему поднимает процесс. Это не обещание сохранить AE3 в проде.

### Волна 1. Политика без насосов

**Сделать.** `planting.py`, `water_demand.py`, `dose_policy.py`. Одно место для `vpd_min`, `vpd_max`, `light_integral_per_shot` по §1.1.5. Окно света — §1.1.6, новой колонки под него нет. Наблюдение климата листа и кислорода корня по §2.4, без команд. Влажность — `soil_moisture_min` / `soil_moisture_max`.

**Не делать.** `policy.py`, `drain_policy.py`, `circuit.py` с третьим баком, HTTP ради арифметики, чтение `TEMPERATURE` без канала.

**Тесты.** E200, E237, E202, E228, E229, E230, E231, E232, E233, E213. Все pytest.

**Критерии.** `degraded_run` отсутствует. Одна функция ограничителя. Интеграл не считается «с начала фазы» отдельным store. Норма из рецепта доезжает до снимка посадки.

### Волна 2. Исполнение команды

**Сделать.** Клиент, gateway, lease, task repository, каркас воркера, подъём своих незавершённых задач после рестарта, `failure_report.py`. Воркер переводит исключение и не-`DONE` в сбой §2.4. Ответ состояния зоны, который экран уже запрашивает, отдаёт и `state_details`, и последнее решение посадки с `failed=false`. Строка текста — в уже открытом блоке этого экрана. Перед копированием `ae_command_repository` проверить FK. FK, которая требует handler AE3, — остановка волны.

**Не делать.** Переносы воды и копирование handler-ов. Новый маршрут и новую страницу.

**Тесты.** E217, E219, E238, E239, E240. Первые YAML лежат в `tests/e2e/scenarios/ae4/`. Контракт формы — `tests/e2e/tests/test_ae4_scenario_contract.py`, его создаёт эта волна. Кадр полива сюда не входит.

**Критерии.** `send_biz_alert` вызывается только из `failure_report`. Воркер AE3 не берёт зону `ae4`. Рестарт не подхватывает чужой runtime.

### Волна 3. Долив, набор чистой воды и кадр

**Сделать.** Короткие процедуры §2.1. Тик воркера обходит зоны с активной посадкой и берёт момент кадра из `water_demand`. Задача мутации пишется с `due_at = now`. Повтор того же `reason_code` не пишет новую `ae_tasks`. Долив и набор не делят тик с кадром. E-STOP — из `zone_events`. Удалить, не патчить:

- команду `automation:dispatch-schedules` и её регистрацию в `backend/laravel/routes/console.php`;
- `App\Console\Commands\AutomationDispatchSchedules`;
- `App\Services\AutomationScheduler\ScheduleDispatcher`;
- маршруты и контроллеры `start-irrigation`, `start-lighting-tick`, `start-solution-topup`;
- кнопку в `ZoneAutomationOpsPanel.vue` и вызов из `Pages/Zones/Show.vue`, которые ходят в `start-irrigation`;
- тесты, которые конструируют `ScheduleDispatcher` или гоняют `automation:dispatch-schedules`.

`start-cycle` не удалять. Пробуждение форточек — один `POST /greenhouses/{id}/start-climate-tick` из тика, без импорта `ae3lite` и без `dispatchDue`. Маршрут остаётся на прежнем обработчике до волны 8.

**Не делать.** Копировать handlers. `sensor_mode_activate`. Команды слива, возврата стока, подмены. Принимать `valve_drain` за уровень стока. Оставлять диспетчер для старых зон. Писать ветку пропуска `ae4`. Ждать intent от Laravel. Заводить второй расчёт форточек.

**Тесты.** E218, E221, E220, E203, E204, E244, E246. E203 — pytest тика воркера.

**Критерии.** В трассе нет `solution_drain` и нет стадии `tank_filling` как фазы растения. Payload кадра содержит `pump_main` / `run_pump` / `duration_ms`, а не строку имени plan.

### Волна 4. Один импульс

**Сделать.** `dose_policy` считает один `dose_ml` по §2.3 и публикует `cmd=dose` с `params.ml`. Протухший или чужой по единицам EC/pH — импульса нет, кадр этим фактом не запрещён. EC или pH выше коридора — нет импульса и нет кадра, тик разбавляет `feed` по §2.1. Ниже цели — один импульс, кадр переносится на следующий тик.

**Не делать.** Серию до цели, дозу по EC стока, копирование correction handler и файла planner-а.

**Тесты.** E205 (YAML: нет `dose`), E206, E207, E208, E209. E207 не ставит `failed`.

### Волна 5. Свет

**Сделать.** `light_policy.py`: ON внутри окна §1.1.6, OFF на границе, яркость фазы, датчик света не обязателен. Тот же тик зоны вызывает свет. Если этот проход уже занят кадром, доливом, набором или импульсом, свет уходит на следующий проход, когда lease свободен.

**Не делать.** Второе окно из фотопериода поверх уже заданной пары `on_time` / `off_time`. Копировать tick форточек в зону. Возвращать `start-lighting-tick`.

**Тесты.** E210.

### Волна 6. Телеметрия посадки

Каталог `backend/services/history-logger/`. Колонка `telemetry_samples.cycle_id` есть, ingest её не пишет.

**Сделать.** Sample зоны с одной активной посадкой получает её `grow_cycles.id`. Нет посадки — `NULL`. В MQTT поле не добавляется. Абзац в `DATA_MODEL_REFERENCE.md` и `TELEMETRY_PIPELINE.md`.

**Тесты.** E214 и pytest history-logger.

### Волна 7. Бак стока

После волны 4.

**Сделать.** Каналы уровня бака стока и EC стока в `NODE_CHANNELS_REFERENCE.md`, миграция ёмкостей, `drain_policy.py` по §2.5. Готовность зоны отличает этот бак от `valve_drain`. Доза начинает учитывать свежий EC стока. Прошивка не входит в diff.

**Не делать.** Стартовать контур со стоком, если каналов нет в контракте. Считать литры из секунд. Подставлять долю возврата без ёмкостей. Писать прошивку. Делать подмену раствора и подтверждение оператора.

**Тесты.** E245 и pytest: полный бак — кадра нет и `failed` не ставится; EC стока выше коридора — нет соли; возврат без ёмкостей не создаёт команду.

### Волна 8. Расчёт форточек

После волны 3. Рядом с 4, 5 и 7, если не трогает их файлы.

**Сделать.** Узкий расчёт на `greenhouse_id` в `ae4/leaf_climate/`. Нет свежей температуры воздуха — пустой список команд. Lease теплицы — уже существующий. Пробуждение уже сделала волна 3. Эта волна подменяет расчёт, второй tick не добавляет.

**Не делать.** Копировать decision engine в цикл зоны. Добавлять нагрев. Возвращать `dispatchDue` в Laravel.

**Тесты.** E211.

### Волна 9. Cutover, версия 1.0.0

После волн 5, 6, 7 и 8.

**Сделать.** В процессе остаётся один воркер. `ae3lite` удаляется. Форточки остаются одним tick воркера на теплицу. Зона без явного старого runtime обслуживается новым. Версия сервиса `1.0.0`. Формулы §2 один раз переносятся в `doc_ai/04_BACKEND_CORE/ae4.md`, этот файл остаётся планом волн. Экран зоны не пишет «AE3» и не пишет «четвёртая версия».

**Критерии.** Стенд поднимается без пакета `ae3lite`. Тесты нового runtime зелёные. Тесты, которые проверяли машину стадий AE3, удалены вместе с ней, а не подделаны под новый код. Второго tick форточек в процессе нет.

---

## 7. Бриф агенту

```
Роль: разработчик волны <N> AE4.
Прочитай §§1.1–1.3, §2, §3 и бриф волны <N> в
doc_ai/04_BACKEND_CORE/AE4_AGENT_EXECUTION_PLAN.md.
ae4.md читай только как контракт runtime, не как формулы.
AE3 не сохраняй. Это не его следующая версия, а прод 1.0.0.
Допущения не пересматривай. Число вместо пустой нормы не подставляй.
Не копируй handlers fill, irrigation, correction, recovery.
Не отправляй в gateway имя ключа плана. Бери channel, cmd, params шагов из профиля зоны, список в §2.1.
dose_ml считай по §2.3. Отдельную функцию из correction_planner не ищи.
Claim берёт только due_at <= now и свой automation_runtime.
Не называй valve_drain баком стока. policy.py не создавай.
Не читай TEMPERATURE без канала. Температуру раствора в VPD не подставляй.
Комментарии русские. reason_code английский.
Сбой — failure_report и failed=true. Пауза политики — текст в состоянии посадки, без failed.
Один тик — одна мутация воды или дозы. Будит только воркер.
Диспетчер Laravel не патчить и не оставлять для старых зон. С волны 3 его нет.
POST start-irrigation / start-lighting-tick / start-solution-topup не сохранять.
Повтор того же reason_code — без новой ae_tasks.
Тесты: только id своей волны из §5. БД — make test-ae и hydro_test.
E246 — команда и класс диспетчера удалены. E203 — pytest тика воркера.
Сдай файлы, команды, итог, что удалил при копировании.
```

Оркестратор дописывает в бриф только факт репозитория, которого нет в плане.

---

## 8. Закрытие волны

1. В `ae4/` нет `import ae3lite` и нет файлов handler-ов fill, irrigation, correction, recovery.
2. Прогон из брифа зелёный, команда есть в отчёте.
3. YAML только из таблицы §5. Правка старых сценариев AE3 ради «чтобы старое ещё проходило» — возврат, если волна не cutover.
4. `ae4.md` волны 0 не содержит формул §2.
5. Нет `policy.py`, нет второго расчёта VPD и дозы вне названных модулей.
6. Комментарии русские. `TODO` и английская проза в комментарии — возврат.
7. `except: pass` и сбой без `failure_report` — возврат. Ночной пропуск не создаёт алерт и не ставит `failed`.
8. В отчёте перечислено удалённое при копировании. Мёртвый скопированный код — возврат.
9. Лишних `.md` нет.
10. После волны 3 в diff нет `ScheduleDispatcher`, команды `automation:dispatch-schedules` и ветки «старые зоны ещё ждут окно». Тесты диспетчера удалены, не адаптированы.
11. В командах нет строкового имени plan вместо `cmd` и канала.

После волны 9 — общий `make test-ae` нового runtime и контракт YAML. Node-sim на арифметику не обязателен. Realhw в план не входит.

---

## 9. Конец плана

В проде работает AE 1.0.0, один воркер. Посадка и коридор фазы решают. Момент кадра, долива, света и форточек назначает тик этого воркера. Диспетчера Laravel нет. Кадр слушает сушь, VPD, интеграл и интервал, не заливает лоток вторым внеочередным кадром в том же полуинтервале и не выходит за потолок окна. Пустой рабочий бак доливается из `clean`. Пустой `clean` набирается своей ролью `valve_clean_fill`, если она привязана. EC `feed` выше коридора в субстрат не льётся: тик разбавляет бак. Соль и кислота — один импульс в `feed` отдельным тиком. Свежий EC стока умеет этот импульс запретить. Бак стока — свой сосуд со своими каналами в контракте. Свет гаснет по уже существующему окну. Форточки — один tick на теплицу. Сбой задачи виден на экране. Правильная пауза политики `failed` не ставит и не плодит задачу на каждый проход. Sample пишется на цикл посадки. Кода `ae3lite` в процессе нет.

Грунт, поле и подмена рабочего раствора этим планом не запускаются.

---

## 10. Чего планом не хватает до боевой теплицы

Эти пункты сознательно вне волн. Пока их нет, зелёный прогон 1.0.0 не равен теплице, которую можно оставить одну.

1. Узел, который публикует уровень и EC бака стока. Контракт и тест со вставленной телеметрией контур на стенде не поднимают.
2. Подмена рабочего раствора и подтверждение оператора. Слив `feed` в 1.0.0 нечем заменить, кроме ручного режима.
3. Нагрев раствора, CO2, туман. Кислород корня только пишется в ограничитель.
4. Прогон на физической ноде и хотя бы один полный фотопериод на живой посадке. Realhw в волны не входит.
5. После cutover откат зоны флагом runtime невозможен. Нужен предыдущий образ и решение, кто его выкладывает.
6. Суточный ограничитель «какая потребность была ниже коридора дольше всех» не считается. На экране видна только худшая потребность текущего решения.
7. Расход насоса и ёмкости баков, если их нет в калибровке, оставляют долю стока и возврат `unseen`. Боевой контур со стоком без этих чисел возврат не откроет.
