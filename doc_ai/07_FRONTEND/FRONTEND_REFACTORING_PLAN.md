# План рефакторинга фронтенда на основе UI референсов

**Дата создания:** 2025-01-27  
**Версия:** 1.0  
**Статус:** В работе  
**Основа:** UI референсы из `docs/ui_refs/`

---

## 📋 Содержание

1. [Анализ текущего состояния vs референсы](#анализ-текущего-состояния-vs-референсы)
2. [Цели рефакторинга](#цели-рефакторинга)
3. [Синхронизация с бэкендом и сервисами](#синхронизация-с-бэкендом-и-сервисами)
4. [Совместимость и ограничения (AI_ASSISTANT_DEV_GUIDE.md)](#совместимость-и-ограничения-ai_assistant_dev_guidemd)
5. [Улучшения на основе лучших практик](#улучшения-на-основе-лучших-практик)
6. [Вопросы для уточнения](#вопросы-для-уточнения)
7. [Критичные задачи (исправление проблем)](#критичные-задачи-исправление-проблем)
8. [Детальный план по референсам](#детальный-план-по-референсам)
9. [Приоритизация задач](#приоритизация-задач)
10. [Этапы реализации](#этапы-реализации)
11. [Метрики успеха](#метрики-успеха)
12. [Тестирование и качество](#тестирование-и-качество)

---

## Анализ текущего состояния vs референсы

### Текущее состояние UI

**Сильные стороны:**
- ✅ Минималистичный дизайн (Vercel/Dashboard стиль)
- ✅ Темная тема по умолчанию
- ✅ Компонентная архитектура
- ✅ Real-time обновления через WebSocket
- ✅ Адаптивный дизайн (базовый)

**Области для улучшения:**
- ⚠️ Недостаточная информационная плотность (vs промышленные дашборды)
- ⚠️ Графики телеметрии можно сделать более информативными (IoT стиль)
- ⚠️ Недостаточно визуальных индикаторов статусов (SCADA стиль)
- ⚠️ Мобильная версия требует улучшения (ref_08)
- ⚠️ Недостаточно агрономических паттернов (ref_02, ref_05)
- 🔴 **КРИТИЧНО:** Некорректно отображается разметка на страницах
- 🔴 **КРИТИЧНО:** 500 ошибки на страницах требуют исправления
- 🔴 **КРИТИЧНО:** AI прогнозы не отображаются на фронтенде
- 🔴 **КРИТИЧНО:** Мастер добавления нового цикла выращивания требует значительного улучшения

---

## Цели рефакторинга

### Критичные цели (исправление проблем)

1. **Исправление критичных ошибок**
   - 🔴 Исправить некорректную разметку на страницах
   - 🔴 Устранить 500 ошибки на страницах
   - 🔴 Добавить отображение AI прогнозов на фронтенде
   - 🔴 Значительно улучшить мастер добавления нового цикла выращивания

2. **Стабильность и качество кода**
   - Покрытие тестами критичных компонентов (80%+)
   - Стабильный, надежный код без багов
   - Правильная обработка ошибок и edge cases

### Основные цели

3. **Промышленный стиль (ref_01, ref_06)**
   - Увеличить информационную плотность дашбордов
   - Добавить визуальные индикаторы статусов оборудования
   - Улучшить организацию данных на экранах
   - **Приоритет:** Дашборды первые, затем поэтапно весь фронт

4. **Агрономический UX (ref_02, ref_05)**
   - Специализированные паттерны для работы с зонами выращивания
   - Визуализация данных о растениях и фазах роста
   - Улучшенное отображение рецептов
   - Красивый лаконичный дизайн страниц

5. **IoT платформа (ref_04)**
   - Профессиональная организация телеметрии устройств
   - Улучшенные графики временных рядов
   - Управление устройствами через веб-интерфейс
   - Исторические данные: по умолчанию 1 день, выбор периодов (сутки, 7 дней, месяц)

6. **Адаптивный дизайн (ref_08)**
   - Адаптивный дизайн для мобильных устройств (полноценное мобильное приложение будет отдельно)
   - Компактное отображение метрик
   - Оптимизированная навигация

7. **Энергетические дашборды (ref_07)**
   - Заглушки для визуализации энергопотребления
   - TODO для будущей реализации
   - Мониторинг систем питания

### Технические требования

- **Язык интерфейса:** Только русский язык везде на фронтенде
- **Библиотеки:** Нет ограничений, можно использовать любые современные библиотеки
- **Браузеры:** Поддержка только современных браузеров (старые не нужны)
- **Дизайн:** Дизайн будет создан ИИ агентом по примерам из референсов
- **Роли:** Поддержка ролей Agronomist, Engineer, Operator

---

## Синхронизация с бэкендом и сервисами

### Доступные API endpoints (Laravel)

#### Телеметрия

| Endpoint | Метод | Описание | Статус |
|----------|-------|----------|--------|
| `/api/zones/{id}/telemetry/last` | GET | Последние значения телеметрии зоны | ✅ Доступен |
| `/api/zones/{id}/telemetry/history` | GET | История телеметрии (метрика, from, to) | ✅ Доступен |
| `/api/telemetry/aggregates` | GET | Агрегированные данные (zone_id, metric, period: 1h/24h/7d/30d) | ✅ Доступен |
| `/api/nodes/{id}/telemetry/last` | GET | Последние значения телеметрии ноды | ✅ Доступен |
| `/api/nodes/{id}/telemetry/history` | GET | История телеметрии ноды | ✅ Доступен |

**Формат aggregates:**
```json
{
  "status": "ok",
  "data": [
    {
      "ts": "2025-01-27T10:00:00Z",
      "avg": 6.2,
      "min": 5.8,
      "max": 6.5,
      "median": 6.1
    }
  ]
}
```

#### Зоны

| Endpoint | Метод | Описание | Статус |
|----------|-------|----------|--------|
| `/api/zones/{id}` | GET | Детали зоны + активный рецепт | ✅ Доступен |
| `/api/zones/{id}/snapshot` | GET | Snapshot зоны для reconciliation | ✅ Доступен |
| `/api/zones/{id}/cycles` | GET | Данные cycles зоны | ✅ Доступен |
| `/api/zones/{id}/events` | GET | События зоны | ✅ Доступен |
| `/api/zones/{id}/grow-cycle` | GET | Активный цикл выращивания | ✅ Доступен |
| `/api/zones/{id}/commands` | POST | Отправка команды зоне | ✅ Доступен |
| `/api/zones/{id}/pause` | POST | Приостановить зону | ✅ Доступен |
| `/api/zones/{id}/resume` | POST | Возобновить зону | ✅ Доступен |
| `/api/zones/{id}/next-phase` | POST | Следующая фаза рецепта | ✅ Доступен |

#### Рецепты

| Endpoint | Метод | Описание | Статус |
|----------|-------|----------|--------|
| `/api/recipes/{id}` | GET | Детали рецепта с фазами | ✅ Доступен |
| `/api/recipes/{id}/analytics` | GET | Аналитика по рецепту | ✅ Доступен |
| `/api/recipes/comparison` | POST | Сравнение рецептов | ✅ Доступен |

#### AI и симуляции

| Endpoint | Метод | Описание | Статус |
|----------|-------|----------|--------|
| `/api/ai/predict` | POST | Прогноз параметра (zone_id, metric_type, horizon_minutes) | ✅ Доступен |
| `/api/ai/explain_zone` | POST | Объяснение состояния зоны | ✅ Доступен |
| `/api/ai/recommend` | POST | Рекомендации для зоны | ✅ Доступен |
| `/api/ai/diagnostics` | POST | Диагностика зоны | ✅ Доступен |
| `/api/simulations/zone/{zone}` | POST | Запуск симуляции зоны (асинхронно) | ✅ Доступен |
| `/api/simulations/{jobId}` | GET | Статус симуляции | ✅ Доступен |

#### Отчеты и аналитика

| Endpoint | Метод | Описание | Статус |
|----------|-------|----------|--------|
| `/api/zones/{id}/harvests` | GET | История урожаев зоны | ✅ Доступен |
| `/api/profitability/calculate` | POST | Расчет прибыльности | ✅ Доступен |
| `/api/profitability/plants/{plant}` | GET | Прибыльность по культуре | ✅ Доступен |

### Python сервисы

#### history-logger (порт 9300)
- **Функционал:** Запись телеметрии в PostgreSQL, публикация команд в MQTT
- **REST API:**
  - `POST /commands` — универсальный endpoint для команд
  - `POST /zones/{zone_id}/commands` — команды для зоны
  - `POST /nodes/{node_uid}/commands` — команды для ноды
  - `GET /health` — health check

#### automation-engine (порт 9405)
- **Функционал:** Контроллер зон, проверка targets, публикация команд
- **REST API:**
  - `POST /scheduler/command` — прием команд от scheduler
  - `GET /health` — health check

#### digital-twin (порт 8003)
- **Функционал:** Симуляция зон на основе Digital Twin моделей
- **REST API:**
  - `POST /simulate/zone` — запуск симуляции
  - `POST /calibrate/zone/{zone_id}` — калибровка моделей

#### scheduler (порт 9402)
- **Функционал:** Расписания поливов/света из recipe phases
- **Метрики:** Prometheus metrics

### Необходимые новые endpoints для рефакторинга

#### Для промышленного стиля Dashboard

| Endpoint | Метод | Описание | Приоритет |
|----------|-------|----------|-----------|
| `/api/dashboard/metrics` | GET | Агрегированные метрики для Dashboard (все зоны) | 🔴 Высокий |
| `/api/dashboard/trends` | GET | Тренды метрик за период (для sparklines) | 🔴 Высокий |
| `/api/zones/{id}/status-history` | GET | История изменений статусов зоны | 🟡 Средний |

#### Для IoT стиля графиков

| Endpoint | Метод | Описание | Приоритет |
|----------|-------|----------|-----------|
| `/api/zones/{id}/telemetry/multi-series` | GET | Мульти-серии данные (несколько метрик) | 🔴 Высокий |
| `/api/zones/{id}/telemetry/anomalies` | GET | Обнаруженные аномалии в телеметрии | 🟡 Средний |
| `/api/zones/{id}/telemetry/export` | GET | Экспорт телеметрии (CSV/JSON) | 🟢 Низкий |

#### Для агрономического стиля

| Endpoint | Метод | Описание | Приоритет |
|----------|-------|----------|-----------|
| `/api/zones/{id}/phase-progress` | GET | Детальный прогресс текущей фазы | 🔴 Высокий |
| `/api/recipes/{id}/phases-timeline` | GET | Timeline всех фаз рецепта | 🟡 Средний |
| `/api/zones/{id}/growth-history` | GET | История фаз роста зоны | 🟡 Средний |

#### Для SCADA стиля

| Endpoint | Метод | Описание | Приоритет |
|----------|-------|----------|-----------|
| `/api/dashboard/widgets` | GET | Конфигурация виджетов дашборда | 🟡 Средний |
| `/api/dashboard/widgets` | PUT | Сохранение конфигурации виджетов | 🟡 Средний |
| `/api/zones/{id}/status-timeline` | GET | Timeline изменений статусов | 🟢 Низкий |

#### Для мобильной версии

| Endpoint | Метод | Описание | Приоритет |
|----------|-------|----------|-----------|
| `/api/zones/{id}/telemetry/summary` | GET | Компактная сводка телеметрии | 🔴 Высокий |
| `/api/dashboard/mobile` | GET | Оптимизированные данные для мобильных | 🟡 Средний |

### Интеграция с существующими данными

#### Данные из Inertia props (уже доступны)

**Dashboard:**
- `dashboard.greenhousesCount`, `dashboard.zonesCount`, `dashboard.devicesCount`, `dashboard.alertsCount`
- `dashboard.zonesByStatus`, `dashboard.nodesByStatus`
- `dashboard.problematicZones`, `dashboard.greenhouses`, `dashboard.zones`
- `dashboard.latestAlerts`

**Zone Show:**
- `zone` — полная модель зоны с отношениями
- `telemetry` — последние значения метрик
- `targets` — цели текущей фазы
- `current_phase` — нормализованные данные текущей фазы
- `active_grow_cycle` — активный цикл выращивания
- `devices` — устройства зоны
- `events` — события зоны
- `cycles` — данные cycles

#### Данные через REST API (для real-time обновлений)

- Телеметрия: `/api/telemetry/aggregates` (уже используется в `useTelemetry`)
- Команды: WebSocket через `commands.{zoneId}` канал
- События: WebSocket через `events.global` канал

### План реализации endpoints на бэкенде

#### Этап 1 (критичные endpoints)

**Неделя 1:**
- [ ] `GET /api/dashboard/metrics` — агрегированные метрики для Dashboard
- [ ] `GET /api/dashboard/trends` — тренды для sparklines
- [ ] `GET /api/zones/{id}/telemetry/multi-series` — мульти-серии данные

**Неделя 2:**
- [ ] `GET /api/zones/{id}/phase-progress` — детальный прогресс фазы
- [ ] Улучшить `/api/telemetry/aggregates` для поддержки нескольких метрик одновременно

#### Этап 2 (важные endpoints)

**Неделя 4-5:**
- [ ] `GET /api/recipes/{id}/phases-timeline` — timeline фаз рецепта
- [ ] `GET /api/zones/{id}/telemetry/anomalies` — обнаружение аномалий
- [ ] `GET /api/dashboard/widgets` — конфигурация виджетов

#### Этап 3 (желательные endpoints)

**Неделя 8-9:**
- [ ] `GET /api/zones/{id}/telemetry/export` — экспорт данных
- [ ] `GET /api/zones/{id}/status-timeline` — timeline статусов
- [ ] `GET /api/dashboard/mobile` — оптимизированные данные для мобильных

### Использование существующих возможностей

#### AI прогнозы (уже доступны)
- Использовать `/api/ai/predict` для отображения прогнозов на графиках
- Использовать `/api/ai/explain_zone` для объяснений состояния зоны

#### Симуляции (уже доступны)
- Использовать `/api/simulations/zone/{zone}` для запуска симуляций
- Отображать результаты симуляций на графиках

#### Аналитика рецептов (уже доступна)
- Использовать `/api/recipes/{id}/analytics` для аналитики
- Использовать `/api/recipes/comparison` для сравнения рецептов

---

## Совместимость и ограничения (AI_ASSISTANT_DEV_GUIDE.md)

Этот раздел определяет правила совместимости для рефакторинга фронтенда согласно `AI_ASSISTANT_DEV_GUIDE.md`. Все изменения должны учитывать весь pipeline системы и не нарушать существующую архитектуру.

### Pipeline системы

```
ESP32 → MQTT → Python → PostgreSQL → Laravel → Vue → User
```

**Каждое изменение фронтенда должно учитывать ВСЕ 6 уровней.**

### Что НЕЛЬЗЯ изменять

#### ❌ MQTT протокол
- **Запрещено:** Менять форматы MQTT-топиков (`hydro/{gh}/{zone}/{node}/{channel}/{message_type}`)
- **Запрещено:** Менять структуру payload телеметрии (`{value, ts}`)
- **Запрещено:** Менять структуру команд (`{cmd, value, ttl_ms, reason, request_id}`)
- **Разрешено:** Использовать существующие топики и payload через Laravel API

#### ❌ Схемы БД с обратной несовместимостью
- **Запрещено:** Удалять существующие таблицы или модели
- **Запрещено:** Переименовывать ключевые поля (`zones.id`, `nodes.uid`, `channels.key`)
- **Запрещено:** Менять типы данных обязательных полей
- **Разрешено:** Добавлять новые таблицы и поля (nullable или с default)

#### ❌ Inertia props структура
- **Запрещено:** Менять структуру props без обновления Vue компонентов
- **Запрещено:** Удалять существующие props, используемые Vue компонентами
- **Разрешено:** Добавлять новые props (backward compatible)

#### ❌ Команды узлам
- **Запрещено:** Обходить Python-сервис и отправлять команды напрямую в MQTT
- **Запрещено:** Менять формат команд (`cmd`, `value`, `ttl_ms`, `reason`, `request_id`)
- **Разрешено:** Использовать существующие Laravel endpoints (`/api/zones/{id}/commands`)

### Что МОЖНО изменять

#### ✅ Фронтенд (Vue 3)
- Добавлять новые UI компоненты
- Улучшать UX и визуализацию
- Добавлять новые страницы и маршруты
- Оптимизировать производительность

#### ✅ Laravel Backend
- Добавлять новые маршруты и контроллеры
- Расширять существующие endpoints (backward compatible)
- Добавлять новые модели и таблицы
- Улучшать алгоритмы обработки данных

#### ✅ Python сервисы
- Улучшать алгоритмы контроллеров
- Добавлять обработку новых типов данных (если они не ломают существующие)
- Оптимизировать производительность

### Зоны ответственности для рефакторинга

| Компонент | Что можно менять | Что нельзя |
|-----------|------------------|------------|
| **Vue 3** | Добавлять UI, улучшать UX, создавать новые компоненты | Ломать структуру props, удалять существующие компоненты |
| **Laravel** | Добавлять маршруты, модели, страницы, расширять endpoints | Менять Inertia формат без обновления Vue, удалять существующие endpoints |
| **PostgreSQL** | Добавлять таблицы, поля (nullable или с default) | Удалять/переименовывать обязательные поля |
| **MQTT** | Использовать существующие топики через Laravel API | Менять форматы топиков или payload |
| **Python-сервис** | Улучшать алгоритмы обработки | Менять command format, обходить history-logger |

### Проверка совместимости на каждом уровне

#### 1. ESP32 → MQTT
- ✅ **Проверка:** Фронтенд НЕ взаимодействует напрямую с ESP32 или MQTT
- ✅ **Вывод:** Рефакторинг фронтенда не влияет на этот уровень

#### 2. MQTT → Python
- ✅ **Проверка:** Все команды отправляются через Laravel → Python → MQTT
- ✅ **Вывод:** Рефакторинг фронтенда не влияет на формат MQTT сообщений

#### 3. Python → PostgreSQL
- ⚠️ **Проверка:** Новые endpoints могут требовать новых запросов к БД
- ✅ **Требование:** Новые запросы должны использовать существующие таблицы или добавлять новые (без удаления старых)

#### 4. PostgreSQL → Laravel
- ⚠️ **Проверка:** Новые endpoints могут требовать новых моделей или расширения существующих
- ✅ **Требование:** Все изменения должны быть backward compatible

#### 5. Laravel → Vue
- ⚠️ **Проверка:** Новые endpoints могут изменять структуру данных
- ✅ **Требование:** Существующие Inertia props не должны изменяться без обновления Vue компонентов

#### 6. Vue → User
- ✅ **Проверка:** UI должен оставаться понятным и функциональным
- ✅ **Требование:** Все изменения должны улучшать UX, а не ухудшать его

### Чек-лист совместимости перед изменениями

**Перед началом реализации каждой задачи:**

- [ ] **MQTT протокол:** Изменение не требует изменения форматов MQTT-топиков или payload?
- [ ] **Telemetry:** Телеметрия сохраняется полностью через существующие endpoints?
- [ ] **Pipeline:** Проверены все 6 уровней pipeline (ESP32 → MQTT → Python → PostgreSQL → Laravel → Vue)?
- [ ] **Python:** Python-сервис обработает все новые данные через существующие endpoints?
- [ ] **Laravel:** Laravel получит понятные props и не сломает существующие Inertia страницы?
- [ ] **Vue:** Vue корректно отобразит новые данные без удаления существующих компонентов?
- [ ] **БД:** Схема БД согласована с Laravel и Python (новые таблицы/поля не ломают существующие)?
- [ ] **Команды:** Команды узлам формируются через существующие Laravel endpoints (не напрямую в MQTT)?
- [ ] **Backward compatibility:** Никаких backward-breaking изменений в существующих API?

### Версионирование

**Текущая совместимость:**
```
Compatible-With:
  Protocol: 2.0
  Backend: >=3.0
  Python: >=3.0
  Database: >=3.0
  Frontend: >=3.0
```

**Все изменения рефакторинга должны быть совместимы с версией 2.0 протокола.**

### Примеры безопасных изменений

#### ✅ Безопасно: Добавление нового компонента Dashboard
```vue
<!-- Новый компонент не влияет на существующие -->
<MetricIndicator :value="phValue" />
```
- Не изменяет существующие props
- Не требует изменений в MQTT или Python
- Использует существующие данные

#### ✅ Безопасно: Добавление нового endpoint для агрегации
```php
// Новый endpoint расширяет функциональность
Route::get('/api/dashboard/metrics', [DashboardController::class, 'metrics']);
```
- Не удаляет существующие endpoints
- Использует существующие таблицы БД
- Backward compatible

#### ❌ НЕбезопасно: Изменение формата телеметрии
```typescript
// НЕЛЬЗЯ: Изменять формат данных телеметрии
interface Telemetry {
  value: number;
  timestamp: number; // ❌ Было 'ts', стало 'timestamp'
}
```
- Ломает совместимость с существующими компонентами
- Требует изменений в Python-сервисе
- Нарушает протокол

#### ❌ НЕбезопасно: Удаление существующего prop
```vue
<!-- НЕЛЬЗЯ: Удалять существующие props -->
<script setup>
// ❌ Было: const props = defineProps(['zone', 'telemetry'])
const props = defineProps(['zone']) // telemetry удален
</script>
```
- Ломает существующие компоненты
- Требует изменений в Laravel контроллерах
- Нарушает Inertia контракт

### План проверки совместимости для каждого этапа

#### Этап 1: Базовый промышленный стиль
- ✅ Использует существующие `dashboard` props из Inertia
- ✅ Использует существующие `/api/telemetry/aggregates` endpoint
- ✅ Новые компоненты не изменяют существующие
- ✅ Новые endpoints расширяют функциональность (backward compatible)

#### Этап 2: Расширенная функциональность
- ✅ Использует существующие `recipe.phases` props
- ✅ Использует существующие `/api/recipes/{id}/analytics` endpoint
- ✅ Новые endpoints для timeline используют существующие таблицы БД
- ✅ Drag-and-drop виджетов сохраняется в новой таблице (не изменяет существующие)

#### Этап 3: Продвинутые функции
- ✅ Экспорт данных использует существующие endpoints
- ✅ Энергопотребление требует новых метрик в телеметрии (если применимо)
- ✅ Все изменения backward compatible

### Интеграция с существующими системами

#### WebSocket (Laravel Reverb)
- ✅ Использует существующие каналы (`events.global`, `commands.{zoneId}`)
- ✅ Не изменяет формат WebSocket сообщений
- ✅ Новые события могут быть добавлены (не удаляя старые)

#### State Management (Pinia)
- ✅ Расширяет существующие stores (не удаляет)
- ✅ Новые stores для новых функций
- ✅ Существующие stores остаются совместимыми

#### Тестирование
- ✅ Существующие тесты должны продолжать работать
- ✅ Новые тесты для новых компонентов
- ✅ Регрессионное тестирование после каждого этапа

---

## Улучшения на основе лучших практик

Этот раздел содержит дополнительные улучшения, предложенные на основе анализа лучших практик промышленных дашбордов, IoT платформ и современных UI/UX паттернов.

### UI/UX улучшения

#### 1. Real-time обновления и оптимистичные UI
- [ ] **Оптимистичные обновления:** Показывать изменения UI сразу при отправке команды, не дожидаясь ответа от сервера
- [ ] **Skeleton loaders:** Использовать skeleton screens вместо спиннеров для лучшего UX
- [ ] **Progressive loading:** Загружать критичные данные первыми, затем второстепенные
- [ ] **Debouncing для фильтров:** Оптимизировать поиск и фильтрацию с задержкой

**Пример реализации:**
```vue
<!-- Оптимистичное обновление статуса зоны -->
<button @click="handlePause" :disabled="isPending">
  {{ optimisticStatus === 'PAUSED' ? 'Возобновить' : 'Приостановить' }}
</button>
```

#### 2. Accessibility (A11y)
- [ ] **ARIA labels:** Добавить aria-labels для всех интерактивных элементов
- [ ] **Keyboard navigation:** Полная поддержка навигации с клавиатуры
- [ ] **Screen reader support:** Семантическая разметка для screen readers
- [ ] **Color contrast:** Проверить контрастность цветов (WCAG AA минимум)
- [ ] **Focus indicators:** Видимые индикаторы фокуса для всех элементов

**Компоненты для улучшения:**
- Все интерактивные компоненты должны иметь `aria-label` или `aria-labelledby`
- Модальные окна должны ловить фокус и возвращать его при закрытии
- Формы должны иметь связанные `label` элементы

#### 3. Производительность и оптимизация
- [ ] **Virtual scrolling:** Для длинных списков зон/устройств (использовать `vue-virtual-scroller`)
- [ ] **Lazy loading компонентов:** Динамический импорт для тяжелых компонентов (графики, редакторы)
- [ ] **Code splitting:** Разделение кода по маршрутам для уменьшения initial bundle
- [ ] **Image optimization:** Оптимизация изображений культур/растений (WebP, lazy loading)
- [ ] **Memoization:** Использование `computed` и `memo` для дорогих вычислений

**Пример:**
```typescript
// Lazy loading тяжелых компонентов
const MultiSeriesChart = defineAsyncComponent(() => 
  import('@/Components/MultiSeriesTelemetryChart.vue')
)
```

#### 4. Расширенные паттерны визуализации
- [ ] **Heatmaps:** Тепловые карты для сравнения зон по метрикам
- [ ] **Gauge charts:** Круговые индикаторы для метрик с целевыми значениями
- [ ] **Sparklines:** Мини-графики трендов в таблицах и карточках
- [ ] **Anomaly visualization:** Визуальное выделение аномалий на графиках (маркеры, зоны)
- [ ] **Comparison views:** Side-by-side сравнение зон/рецептов

**Библиотеки для рассмотрения:**
- ECharts (уже используется) — для графиков и heatmaps
- Vue Gauge — для круговых индикаторов
- Vue Sparklines — для мини-графиков

#### 5. Улучшенная навигация и поиск
- [ ] **Command palette (Ctrl+K):** Быстрый поиск и переход к зонам/устройствам/рецептам
- [ ] **Breadcrumbs:** Навигационные крошки для глубоких страниц
- [ ] **Quick actions:** Контекстные быстрые действия (hover меню, right-click меню)
- [ ] **Recent items:** История недавно просмотренных зон/рецептов
- [ ] **Favorites:** Возможность добавить зоны/рецепты в избранное

**Пример Command Palette:**
```vue
<CommandPalette
  :items="searchableItems"
  :shortcut="['ctrl', 'k']"
  @select="handleNavigate"
/>
```

#### 6. Улучшенная обработка ошибок
- [ ] **Error boundaries:** Компоненты для обработки ошибок рендеринга
- [ ] **Retry mechanisms:** Автоматические повторы для failed запросов
- [ ] **Error notifications:** Понятные сообщения об ошибках с действиями
- [ ] **Offline support:** Индикация офлайн режима и очередь действий
- [ ] **Error logging:** Логирование ошибок на клиенте для анализа

#### 7. Персонализация и настройки
- [ ] **User preferences:** Сохранение пользовательских настроек (тема, единицы измерения, язык)
- [ ] **Dashboard customization:** Настраиваемые дашборды с сохранением конфигурации
- [ ] **Column customization:** Настройка отображаемых колонок в таблицах
- [ ] **Default views:** Сохранение предпочтительных фильтров и сортировок

### Технические улучшения

#### 1. Тестирование
- [ ] **Увеличить покрытие тестами:** Цель — 80%+ покрытие для критичных компонентов
- [ ] **Visual regression testing:** Использовать Percy или Chromatic для визуальных регрессий
- [ ] **E2E тесты для критичных потоков:** Полные сценарии для основных действий пользователя
- [ ] **Performance testing:** Тесты производительности для тяжелых компонентов (графики, таблицы)
- [ ] **Accessibility testing:** Автоматические тесты доступности (axe-core, jest-axe)

**План тестирования:**
- Unit тесты для всех новых компонентов
- Integration тесты для взаимодействия компонентов
- E2E тесты для критичных пользовательских потоков
- Visual regression тесты для UI компонентов

#### 2. Мониторинг и аналитика
- [ ] **Frontend monitoring:** Интеграция Sentry или аналогичного для отслеживания ошибок
- [ ] **Performance monitoring:** Отслеживание метрик производительности (FCP, LCP, CLS)
- [ ] **User analytics:** Аналитика использования функций (Google Analytics или аналог)
- [ ] **Error tracking:** Детальное логирование ошибок с контекстом
- [ ] **Performance budgets:** Ограничения на размер bundle и время загрузки

**Метрики для отслеживания:**
- Time to First Byte (TTFB)
- First Contentful Paint (FCP)
- Largest Contentful Paint (LCP)
- Cumulative Layout Shift (CLS)
- Time to Interactive (TTI)

#### 3. Документация компонентов
- [ ] **Storybook:** Создать Storybook для компонентной библиотеки
- [ ] **Component docs:** Документация для каждого компонента (props, events, examples)
- [ ] **Design tokens:** Документация дизайн-системы (цвета, spacing, типографика)
- [ ] **Usage examples:** Примеры использования компонентов в разных контекстах

#### 4. Оптимизация bundle size
- [ ] **Tree shaking:** Убедиться, что неиспользуемый код удаляется
- [ ] **Dynamic imports:** Ленивая загрузка для тяжелых библиотек
- [ ] **Bundle analysis:** Регулярный анализ размера bundle (webpack-bundle-analyzer)
- [ ] **CDN для библиотек:** Рассмотреть использование CDN для больших библиотек (ECharts)

### Архитектурные улучшения

#### 1. State Management
- [ ] **Оптимизация Pinia stores:** Убедиться в эффективном использовании stores
- [ ] **Normalization данных:** Нормализация данных в stores для избежания дублирования
- [ ] **Selectors:** Использование селекторов для вычисляемых данных
- [ ] **Cache invalidation:** Стратегия инвалидации кеша при обновлениях

#### 2. API клиент
- [ ] **Request deduplication:** Предотвращение дублирующихся запросов
- [ ] **Request cancellation:** Отмена запросов при размонтировании компонентов
- [ ] **Retry logic:** Умная логика повторов для failed запросов
- [ ] **Request caching:** Кеширование запросов с TTL

**Пример:**
```typescript
// Request deduplication
const pendingRequests = new Map()
const deduplicatedGet = async (url: string) => {
  if (pendingRequests.has(url)) {
    return pendingRequests.get(url)
  }
  const promise = api.get(url)
  pendingRequests.set(url, promise)
  try {
    return await promise
  } finally {
    pendingRequests.delete(url)
  }
}
```

#### 3. WebSocket оптимизация
- [ ] **Connection pooling:** Переиспользование WebSocket соединений
- [ ] **Message batching:** Батчинг обновлений для уменьшения количества сообщений
- [ ] **Selective subscriptions:** Подписка только на нужные каналы
- [ ] **Reconnection strategy:** Умная стратегия переподключения с экспоненциальной задержкой

---

## Вопросы для уточнения

Перед началом реализации необходимо уточнить следующие вопросы:

### Приоритеты и ограничения

1. **Какие функции являются критичными для первого релиза?**
   - Какие компоненты должны быть реализованы в первую очередь?
   - Есть ли жесткие дедлайны для определенных функций?

2. **Какие ограничения по ресурсам?**
   - Сколько разработчиков будет работать над рефакторингом?
   - Есть ли ограничения по времени?
   - Каков бюджет на внешние библиотеки/инструменты?

3. **Какие существующие проблемы нужно решить в первую очередь?**
   - Есть ли известные проблемы с производительностью?
   - Какие части UI вызывают больше всего жалоб от пользователей?
   - Есть ли проблемы с доступностью?

### Технические вопросы

4. **Какие библиотеки можно использовать?**
   - Есть ли ограничения на использование внешних библиотек?
   - Предпочтительны ли lightweight решения?
   - Нужна ли поддержка старых браузеров?

5. **Как организована работа с дизайном?**
   - Есть ли дизайнер, который будет создавать макеты?
   - Нужно ли создавать дизайн-систему с нуля?
   - Есть ли готовые дизайн-макеты для новых компонентов?

6. **Какие метрики успеха важны?**
   - Какие KPI нужно улучшить (время загрузки, конверсия, удовлетворенность)?
   - Есть ли бенчмарки для сравнения?
   - Как будет измеряться успех рефакторинга?

### Функциональные вопросы

7. **Какие функции из референсов являются приоритетными?**
   - Какие паттерны из UI референсов наиболее важны?
   - Есть ли функции, которые не нужны в текущей версии?

8. **Как обрабатывать мобильную версию?**
   - Нужна ли полноценная мобильная версия или достаточно адаптивного дизайна?
   - Какие устройства являются приоритетными (iOS, Android, планшеты)?
   - Нужны ли нативные функции (push notifications, offline mode)?

9. **Какие роли пользователей нужно учитывать?**
   - Какие роли существуют в системе (Agronomist, Engineer, Operator)?
   - Нужны ли разные интерфейсы для разных ролей?
   - Какие функции доступны каждой роли?

### Интеграция и данные

10. **Как обрабатывать исторические данные?**
    - Сколько данных нужно отображать на графиках?
    - Нужна ли поддержка экспорта больших объемов данных?
    - Как обрабатывать данные за длительные периоды (месяцы, годы)?

11. **Какие метрики энергопотребления доступны?**
    - Есть ли данные об энергопотреблении устройств?
    - Нужна ли визуализация энергопотребления?
    - Как часто обновляются эти данные?

12. **Как обрабатывать AI прогнозы и рекомендации?**
    - Насколько точны текущие AI прогнозы?
    - Нужно ли показывать confidence intervals?
    - Как часто обновляются прогнозы?

### Тестирование и качество

13. **Какое покрытие тестами является целевым?**
    - Какой процент покрытия считается достаточным?
    - Какие компоненты требуют наибольшего покрытия?
    - Нужны ли E2E тесты для всех критичных потоков?

14. **Как организован процесс развертывания?**
    - Есть ли staging окружение для тестирования?
    - Как часто происходят релизы?
    - Нужна ли поддержка feature flags?

### Пользовательский опыт

15. **Какие пользовательские сценарии являются критичными?**
    - Какие действия пользователи выполняют чаще всего?
    - Какие сценарии вызывают больше всего проблем?
    - Есть ли данные об использовании (analytics)?

16. **Нужна ли поддержка нескольких языков?**
    - Какие языки нужно поддерживать?
    - Есть ли готовые переводы?
    - Как организована локализация?

---

## Детальный план по референсам

### 📊 ref_01_industrial_dribbble.jpg — Промышленные дашборды

**Применение:** Общий стиль промышленных интерфейсов, организация данных, цветовая схема статусов

#### Задачи:

**1.1. Dashboard — промышленный стиль**
- [ ] Увеличить информационную плотность карточек
  - **Данные:** Использовать существующие `dashboard` props из Inertia
- [ ] Добавить крупные цифровые индикаторы для ключевых метрик
  - **Данные:** Использовать `dashboard.zonesByStatus`, `dashboard.nodesByStatus`
  - **API:** Использовать `/api/telemetry/aggregates` для метрик зон
  - **Backend:** Создать `/api/dashboard/metrics` для агрегированных метрик всех зон
- [ ] Визуальные индикаторы статусов оборудования (светодиоды, индикаторы)
  - **Данные:** Использовать `dashboard.zonesByStatus`, `dashboard.nodesByStatus`
- [ ] Группировка данных по категориям (теплицы, зоны, устройства)
  - **Данные:** Использовать `dashboard.greenhouses`, `dashboard.problematicZones`
- [ ] Цветовая кодировка статусов (зеленый/желтый/красный/серый)
  - **Реализация:** CSS переменные для статусов

**Компоненты для изменения:**
- `Pages/Dashboard/Index.vue`
- `Components/MetricCard.vue` (создать/улучшить)
- `Components/StatusIndicator.vue` (создать новый)
- `Components/StatCard.vue` (улучшить)

**API интеграция:**
```typescript
// Использование существующих данных + новый endpoint для метрик
const dashboardMetrics = computed(() => props.dashboard)
const telemetryTrends = await fetch('/api/dashboard/trends', {
  params: { period: '24h' }
})
```

**Пример реализации:**
```vue
<!-- Крупный индикатор метрики -->
<MetricIndicator
  :value="phValue"
  :target="phTarget"
  :status="phStatus"
  size="large"
  show-trend
  show-gauge
/>
```

**1.2. Организация данных**
- [ ] Таблицы с группировкой по статусам
- [ ] Фильтры и сортировка в стиле SCADA
- [ ] Быстрые действия в контекстных меню
- [ ] Экспорт данных в различных форматах

**1.3. Цветовая схема статусов**
- [ ] Единая система цветов для всех статусов
- [ ] Индикаторы в стиле светодиодов
- [ ] Анимация пульсации для критичных статусов

---

### 🌱 ref_02_greenhouse_behance.png — Интерфейсы управления теплицами

**Применение:** Специфичные паттерны для агросистем, отображение метрик зон, визуализация данных о растениях

#### Задачи:

**2.1. Zone Detail — агрономический стиль**
- [ ] Визуализация прогресса фазы роста (круговой индикатор)
  - **Данные:** Использовать `current_phase` из Inertia props (уже доступно)
  - **Backend:** Создать `/api/zones/{id}/phase-progress` для детального прогресса
- [ ] Отображение дней до следующей фазы
  - **Данные:** Вычислять из `current_phase.phase_ends_at` (уже доступно)
- [ ] Визуализация целевых значений vs текущие (графики сравнения)
  - **Данные:** Использовать `targets` и `telemetry` из Inertia props
  - **API:** Использовать `/api/telemetry/aggregates` для исторических данных
- [ ] Индикаторы здоровья растений (визуальные метрики)
  - **API:** Использовать `/api/ai/explain_zone` для AI-объяснений
  - **Данные:** Сравнивать `telemetry` с `targets`
- [ ] История фаз роста (timeline)
  - **Backend:** Создать `/api/zones/{id}/growth-history` для истории фаз
  - **Альтернатива:** Использовать `events` из Inertia props (фильтровать по типу CYCLE_*)

**Компоненты для изменения:**
- `Pages/Zones/Show.vue`
- `Components/ZoneTargets.vue` (переработать)
- `Components/PhaseProgress.vue` (улучшить)
- `Components/GrowthTimeline.vue` (создать новый)

**API интеграция:**
```typescript
// Использование существующих данных из Inertia props
const phaseProgress = computed(() => {
  const phase = props.current_phase
  if (!phase) return null
  
  const now = new Date()
  const started = new Date(phase.phase_started_at)
  const ends = new Date(phase.phase_ends_at)
  const elapsed = (now.getTime() - started.getTime()) / (1000 * 60 * 60) // часы
  const total = phase.duration_hours
  const progress = Math.min(100, (elapsed / total) * 100)
  const daysLeft = (ends.getTime() - now.getTime()) / (1000 * 60 * 60 * 24)
  
  return { progress, daysLeft, elapsed, total }
})
```

**Пример реализации:**
```vue
<!-- Визуализация прогресса фазы -->
<PhaseProgressRing
  :current-day="phaseDaysElapsed"
  :total-days="phaseDaysTotal"
  :phase-name="currentPhaseName"
  :next-phase-name="nextPhaseName"
  size="large"
/>
```

**2.2. Zone Card — улучшенная визуализация**
- [ ] Мини-графики трендов (sparklines) для каждой метрики
- [ ] Визуальные индикаторы отклонений от цели
- [ ] Прогресс текущей фазы на карточке
- [ ] Быстрые действия (hover menu)

**2.3. Recipe Visualization**
- [ ] Визуализация всех фаз рецепта на одном экране
- [ ] Сравнение фаз (графики целевых значений)
- [ ] Интерактивное редактирование фаз (drag-and-drop)

---

### 🚜 ref_03_digital_farming_prolim.png — Цифровое фермерство

**Применение:** Современные подходы к отображению агроданных, интеграция IoT метрик, паттерны для мобильных и десктопных интерфейсов

#### Задачи:

**3.1. Современные паттерны отображения данных**
- [ ] Карточки с визуализацией трендов
- [ ] Интеграция IoT метрик в единый интерфейс
- [ ] Адаптивные компоненты для разных размеров экранов
- [ ] Современная типографика и spacing

**3.2. Интеграция IoT метрик**
- [ ] Единый формат отображения всех метрик
- [ ] Группировка метрик по типам (pH/EC, климат, полив)
- [ ] Визуальные связи между метриками

---

### 📡 ref_04_iot_thingsboard.png — IoT платформы и дашборды

**Применение:** Организация телеметрии устройств, графики временных рядов, управление устройствами

#### Задачи:

**4.1. Графики телеметрии — IoT стиль**
- [ ] Мульти-серии графики (pH + EC на одном графике)
  - **API:** Использовать `/api/telemetry/aggregates` для каждой метрики отдельно (пока нет multi-series endpoint)
  - **Backend:** Создать `/api/zones/{id}/telemetry/multi-series` для оптимизации
- [ ] **Выбор периода для исторических данных:**
  - **По умолчанию:** 1 день
  - **Доступные периоды:** Сутки (24h), 7 дней, Месяц (30d)
  - **UI:** Выпадающий список или кнопки переключения периодов
  - **API:** Использовать параметр `period` в `/api/telemetry/aggregates` (1h, 24h, 7d, 30d)
- [ ] Zoom и pan для детального просмотра
  - **Реализация:** Использовать встроенные возможности ECharts (dataZoom, brush)
- [ ] Выделение аномалий на графиках
  - **Backend:** Создать `/api/zones/{id}/telemetry/anomalies` для обнаружения аномалий
  - **Альтернатива:** Использовать AI endpoint `/api/ai/diagnostics` для выявления проблем
- [ ] Легенда с интерактивным включением/выключением серий
  - **Реализация:** Встроенная функциональность ECharts
- [ ] Экспорт графиков в PNG/PDF
  - **Реализация:** Использовать `echarts.getInstanceByDom().getDataURL()` для PNG
  - **Backend:** Создать `/api/zones/{id}/telemetry/export` для CSV/JSON экспорта данных
- [ ] **Интеграция AI прогнозов на графиках:**
  - Отображение линии прогноза на графиках
  - Confidence interval (затененная область)
  - Переключатель включения/выключения прогнозов

**Компоненты для изменения:**
- `Components/MultiSeriesTelemetryChart.vue` (улучшить)
- `Components/ZoneTelemetryChart.vue` (переработать)
- `Components/ChartToolbar.vue` (создать новый)

**API интеграция:**
```typescript
// Использование существующего aggregates endpoint
const fetchMultiSeries = async (zoneId: number, metrics: string[], period: string) => {
  const promises = metrics.map(metric => 
    api.get('/telemetry/aggregates', {
      params: { zone_id: zoneId, metric, period }
    })
  )
  const results = await Promise.all(promises)
  return results.map((r, i) => ({
    name: metrics[i],
    data: r.data.data.map(item => ({ ts: item.ts, value: item.avg }))
  }))
}
```

**Пример реализации:**
```vue
<MultiSeriesChart
  :series="[
    { name: 'pH', data: phData, color: '#3b82f6', yAxis: 0 },
    { name: 'EC', data: ecData, color: '#10b981', yAxis: 1 }
  ]"
  :anomalies="detectedAnomalies"
  :time-range="timeRange"
  :zoom-enabled="true"
  :pan-enabled="true"
  @export="handleExport"
/>
```

**4.2. Организация телеметрии устройств**
- [ ] Таблица устройств с телеметрией в реальном времени
  - **Данные:** Использовать `devices` из Inertia props
  - **API:** Использовать `/api/nodes/{id}/telemetry/last` для телеметрии устройств
  - **Real-time:** WebSocket обновления через `events.global` канал
- [ ] Группировка устройств по типам (PH, EC, Climate, Irrigation)
  - **Данные:** Использовать `device.type` из Inertia props
- [ ] Фильтрация и поиск устройств
  - **Реализация:** Клиентская фильтрация существующих данных
- [ ] Быстрый доступ к настройкам устройства
  - **API:** Использовать `/api/nodes/{id}/config` для получения конфигурации

**4.3. Управление устройствами**
- [ ] Визуализация статусов устройств (ONLINE/OFFLINE)
  - **Данные:** Использовать `device.status` из Inertia props
  - **Real-time:** WebSocket обновления через `events.global` канал
- [ ] Индикаторы RSSI и качества связи
  - **Данные:** Проверить наличие `device.rssi` в модели DeviceNode
  - **Backend:** Добавить RSSI в модель, если отсутствует
- [ ] Быстрые действия (restart, test channels)
  - **API:** Использовать `/api/nodes/{id}/commands` для отправки команд
- [ ] История команд устройства
  - **API:** Использовать `/api/nodes/{id}/telemetry/history` для истории
  - **Backend:** Создать `/api/nodes/{id}/commands/history` для истории команд

---

### 🚜 ref_05_farm_behance.png — Фермерские и агрономические интерфейсы

**Применение:** UX паттерны для агрономов, отображение рецептов и фаз роста, управление зонами и культурами

#### Задачи:

**5.1. Recipe Management — агрономический стиль**
- [ ] Визуализация всех фаз рецепта на timeline
  - **Данные:** Использовать `recipe.phases` из Inertia props (уже доступно)
  - **Backend:** Создать `/api/recipes/{id}/phases-timeline` для оптимизированных данных
- [ ] Сравнение целевых значений между фазами
  - **Данные:** Использовать `recipe.phases[].targets` из Inertia props
  - **API:** Использовать `/api/recipes/comparison` для сравнения рецептов
- [ ] Визуализация кривых роста
  - **API:** Использовать `/api/recipes/{id}/analytics` для аналитики рецепта
  - **Данные:** Использовать `RecipeAnalytics` модель
- [ ] Интерактивное редактирование фаз
  - **API:** Использовать существующие endpoints:
    - `POST /api/recipes/{id}/phases` — добавление фазы
    - `PATCH /api/recipe-phases/{id}` — обновление фазы
    - `DELETE /api/recipe-phases/{id}` — удаление фазы

**Компоненты для изменения:**
- `Pages/Recipes/Show.vue`
- `Pages/Recipes/Edit.vue`
- `Components/RecipeTimeline.vue` (создать новый)
- `Components/PhaseComparison.vue` (создать новый)

**API интеграция:**
```typescript
// Использование существующих endpoints
const updatePhase = async (phaseId: number, data: PhaseData) => {
  await api.patch(`/recipe-phases/${phaseId}`, data)
  // Обновить локальное состояние или перезагрузить рецепт
}
```

**5.2. Zone Management**
- [ ] Визуализация связей между зонами и культурами
- [ ] Группировка зон по культурам
- [ ] Быстрое сравнение зон
- [ ] История изменений зон

**5.3. Culture Management**
- [ ] Визуализация культур с фотографиями
- [ ] Информация о требованиях культур
- [ ] Связь культур с рецептами

---

### 🏭 ref_06_scada_dribbble.png — SCADA системы

**Применение:** Организация сложных дашбордов, отображение статусов оборудования, алерты в промышленном стиле

#### Задачи:

**6.1. Сложные дашборды**
- [ ] Grid layout для организации виджетов
  - **Реализация:** Использовать CSS Grid или библиотеку (например, gridstack.js)
- [ ] Drag-and-drop для перестановки виджетов
  - **Реализация:** Использовать библиотеку drag-and-drop (например, vue-draggable)
- [ ] Сохранение конфигурации дашборда
  - **Backend:** Создать `/api/dashboard/widgets` (GET/PUT) для сохранения конфигурации
  - **Альтернатива:** Использовать localStorage для временного хранения
- [ ] Разные layouts для разных ролей
  - **Данные:** Использовать `auth.user.role` из Inertia props
  - **Backend:** Расширить `/api/dashboard/widgets` для поддержки ролевых layouts

**Компоненты для создания:**
- `Components/DashboardGrid.vue` (создать новый)
- `Components/DashboardWidget.vue` (создать новый)
- `Components/DashboardLayout.vue` (создать новый)

**API интеграция:**
```typescript
// Сохранение конфигурации дашборда
const saveDashboardLayout = async (layout: DashboardLayout) => {
  await api.put('/dashboard/widgets', { layout, role: user.role })
}
```

**6.2. Статусы оборудования**
- [ ] Визуальные индикаторы в стиле SCADA (светодиоды)
- [ ] Таблицы статусов с группировкой
- [ ] История изменений статусов
- [ ] Алерты при изменении статусов

**6.3. Алерты в промышленном стиле**
- [ ] Крупные визуальные индикаторы критичных алертов
- [ ] Группировка алертов по приоритетам
- [ ] Звуковые уведомления (опционально)
- [ ] История алертов с фильтрацией

**Компоненты для изменения:**
- `Pages/Alerts/Index.vue`
- `Components/AlertCard.vue` (переработать)
- `Components/AlertIndicator.vue` (создать новый)

---

### ⚡ ref_07_energy_dribbble.png — Энергетические дашборды

**Применение:** Визуализация энергопотребления, мониторинг систем питания, графики производительности

#### Задачи:

**7.1. Энергопотребление (заглушки + TODO для будущей реализации)**
- [ ] Создать компоненты-заглушки для энергопотребления
  - `Components/EnergyConsumptionCard.vue` (заглушка)
  - `Components/EnergyConsumptionChart.vue` (заглушка)
  - Добавить TODO комментарии для будущей реализации
- [ ] Графики энергопотребления устройств (TODO)
- [ ] Суммарное энергопотребление по зонам (TODO)
- [ ] Прогноз энергопотребления (TODO)
- [ ] Оптимизация энергопотребления (TODO)

**7.2. Мониторинг систем питания (заглушки + TODO)**
- [ ] Статусы питания устройств (TODO)
- [ ] Индикаторы уровня заряда (TODO)
- [ ] История энергопотребления (TODO)

**7.3. Графики производительности (заглушки + TODO)**
- [ ] Производительность системы по времени (TODO)
- [ ] Сравнение производительности зон (TODO)
- [ ] Метрики эффективности (TODO)

**Пример заглушки:**
```vue
<!-- TODO: Реализовать отображение энергопотребления после добавления метрик в телеметрию -->
<EnergyConsumptionCard>
  <div class="text-center py-8 text-gray-500">
    <p>Отображение энергопотребления будет доступно после добавления метрик в телеметрию</p>
    <p class="text-sm mt-2">TODO: Интеграция с метриками энергопотребления</p>
  </div>
</EnergyConsumptionCard>
```

---

### 📱 ref_08_mobile_greenhouse_dribbble.png — Мобильные интерфейсы

**Применение:** Адаптивный дизайн для мобильных устройств, компактное отображение метрик, мобильная навигация

#### Задачи:

**8.1. Мобильная навигация**
- [ ] Bottom navigation для мобильных устройств
- [ ] Swipe gestures для навигации между экранами
- [ ] Pull-to-refresh
- [ ] Оптимизированное меню для мобильных

**Компоненты для создания:**
- `Components/MobileNavigation.vue` (улучшить существующий)
- `Components/BottomNav.vue` (создать новый)
- `Layouts/MobileLayout.vue` (создать новый)

**8.2. Адаптивное отображение метрик**
- [ ] Адаптивные карточки метрик для мобильных устройств
  - **Данные:** Использовать существующие endpoints
  - **Backend:** Создать `/api/zones/{id}/telemetry/summary` для компактной сводки
- [ ] Компактные графики для мобильных
  - **API:** Использовать `/api/telemetry/aggregates` с меньшим периодом (1h вместо 24h)
  - **Backend:** Создать `/api/dashboard/mobile` для оптимизированных данных
- [ ] Оптимизация размера шрифтов
  - **Реализация:** Адаптивная типографика через Tailwind responsive classes
- [ ] **Примечание:** Полноценное мобильное приложение будет разработано отдельно, здесь только адаптивный дизайн для веб-версии

**8.3. Touch-friendly интерфейс**
- [ ] Увеличенные области клика
- [ ] Swipe actions для карточек
- [ ] Оптимизация форм для мобильных
- [ ] Виртуальная клавиатура для числовых полей

**8.4. Оптимизация производительности**
- [ ] Lazy loading компонентов для мобильных
- [ ] Виртуализация списков
- [ ] Оптимизация графиков для мобильных
- [ ] Кеширование данных

---

## Критичные задачи (исправление проблем)

### 🔴 Критичные исправления — Приоритет 0 (1-2 недели)

**Цель:** Исправить критические проблемы перед началом рефакторинга

#### 1. Исправление разметки и 500 ошибок

**Задачи:**
- [ ] **Аудит разметки:** Проверить все страницы на некорректную разметку
  - Проверить валидность HTML
  - Исправить незакрытые теги
  - Исправить неправильную вложенность элементов
  - Проверить семантическую разметку
  
- [ ] **Исправление 500 ошибок:**
  - Найти и исправить все страницы с 500 ошибками
  - Добавить правильную обработку ошибок в компонентах
  - Добавить error boundaries для критичных компонентов
  - Улучшить логирование ошибок для отладки
  
- [ ] **Тестирование:**
  - Протестировать все страницы после исправлений
  - Убедиться, что нет 500 ошибок
  - Проверить корректность отображения на всех страницах

**Компоненты для проверки:**
- `Pages/Dashboard/Index.vue`
- `Pages/Zones/Show.vue`
- `Pages/Zones/Index.vue`
- `Pages/Devices/Index.vue`
- `Pages/Recipes/Show.vue`
- `Pages/Alerts/Index.vue`
- Все остальные страницы

#### 2. Добавление отображения AI прогнозов

**Текущая ситуация:** AI прогнозы генерируются на бэкенде (`/api/ai/predict`), но не отображаются на фронтенде

**Задачи:**
- [ ] **Компонент для отображения прогнозов:**
  - Создать компонент `AIPredictionCard.vue` для отображения прогнозов
  - Показывать прогнозируемое значение, confidence, горизонт прогноза
  - Визуализировать прогнозы на графиках телеметрии
  
- [ ] **Интеграция в Zone Detail:**
  - Добавить секцию "AI Прогнозы" на страницу зоны
  - Показывать прогнозы для основных метрик (pH, EC, temp_air, humidity_air)
  - Обновлять прогнозы автоматически (polling или WebSocket)
  
- [ ] **Визуализация на графиках:**
  - Добавить линию прогноза на графики телеметрии
  - Показывать confidence interval (затененная область)
  - Позволить включать/выключать отображение прогнозов
  
- [ ] **Использование существующих API:**
  - `POST /api/ai/predict` — получение прогноза для метрики
  - `POST /api/ai/explain_zone` — объяснение состояния зоны (уже используется, но можно улучшить)
  - `POST /api/ai/recommend` — рекомендации (можно добавить отображение)

**Компоненты для создания/изменения:**
- `Components/AIPredictionCard.vue` (создать новый)
- `Components/AIPredictionChart.vue` (создать новый)
- `Components/ZoneTelemetryChart.vue` (добавить поддержку прогнозов)
- `Pages/Zones/Show.vue` (добавить секцию прогнозов)

**Пример реализации:**
```vue
<!-- Компонент для отображения прогноза -->
<AIPredictionCard
  :zone-id="zone.id"
  :metric-type="'ph'"
  :horizon-minutes="60"
  :auto-refresh="true"
  :refresh-interval="300000"
/>
```

#### 3. Улучшение мастера добавления нового цикла выращивания

**Текущая ситуация:** Существует `GrowthCycleModal.vue`, но мастер требует значительного улучшения

**Задачи:**
- [ ] **Анализ текущего мастера:**
  - Изучить `Components/GrowthCycleModal.vue`
  - Изучить `Pages/Setup/Wizard.vue` (мастер настройки системы)
  - Определить проблемы и области для улучшения
  
- [ ] **Улучшенный мастер (многошаговый wizard):**
  - **Шаг 1:** Выбор зоны (если не выбрана)
  - **Шаг 2:** Выбор рецепта или создание нового
  - **Шаг 3:** Настройка параметров цикла (дата начала, ожидаемая дата окончания)
  - **Шаг 4:** Предпросмотр и подтверждение
  - **Шаг 5:** Успешное создание и переход к зоне
  
- [ ] **Улучшения UX:**
  - Прогресс-бар с шагами
  - Валидация на каждом шаге
  - Возможность вернуться к предыдущему шагу
  - Сохранение черновика (localStorage)
  - Подсказки и помощь на каждом шаге
  - Визуализация выбранного рецепта с фазами
  
- [ ] **Интеграция с существующими компонентами:**
  - Использовать `RecipeCreateWizard.vue` для создания рецепта на лету
  - Использовать существующие компоненты выбора зоны/рецепта
  - Интеграция с API создания циклов
  
- [ ] **Визуализация:**
  - Timeline выбранного рецепта
  - Предпросмотр фаз и их длительности
  - Визуализация целевых значений

**Компоненты для создания/изменения:**
- `Components/GrowthCycleWizard.vue` (переработать/создать новый)
- `Components/GrowthCycleStepZone.vue` (создать новый)
- `Components/GrowthCycleStepRecipe.vue` (создать новый)
- `Components/GrowthCycleStepParams.vue` (создать новый)
- `Components/GrowthCyclePreview.vue` (создать новый)

**API интеграция:**
```typescript
// Создание нового цикла выращивания
const createGrowCycle = async (zoneId: number, recipeId: number, params: {
  started_at: string,
  expected_harvest_at?: string
}) => {
  await api.post(`/zones/${zoneId}/grow-cycles`, {
    recipe_id: recipeId,
    ...params
  })
}
```

#### 4. Локализация интерфейса (только русский)

**Задачи:**
- [ ] **Проверка всех текстов:**
  - Убедиться, что все тексты на русском языке
  - Проверить все компоненты, страницы, сообщения об ошибках
  - Исправить английские тексты
  
- [ ] **Компоненты и сообщения:**
  - Все labels, placeholders, кнопки на русском
  - Все toast сообщения на русском
  - Все ошибки и валидационные сообщения на русском
  
- [ ] **Документация:**
  - Комментарии в коде могут быть на английском (для разработчиков)
  - Пользовательский интерфейс — только русский

**Файлы для проверки:**
- Все `.vue` файлы в `resources/js/Pages/`
- Все `.vue` файлы в `resources/js/Components/`
- Все composables с пользовательскими сообщениями

---

## Приоритизация задач

### 🔴 Критичные (Этап 0) — 1-2 недели

**Цель:** Исправить критические проблемы перед началом рефакторинга

1. **Исправление разметки и 500 ошибок**
   - Аудит и исправление разметки
   - Исправление всех 500 ошибок
   - Тестирование всех страниц

2. **Добавление отображения AI прогнозов**
   - Компоненты для отображения прогнозов
   - Интеграция в Zone Detail
   - Визуализация на графиках

3. **Улучшение мастера добавления цикла выращивания**
   - Многошаговый wizard
   - Улучшения UX
   - Визуализация рецепта

4. **Локализация (только русский)**
   - Проверка всех текстов
   - Исправление английских текстов

### 🟠 Высокий приоритет (Этап 1) — 2-3 недели

**Цель:** Базовый промышленный стиль и улучшенная визуализация данных

1. **Dashboard — промышленный стиль**
   - Крупные индикаторы метрик
   - Визуальные индикаторы статусов
   - Улучшенная организация данных
   - Цветовая схема статусов

2. **Графики телеметрии — IoT стиль**
   - Мульти-серии графики
   - Zoom и pan
   - Выделение аномалий
   - Улучшенная легенда

3. **Zone Detail — агрономический стиль**
   - Визуализация прогресса фазы
   - Улучшенные метрики зоны
   - Визуализация целевых значений

4. **Мобильная версия — базовая**
   - Bottom navigation
   - Адаптивные карточки
   - Touch-friendly интерфейс

### 🟡 Важные (Этап 2) — 3-4 недели

**Цель:** Расширенная функциональность и специализированные паттерны

5. **Recipe Management — агрономический стиль**
   - Визуализация фаз на timeline
   - Сравнение фаз
   - Интерактивное редактирование

6. **SCADA стиль для дашбордов**
   - Grid layout с drag-and-drop
   - Визуальные индикаторы статусов
   - Улучшенные алерты

7. **Device Management — IoT стиль**
   - Таблица устройств с телеметрией
   - Группировка и фильтрация
   - Быстрые действия

8. **Мобильная версия — расширенная**
   - Swipe gestures
   - Pull-to-refresh
   - Оптимизация графиков

### 🟢 Желательные (Этап 3) — 2-3 недели

**Цель:** Продвинутые функции и оптимизация

9. **Энергопотребление (если применимо)**
   - Графики энергопотребления
   - Мониторинг систем питания

10. **Персонализация дашбордов**
    - Сохранение конфигурации
    - Разные layouts для ролей

11. **Расширенная аналитика**
    - Сравнение зон
    - Экспорт данных
    - Прогнозы

---

## Этапы реализации

### Этап 0: Критичные исправления (1-2 недели)

**Неделя 1:**
- [ ] Аудит и исправление разметки на всех страницах
- [ ] Исправление всех 500 ошибок
- [ ] Добавление error boundaries
- [ ] Тестирование исправлений
- [ ] Проверка локализации (только русский)

**Неделя 2:**
- [ ] Создать компоненты для отображения AI прогнозов
- [ ] Интегрировать прогнозы в Zone Detail
- [ ] Добавить визуализацию прогнозов на графиках
- [ ] Переработать мастер добавления цикла выращивания
- [ ] Улучшить UX мастера (wizard, валидация, визуализация)

### Этап 1: Базовый промышленный стиль (2-3 недели)

**Приоритет:** Дашборды первые, затем поэтапно весь фронт

**Неделя 3:**
- [ ] Создать компонент `StatusIndicator.vue` (светодиоды)
- [ ] Улучшить `MetricCard.vue` с крупными индикаторами
- [ ] Переработать Dashboard с промышленным стилем
- [ ] Добавить цветовую схему статусов
- [ ] Увеличить информационную плотность Dashboard

**Неделя 4:**
- [ ] Улучшить графики телеметрии (мульти-серии, zoom, pan)
- [ ] Создать компонент `ChartToolbar.vue`
- [ ] Добавить выделение аномалий на графиках
- [ ] Улучшить легенду графиков
- [ ] Добавить выбор периодов для графиков (сутки, 7 дней, месяц, по умолчанию 1 день)

**Неделя 5:**
- [ ] Переработать Zone Detail с агрономическим стилем
- [ ] Создать компонент `PhaseProgressRing.vue`
- [ ] Улучшить `ZoneTargets.vue`
- [ ] Базовая адаптивная навигация (мобильное приложение будет отдельно)

### Этап 2: Расширенная функциональность (3-4 недели)

**Неделя 4:**
- [ ] Recipe Management с timeline
- [ ] Создать компонент `RecipeTimeline.vue`
- [ ] Сравнение фаз рецепта
- [ ] Интерактивное редактирование фаз

**Неделя 5:**
- [ ] SCADA стиль для дашбордов
- [ ] Создать компонент `DashboardGrid.vue`
- [ ] Drag-and-drop для виджетов
- [ ] Визуальные индикаторы статусов

**Неделя 6:**
- [ ] Device Management в IoT стиле
- [ ] Таблица устройств с телеметрией
- [ ] Группировка и фильтрация устройств
- [ ] Быстрые действия для устройств

**Неделя 7:**
- [ ] Расширенная мобильная версия
- [ ] Swipe gestures
- [ ] Pull-to-refresh
- [ ] Оптимизация графиков для мобильных

### Этап 3: Продвинутые функции (2-3 недели)

**Неделя 8:**
- [ ] Энергопотребление (если применимо)
- [ ] Мониторинг систем питания
- [ ] Графики производительности

**Неделя 9:**
- [ ] Персонализация дашбордов
- [ ] Сохранение конфигурации
- [ ] Разные layouts для ролей

**Неделя 10:**
- [ ] Расширенная аналитика
- [ ] Сравнение зон
- [ ] Экспорт данных
- [ ] Финальная оптимизация
- [ ] Тестирование и исправление багов
- [ ] Документация компонентов

---

## Тестирование и качество

### Стратегия тестирования

Рефакторинг должен сопровождаться комплексным тестированием для обеспечения качества и стабильности системы.

#### 1. Unit/Component тесты (Vitest)

**Цель покрытия:** 80%+ для критичных компонентов

**Приоритетные компоненты для тестирования:**
- [ ] `StatusIndicator.vue` — индикаторы статусов
- [ ] `MetricIndicator.vue` — крупные индикаторы метрик
- [ ] `PhaseProgressRing.vue` — прогресс фаз
- [ ] `MultiSeriesTelemetryChart.vue` — мульти-серии графики
- [ ] `RecipeTimeline.vue` — timeline рецептов
- [ ] `DashboardGrid.vue` — grid layout дашборда
- [ ] `CommandPalette.vue` — командная палитра

**Пример теста:**
```typescript
describe('StatusIndicator.vue', () => {
  it('показывает зеленый индикатор для статуса RUNNING', () => {
    const wrapper = mount(StatusIndicator, {
      props: { status: 'RUNNING' }
    })
    expect(wrapper.find('.status-indicator').classes()).toContain('status-running')
  })
})
```

#### 2. Integration тесты

**Тестируемые сценарии:**
- [ ] Взаимодействие компонентов на Dashboard
- [ ] Загрузка и отображение данных зоны
- [ ] Обновление данных через WebSocket
- [ ] Отправка команд и обновление UI
- [ ] Навигация между страницами

#### 3. E2E тесты (Playwright)

**Критичные пользовательские потоки:**
- [ ] Просмотр Dashboard и навигация
- [ ] Просмотр деталей зоны и телеметрии
- [ ] Управление зоной (pause/resume/next-phase)
- [ ] Просмотр и редактирование рецепта
- [ ] Управление устройствами
- [ ] Просмотр и фильтрация алертов
- [ ] Мобильная версия (основные потоки)

**Пример E2E теста:**
```typescript
test('пользователь может приостановить зону', async ({ page }) => {
  await page.goto('/zones/1')
  await page.click('button:has-text("Приостановить")')
  await expect(page.locator('.status-indicator')).toHaveClass(/status-paused/)
})
```

#### 4. Visual regression тесты

**Инструменты:** Percy, Chromatic или аналогичные

**Тестируемые компоненты:**
- [ ] Все новые компоненты UI
- [ ] Критичные страницы (Dashboard, Zone Show, Recipe Show)
- [ ] Разные состояния компонентов (loading, error, empty)
- [ ] Разные размеры экранов (desktop, tablet, mobile)

#### 5. Performance тесты

**Метрики для отслеживания:**
- [ ] Time to First Byte (TTFB) < 200ms
- [ ] First Contentful Paint (FCP) < 1.5s
- [ ] Largest Contentful Paint (LCP) < 2.5s
- [ ] Cumulative Layout Shift (CLS) < 0.1
- [ ] Time to Interactive (TTI) < 3.5s

**Тестируемые сценарии:**
- [ ] Загрузка Dashboard с большим количеством зон
- [ ] Отображение графиков с большим объемом данных
- [ ] Прокрутка длинных списков
- [ ] Переключение между вкладками

#### 6. Accessibility тесты

**Инструменты:** axe-core, jest-axe, Lighthouse

**Проверки:**
- [ ] ARIA labels для всех интерактивных элементов
- [ ] Keyboard navigation для всех функций
- [ ] Color contrast (WCAG AA минимум)
- [ ] Screen reader compatibility
- [ ] Focus management

### План тестирования по этапам

#### Этап 1: Базовый промышленный стиль
- [ ] Unit тесты для новых компонентов (`StatusIndicator`, `MetricIndicator`)
- [ ] Integration тесты для Dashboard
- [ ] E2E тесты для основных потоков Dashboard
- [ ] Performance тесты для загрузки Dashboard

#### Этап 2: Расширенная функциональность
- [ ] Unit тесты для новых компонентов (`RecipeTimeline`, `DashboardGrid`)
- [ ] Integration тесты для взаимодействия компонентов
- [ ] E2E тесты для управления рецептами и устройствами
- [ ] Visual regression тесты для новых UI компонентов

#### Этап 3: Продвинутые функции
- [ ] Unit тесты для всех новых компонентов
- [ ] E2E тесты для всех критичных потоков
- [ ] Performance тесты для оптимизации
- [ ] Accessibility аудит всего приложения

### Мониторинг и аналитика

#### 1. Error tracking

**Инструменты:** Sentry или аналогичные

**Отслеживаемые ошибки:**
- [ ] JavaScript ошибки в production
- [ ] Ошибки API запросов
- [ ] Ошибки WebSocket соединений
- [ ] Ошибки рендеринга компонентов

**Настройка:**
```typescript
// Инициализация Sentry
import * as Sentry from '@sentry/vue'
Sentry.init({
  app,
  dsn: process.env.VITE_SENTRY_DSN,
  environment: process.env.NODE_ENV,
  tracesSampleRate: 0.1,
})
```

#### 2. Performance monitoring

**Инструменты:** Web Vitals, Lighthouse CI

**Метрики:**
- [ ] Real User Monitoring (RUM) для production
- [ ] Synthetic monitoring для критичных страниц
- [ ] Bundle size tracking
- [ ] API response time tracking

#### 3. User analytics

**Инструменты:** Google Analytics, Plausible или аналогичные

**Отслеживаемые события:**
- [ ] Просмотр страниц
- [ ] Использование функций (команды, фильтры, экспорт)
- [ ] Ошибки пользователей
- [ ] Время на странице
- [ ] Конверсии (создание рецепта, запуск зоны)

### Качество кода

#### 1. Code review

**Требования:**
- [ ] Все изменения проходят code review
- [ ] Проверка соответствия стандартам кода
- [ ] Проверка тестового покрытия
- [ ] Проверка производительности

#### 2. Linting и форматирование

**Инструменты:**
- [ ] ESLint для JavaScript/TypeScript
- [ ] Prettier для форматирования
- [ ] Stylelint для CSS
- [ ] Vue ESLint plugin для Vue компонентов

**Настройка:**
```json
{
  "scripts": {
    "lint": "eslint resources/js --ext .vue,.js,.ts",
    "lint:fix": "eslint resources/js --ext .vue,.js,.ts --fix",
    "format": "prettier --write resources/js"
  }
}
```

#### 3. Pre-commit hooks

**Настройка Husky:**
```json
{
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged",
      "pre-push": "npm run test && npm run lint"
    }
  },
  "lint-staged": {
    "*.{vue,js,ts}": ["eslint --fix", "prettier --write"],
    "*.{css,scss}": ["stylelint --fix", "prettier --write"]
  }
}
```

### Документация

#### 1. Компонентная документация

**Инструменты:** Storybook или VitePress

**Требования:**
- [ ] Документация для каждого компонента
- [ ] Примеры использования
- [ ] Описание props и events
- [ ] Визуальные примеры разных состояний

#### 2. Руководства для разработчиков

**Документы:**
- [ ] Руководство по добавлению новых компонентов
- [ ] Руководство по стилизации
- [ ] Руководство по тестированию
- [ ] Руководство по работе с API

#### 3. Changelog

**Требования:**
- [ ] Ведение changelog для всех изменений
- [ ] Описание breaking changes
- [ ] Миграционные гайды при необходимости

---

## Метрики успеха

### Визуальные метрики

- [ ] Информационная плотность увеличена на 30-40%
- [ ] Время до понимания статуса системы < 3 секунд
- [ ] Все критические метрики видны без прокрутки на Dashboard

### Функциональные метрики

- [ ] Графики поддерживают zoom и pan
- [ ] Мульти-серии графики работают корректно
- [ ] Мобильная версия полностью функциональна
- [ ] Все компоненты адаптивны для разных размеров экранов

### UX метрики

- [ ] Время до первого действия (TTFA) < 2 секунд
- [ ] Количество кликов до нужной функции уменьшено на 20%
- [ ] Удовлетворенность пользователей > 4.5/5

### Производительность

- [ ] FPS при анимациях > 60
- [ ] Время загрузки страниц < 2 секунд
- [ ] Использование памяти оптимизировано

---

## Компоненты для создания/изменения

### Новые компоненты

1. `StatusIndicator.vue` — визуальные индикаторы статусов (светодиоды)
2. `MetricIndicator.vue` — крупные индикаторы метрик
3. `PhaseProgressRing.vue` — круговой индикатор прогресса фазы
4. `RecipeTimeline.vue` — timeline всех фаз рецепта
5. `PhaseComparison.vue` — сравнение фаз рецепта
6. `ChartToolbar.vue` — панель инструментов для графиков
7. `DashboardGrid.vue` — grid layout для дашбордов
8. `DashboardWidget.vue` — виджет для дашборда
9. `AlertIndicator.vue` — индикатор алертов
10. `BottomNav.vue` — нижняя навигация для мобильных
11. `MobileLayout.vue` — layout для мобильных устройств
12. `GrowthTimeline.vue` — timeline роста культуры

### Компоненты для улучшения

1. `MetricCard.vue` — добавить крупные индикаторы, тренды
2. `ZoneTargets.vue` — визуализация прогресса, круговые индикаторы
3. `ZoneCard.vue` — sparklines, быстрые действия
4. `MultiSeriesTelemetryChart.vue` — zoom, pan, аномалии
5. `ZoneTelemetryChart.vue` — улучшенная визуализация
6. `AlertCard.vue` — промышленный стиль
7. `CommandPalette.vue` — fuzzy search, категории
8. `MobileNavigation.vue` — улучшить существующий

---

## Технические детали

### Стилизация

**Цветовая схема статусов:**
- `RUNNING` / `ONLINE` — зеленый (#10b981)
- `PAUSED` — серый (#6b7280)
- `WARNING` — желтый (#f59e0b)
- `ALARM` / `OFFLINE` — красный (#ef4444)

**Размеры индикаторов:**
- Крупные метрики: `text-4xl` (36px)
- Средние метрики: `text-2xl` (24px)
- Компактные метрики: `text-lg` (18px)

**Spacing:**
- Карточки: `p-5` (20px)
- Группы элементов: `gap-4` (16px)
- Мелкие элементы: `gap-2` (8px)

### Анимации

- Плавные переходы: `transition-all duration-200`
- Пульсация для критичных статусов: `animate-pulse`
- Hover эффекты: `hover:shadow-lg transition-shadow`

### Графики

- Библиотека: ECharts (уже используется)
- Мульти-серии: несколько yAxis
- Zoom/Pan: встроенные возможности ECharts
- Аномалии: маркеры на графиках

---

## Чек-лист перед началом

### Подготовка
- [ ] Изучить все референсы из `docs/ui_refs/`
- [ ] Изучить `AI_ASSISTANT_DEV_GUIDE.md` для понимания ограничений
- [ ] Создать ветку для рефакторинга
- [ ] Настроить компонентную библиотеку
- [ ] Подготовить дизайн-систему (цвета, spacing, типографика)
- [ ] Создать задачи в трекере (GitHub Issues / Jira)
- [ ] Назначить ответственных за компоненты

### Проверка совместимости (AI_ASSISTANT_DEV_GUIDE.md)
- [ ] Изменение не ломает MQTT протокол?
- [ ] Telemetry сохраняется полностью через существующие endpoints?
- [ ] Проверены все уровни pipeline (ESP32 → MQTT → Python → PostgreSQL → Laravel → Vue)?
- [ ] Python обработает все новые данные через существующие endpoints?
- [ ] Laravel получит понятные props и не сломает существующие Inertia страницы?
- [ ] Vue корректно отобразит новые данные без удаления существующих компонентов?
- [ ] Схема БД согласована с Laravel и Python (новые таблицы/поля не ломают существующие)?
- [ ] Команды узлам формируются через существующие Laravel endpoints (не напрямую в MQTT)?
- [ ] Никаких backward-breaking изменений в существующих API?

---

## Связанные документы

- `docs/ui_refs/README.md` — описание референсов
- `doc_ai/07_FRONTEND/FRONTEND_UI_UX_SPEC.md` — спецификация UI/UX
- `doc_ai/07_FRONTEND/UI_UX_IMPROVEMENT_PLAN.md` — план улучшений
- `doc_ai/07_FRONTEND/FRONTEND_ARCH_FULL.md` — архитектура фронтенда
- `doc_ai/07_FRONTEND/FRONTEND_TESTING.md` — стратегия тестирования фронтенда
- `doc_ai/10_AI_DEV_GUIDES/AI_ASSISTANT_DEV_GUIDE.md` — руководство для ИИ-агентов по совместимости

---

## Зависимости фронтенд ↔ бэкенд

### Сводная таблица задач и требуемых endpoints

| Задача фронтенда | Существующие endpoints | Новые endpoints | Приоритет |
|------------------|------------------------|-----------------|-----------|
| Крупные индикаторы метрик на Dashboard | `/api/telemetry/aggregates` | `/api/dashboard/metrics` | 🔴 Высокий |
| Sparklines трендов | `/api/telemetry/aggregates` | `/api/dashboard/trends` | 🔴 Высокий |
| Мульти-серии графики | `/api/telemetry/aggregates` (по метрике) | `/api/zones/{id}/telemetry/multi-series` | 🔴 Высокий |
| Прогресс фазы зоны | `current_phase` из Inertia | `/api/zones/{id}/phase-progress` | 🔴 Высокий |
| Timeline фаз рецепта | `recipe.phases` из Inertia | `/api/recipes/{id}/phases-timeline` | 🟡 Средний |
| Обнаружение аномалий | `/api/ai/diagnostics` | `/api/zones/{id}/telemetry/anomalies` | 🟡 Средний |
| Конфигурация дашборда | - | `/api/dashboard/widgets` | 🟡 Средний |
| Экспорт телеметрии | - | `/api/zones/{id}/telemetry/export` | 🟢 Низкий |
| История статусов зоны | `events` из Inertia | `/api/zones/{id}/status-history` | 🟢 Низкий |
| Мобильная сводка | `/api/telemetry/aggregates` | `/api/zones/{id}/telemetry/summary` | 🟡 Средний |

### Использование существующих возможностей

#### AI и прогнозы (готово к использованию)
- ✅ `/api/ai/predict` — прогнозы параметров для отображения на графиках
- ✅ `/api/ai/explain_zone` — объяснения состояния зоны для агронома
- ✅ `/api/ai/recommend` — рекомендации для оптимизации
- ✅ `/api/ai/diagnostics` — диагностика проблем зоны

#### Симуляции (готово к использованию)
- ✅ `/api/simulations/zone/{zone}` — запуск симуляции (асинхронно)
- ✅ `/api/simulations/{jobId}` — статус симуляции
- ✅ Digital Twin сервис (порт 8003) — калибровка моделей

#### Аналитика (готово к использованию)
- ✅ `/api/recipes/{id}/analytics` — аналитика по рецепту
- ✅ `/api/recipes/comparison` — сравнение рецептов
- ✅ `/api/zones/{id}/harvests` — история урожаев
- ✅ `/api/profitability/calculate` — расчет прибыльности

### План создания новых endpoints на бэкенде

#### Приоритет 1 (критичные для Этапа 1)

**1. `/api/dashboard/metrics` (GET)**
```php
// Контроллер: DashboardController
// Метод: metrics()
// Параметры: period (1h/24h/7d)
// Возвращает: агрегированные метрики всех зон для Dashboard
{
  "status": "ok",
  "data": {
    "zones": [
      {
        "zone_id": 1,
        "zone_name": "Зона 1",
        "metrics": {
          "ph": { "current": 6.2, "target": { "min": 5.8, "max": 6.5 } },
          "ec": { "current": 1.5, "target": { "min": 1.2, "max": 1.8 } }
        },
        "status": "RUNNING"
      }
    ],
    "summary": {
      "total_zones": 10,
      "running": 8,
      "paused": 1,
      "alarm": 1
    }
  }
}
```

**2. `/api/dashboard/trends` (GET)**
```php
// Контроллер: DashboardController
// Метод: trends()
// Параметры: zone_id (опционально), period (1h/24h), metrics (ph,ec,temp,humidity)
// Возвращает: краткие тренды для sparklines (последние 20-30 точек)
{
  "status": "ok",
  "data": {
    "zone_id": 1,
    "trends": {
      "ph": [{ "ts": "...", "value": 6.2 }, ...],
      "ec": [{ "ts": "...", "value": 1.5 }, ...]
    }
  }
}
```

**3. `/api/zones/{id}/telemetry/multi-series` (GET)**
```php
// Контроллер: TelemetryController
// Метод: multiSeries()
// Параметры: metrics[] (ph,ec,temp,humidity), period (1h/24h/7d/30d)
// Возвращает: данные нескольких метрик одновременно
{
  "status": "ok",
  "data": {
    "series": [
      {
        "name": "ph",
        "metric": "PH",
        "data": [{ "ts": "...", "avg": 6.2, "min": 5.8, "max": 6.5 }]
      },
      {
        "name": "ec",
        "metric": "EC",
        "data": [{ "ts": "...", "avg": 1.5, "min": 1.2, "max": 1.8 }]
      }
    ]
  }
}
```

**4. `/api/zones/{id}/phase-progress` (GET)**
```php
// Контроллер: ZoneController
// Метод: phaseProgress()
// Возвращает: детальный прогресс текущей фазы
{
  "status": "ok",
  "data": {
    "current_phase_index": 1,
    "phase_name": "Вегетация",
    "started_at": "2025-01-20T10:00:00Z",
    "ends_at": "2025-02-10T10:00:00Z",
    "duration_hours": 504,
    "elapsed_hours": 168,
    "remaining_hours": 336,
    "progress_percent": 33.3,
    "days_elapsed": 7,
    "days_total": 21,
    "days_remaining": 14,
    "next_phase": {
      "index": 2,
      "name": "Цветение"
    }
  }
}
```

#### Приоритет 2 (важные для Этапа 2)

**5. `/api/recipes/{id}/phases-timeline` (GET)**
```php
// Контроллер: RecipeController
// Метод: phasesTimeline()
// Возвращает: оптимизированные данные для timeline визуализации
{
  "status": "ok",
  "data": {
    "phases": [
      {
        "index": 0,
        "name": "Проращивание",
        "duration_hours": 168,
        "targets": { "ph": { "min": 5.5, "max": 6.0 }, ... }
      },
      ...
    ],
    "total_duration_hours": 1008
  }
}
```

**6. `/api/zones/{id}/telemetry/anomalies` (GET)**
```php
// Контроллер: TelemetryController
// Метод: anomalies()
// Параметры: period (24h/7d), threshold (опционально)
// Возвращает: обнаруженные аномалии в телеметрии
{
  "status": "ok",
  "data": {
    "anomalies": [
      {
        "metric": "ph",
        "ts": "2025-01-27T10:30:00Z",
        "value": 7.2,
        "expected_range": { "min": 5.8, "max": 6.5 },
        "severity": "high",
        "description": "pH значительно выше целевого диапазона"
      }
    ]
  }
}
```

**7. `/api/dashboard/widgets` (GET/PUT)**
```php
// Контроллер: DashboardController
// Методы: getWidgets(), saveWidgets()
// GET: возвращает сохраненную конфигурацию виджетов для роли
// PUT: сохраняет конфигурацию виджетов
{
  "status": "ok",
  "data": {
    "role": "agronomist",
    "layout": "grid",
    "widgets": [
      { "id": "metrics", "x": 0, "y": 0, "w": 4, "h": 2 },
      { "id": "zones", "x": 4, "y": 0, "w": 4, "h": 2 }
    ]
  }
}
```

#### Приоритет 3 (желательные для Этапа 3)

**8. `/api/zones/{id}/telemetry/export` (GET)**
```php
// Контроллер: TelemetryController
// Метод: export()
// Параметры: format (csv/json), metrics[], period, from, to
// Возвращает: файл экспорта или JSON данные
```

**9. `/api/zones/{id}/status-history` (GET)**
```php
// Контроллер: ZoneController
// Метод: statusHistory()
// Параметры: from, to
// Возвращает: история изменений статусов зоны
{
  "status": "ok",
  "data": [
    {
      "status": "RUNNING",
      "changed_at": "2025-01-27T10:00:00Z",
      "reason": "MANUAL_START",
      "user_id": 1
    },
    ...
  ]
}
```

**10. `/api/zones/{id}/telemetry/summary` (GET)**
```php
// Контроллер: TelemetryController
// Метод: summary()
// Возвращает: компактная сводка телеметрии для мобильных
{
  "status": "ok",
  "data": {
    "ph": { "current": 6.2, "status": "ok" },
    "ec": { "current": 1.5, "status": "ok" },
    "temp": { "current": 22.5, "status": "ok" },
    "humidity": { "current": 65, "status": "warning" }
  }
}
```

**11. `/api/dashboard/mobile` (GET)**
```php
// Контроллер: DashboardController
// Метод: mobile()
// Возвращает: оптимизированные данные для мобильных устройств
{
  "status": "ok",
  "data": {
    "summary": { "zones": 10, "running": 8, "alerts": 2 },
    "problematic_zones": [...], // только критичные
    "recent_events": [...] // последние 5 событий
  }
}
```

### Интеграция с Python сервисами

#### Использование Digital Twin для симуляций

**Текущий flow:**
1. Фронтенд → `/api/simulations/zone/{zone}` (Laravel)
2. Laravel → `RunSimulationJob` → Digital Twin сервис (порт 8003)
3. Digital Twin → возвращает результаты симуляции
4. Laravel → сохраняет результаты в Cache
5. Фронтенд → `/api/simulations/{jobId}` → получает результаты

**Улучшения для рефакторинга:**
- [ ] Добавить WebSocket уведомления о завершении симуляции
- [ ] Добавить прогресс симуляции (процент выполнения)
- [ ] Интегрировать результаты симуляции в графики телеметрии

#### Использование AI для прогнозов

**Текущий flow:**
1. Фронтенд → `/api/ai/predict` (zone_id, metric_type, horizon_minutes)
2. Laravel → `PredictionService` → генерирует прогноз
3. Возвращает прогноз с confidence

**Улучшения для рефакторинга:**
- [ ] Отображать прогнозы на графиках телеметрии
- [ ] Использовать `/api/ai/explain_zone` для объяснений в UI
- [ ] Использовать `/api/ai/recommend` для рекомендаций агроному

### Зависимости от моделей данных

#### Модели, которые нужно расширить

**DeviceNode:**
- [ ] Добавить поле `rssi` для индикатора качества связи
- [ ] Добавить поле `battery_level` (если применимо)
- [ ] Добавить поле `power_consumption` (если применимо)

**Zone:**
- [ ] Убедиться, что `settings` содержит все необходимые данные для cycles
- [ ] Проверить наличие `updated_at` для отслеживания изменений

**Recipe:**
- [ ] Убедиться, что `phases` содержат все необходимые данные для timeline
- [ ] Проверить формат `targets` в фазах

### Чек-лист синхронизации

**Перед началом реализации фронтенда:**
- [ ] Проверить доступность всех существующих endpoints
- [ ] Создать задачи на бэкенде для новых endpoints (Приоритет 1)
- [ ] Убедиться, что модели данных содержат все необходимые поля
- [ ] Протестировать интеграцию с Python сервисами (Digital Twin, AI)

**Во время реализации:**
- [ ] Использовать существующие endpoints где возможно
- [ ] Создавать новые endpoints параллельно с фронтендом
- [ ] Тестировать интеграцию после каждого этапа

**После завершения этапа:**
- [ ] Проверить производительность новых endpoints
- [ ] Оптимизировать запросы к БД
- [ ] Добавить кеширование где необходимо

---

**Дата создания:** 2025-01-27  
**Последнее обновление:** 2025-01-27  
**Статус:** Полностью синхронизировано с требованиями:
- ✅ Синхронизировано с бэкендом и правилами совместимости (AI_ASSISTANT_DEV_GUIDE.md)
- ✅ Дополнено улучшениями на основе лучших практик
- ✅ Добавлены критичные задачи для исправления проблем (разметка, 500 ошибки, AI прогнозы, мастер циклов)
- ✅ Уточнены приоритеты (дашборды первые, поэтапно весь фронт)
- ✅ Уточнены технические требования (только русский язык, современные браузеры, адаптивный дизайн)
- ✅ Добавлены заглушки для энергопотребления с TODO
- ✅ Уточнены периоды для исторических данных (по умолчанию 1 день, выбор: сутки, 7 дней, месяц)

