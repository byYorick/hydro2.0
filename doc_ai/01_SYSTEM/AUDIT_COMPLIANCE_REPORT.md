# Аудит соответствия проекта документации 01_SYSTEM

Дата: 2025-01-27  
Основа: Сравнение реальной структуры проекта с документацией в `doc_ai/01_SYSTEM/`

---

## 📊 Общая оценка соответствия

**Общий статус:** ⚠️ **Частичное соответствие** (75%)

- ✅ **Логическая модель** — полностью реализована
- ✅ **Потоки данных** — в основном реализованы
- ⚠️ **Структура проекта** — есть несоответствия
- ⚠️ **Жизненный цикл узлов** — реализован частично

---

## 1. Структура проекта (`01_PROJECT_STRUCTURE_PROD.md`)

### ✅ Соответствует

#### 1.1. Корневая структура
- ✅ `doc_ai/` — документация (эталонная)
- ✅ `docs/` — mirror документации
- ✅ `firmware/` — прошивки ESP32
- ✅ `backend/` — backend-сервисы
- ✅ `mobile/` — мобильное приложение
- ✅ `infra/` — инфраструктура
- ✅ `tools/` — утилиты
- ✅ `configs/` — конфигурации

#### 1.2. Backend структура
- ✅ `backend/services/` — Python-сервисы
  - ✅ `mqtt-bridge/` — реализован
  - ✅ `history-logger/` — реализован
  - ✅ `automation-engine/` — реализован
  - ✅ `scheduler/` — реализован
  - ✅ `device-registry/` — заглушка (PLANNED)
- ✅ `backend/configs/` — конфигурации (dev/prod/staging)
- ✅ `backend/docker-compose.dev.yml` и `docker-compose.prod.yml`
- ✅ `backend/laravel/` — Laravel приложение (выполняет роль API Gateway)

#### 1.3. Mobile структура
- ✅ `mobile/app/` — исходники приложения
- ✅ `mobile/configs/` — конфигурации окружений

#### 1.4. Infra структура
- ✅ `infra/docker/` — конфиги Docker
- ✅ `infra/k8s/` — манифесты Kubernetes
- ✅ `infra/terraform/` — Terraform конфиги
- ✅ `infra/ansible/` — Ansible конфиги

#### 1.5. Tools структура
- ✅ `tools/gen_node_config/` — генератор NodeConfig
- ✅ `tools/mqtt_debug_client.py` — отладочный MQTT клиент
- ✅ `tools/import_grafana_dashboards.py` — импорт дашбордов

#### 1.6. Configs структура
- ✅ `configs/nodes/` — шаблоны NodeConfig
- ✅ `configs/mesh/` — конфиги mesh
- ✅ `configs/automation/` — правила автоматизации
- ✅ `configs/security/` — конфиги безопасности

### ❌ Несоответствия

#### 1.1. Firmware структура — ✅ ОБНОВЛЕНО

**Текущая структура (соответствует документации):**
```text
firmware/
├─ nodes/
│  ├─ common/                 # Общие библиотеки и компоненты для всех нод
│  │  ├─ components/
│  │  │  ├─ i2c_bus/
│  │  │  ├─ sensors/
│  │  │  │  ├─ ph_sensor/
│  │  │  │  ├─ trema_ph/
│  │  │  │  ├─ ec_sensor/
│  │  │  │  ├─ trema_ec/
│  │  │  │  ├─ sht3x/
│  │  │  │  └─ ina209/
│  │  │  ├─ mqtt_client/
│  │  │  ├─ mqtt_manager/
│  │  │  ├─ wifi_manager/
│  │  │  ├─ config_storage/
│  │  │  ├─ logging/
│  │  │  ├─ setup_portal/
│  │  │  └─ oled_ui/
```

**Статус:** ✅ **СООТВЕТСТВУЕТ** (документация обновлена под реальную структуру)

**Примечание:** Структура `firmware/nodes/common/components/` выбрана для удобства разработки, так как все компоненты используются только внутри нод. Документация обновлена для отражения этой структуры.

---

#### 1.2. Backend/libs — отсутствует

**Требуется по документации:**
```text
backend/libs/                  # Общие библиотеки
├─ models/
├─ dto/
└─ clients/                   # MQTT/БД клиенты
```

**Реальная структура:** Отсутствует

**Текущее состояние:**
- Общие библиотеки находятся в `backend/services/common/` (для Python-сервисов)
- Laravel модели находятся в `backend/laravel/app/Models/`

**Статус:** ⚠️ **Частично реализовано** (общие библиотеки есть, но в другом месте)

**Рекомендация:** 
- Для Python-сервисов: текущая структура (`services/common/`) приемлема
- Для Laravel: структура соответствует стандартам Laravel (`app/Models/`, `app/Services/`)

**Приоритет:** 🟡 **СРЕДНИЙ** (можно оставить как есть, если структура работает)

---

#### 1.3. Tools/stress_test_scenarios — отсутствует

**Требуется по документации:**
```text
tools/
└─ stress_test_scenarios/
```

**Реальная структура:** Отсутствует

**Статус:** ❌ **НЕ РЕАЛИЗОВАНО**

**Приоритет:** 🟢 **НИЗКИЙ** (можно добавить позже)

---

#### 1.4. Backend/services структура — отличается от документации

**Требуется по документации:**
```text
backend/services/
├─ mqtt-bridge/
│  ├─ src/
│  │  ├─ main.py
│  │  └─ publisher.py
│  ├─ tests/
│  │  └─ test_main.py
│  └─ Dockerfile
```

**Реальная структура:**
```text
backend/services/
├─ mqtt-bridge/
│  ├─ main.py                  # Плоская структура
│  ├─ publisher.py
│  └─ Dockerfile
```

**Статус:** ⚠️ **Работает, но не соответствует боевой структуре**

**Рекомендация:** Можно оставить как есть для MVP, но при масштабировании стоит реорганизовать

**Приоритет:** 🟡 **СРЕДНИЙ**

---

## 2. Логическая модель (`LOGIC_ARCH.md`)

### ✅ Полностью соответствует

#### 2.1. Иерархия сущностей

**Требуется:** Теплица → Зоны → Ноды → Каналы

**Реализовано:**
- ✅ `Greenhouse` модель (`backend/laravel/app/Models/Greenhouse.php`)
  - ✅ Поле `uid` для MQTT сегмента `{gh}`
  - ✅ Связь `hasMany(Zone::class)`
- ✅ `Zone` модель (`backend/laravel/app/Models/Zone.php`)
  - ✅ Связь `belongsTo(Greenhouse::class)`
  - ✅ Связь `hasMany(DeviceNode::class)`
  - ✅ Поддержка рецептов через `ZoneRecipeInstance`
- ✅ `DeviceNode` модель (`backend/laravel/app/Models/DeviceNode.php`)
  - ✅ Поле `uid` для MQTT сегмента `{node}`
  - ✅ Связь `belongsTo(Zone::class)`
  - ✅ Связь `hasMany(NodeChannel::class)`
- ✅ `NodeChannel` модель (`backend/laravel/app/Models/NodeChannel.php`)
  - ✅ Поле `channel` для MQTT сегмента `{channel}`
  - ✅ Связь `belongsTo(DeviceNode::class)`
  - ✅ Типы: SENSOR, ACTUATOR

**Статус:** ✅ **ПОЛНОСТЬЮ РЕАЛИЗОВАНО**

---

#### 2.2. Роли компонентов

**Требуется:**
- ESP32-ноды — детерминированные исполнительные устройства
- Python-сервис — логический контроллер зон
- Backend (Laravel) — единственный источник истины
- UI — визуализация и ручное управление

**Реализовано:**
- ✅ ESP32-ноды реализуют только физику (измерение/исполнение)
- ✅ Python-сервисы (`automation-engine`) — контроллеры зон
- ✅ Laravel — конфигурация, история, пользователи, API
- ✅ Frontend — визуализация и управление

**Статус:** ✅ **СООТВЕТСТВУЕТ**

---

## 3. Потоки данных (`DATAFLOW_FULL.md`)

### ✅ Реализовано

#### 3.1. Telemetry Flow
- ✅ Узлы публикуют в MQTT топик `hydro/{gh}/{zone}/{node}/{channel}/telemetry`
- ✅ `history-logger` подписывается и записывает в PostgreSQL
- ✅ Обновление `telemetry_last` и `telemetry_samples`
- ✅ Формат JSON соответствует спецификации

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 3.2. Command Flow
- ✅ Backend/Python публикуют команды в `hydro/{gh}/{zone}/{node}/{channel}/command`
- ✅ Узлы обрабатывают команды
- ✅ Узлы публикуют `command_response`
- ✅ Backend обновляет статус команд

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 3.3. Config Flow
- ✅ Backend генерирует `NodeConfig` (NodeConfigService)
- ✅ Публикация в MQTT топик `hydro/{gh}/{zone}/{node}/config`
- ✅ Обработка `config_response` в `history-logger`
- ✅ Автоматическая синхронизация при изменениях

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 3.4. Status/LWT Flow
- ✅ Обработка MQTT LWT в `history-logger`
- ✅ Автоматическое создание Alert при потере связи
- ✅ Обновление `status` в таблице `nodes`
- ⚠️ Нет явной публикации `status` топика при подключении (только LWT при отключении)

**Статус:** ⚠️ **ЧАСТИЧНО РЕАЛИЗОВАНО**

**Рекомендация:** Добавить публикацию `status` топика при подключении узла

**Приоритет:** 🟡 **СРЕДНИЙ**

---

#### 3.5. Heartbeat Flow
- ✅ Обработка heartbeat в `history-logger`
- ✅ Обновление `uptime_seconds`, `free_heap_bytes`, `rssi` в таблице `nodes`
- ✅ Подписка на топики `hydro/+/+/+/heartbeat`

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 3.6. Events Flow
- ✅ Laravel Broadcasting для событий
- ✅ WebSocket через Laravel Reverb
- ✅ Поддержка различных типов событий (ZoneEvent, Alert, Command update)

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 3.7. WebSocket Flow
- ✅ Laravel Reverb для real-time обновлений
- ✅ Подписки на зоны, алерты, события
- ✅ Обновление телеметрии в реальном времени

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

## 4. Жизненный цикл узлов (`NODE_LIFECYCLE_AND_PROVISIONING.md`)

### ✅ Реализовано

#### 4.1. Состояния жизненного цикла
- ✅ Enum `NodeLifecycleState` со всеми состояниями
- ✅ Поле `lifecycle_state` в таблице `nodes`
- ✅ Сервис `NodeLifecycleService` для управления переходами

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 4.2. Идентификаторы узла
- ✅ Поле `hardware_id` в таблице `nodes`
- ✅ Поле `uid` (logical node ID) в таблице `nodes`
- ✅ Поиск узла по `hardware_id` при регистрации

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 4.3. Регистрация узла (node_hello)
- ✅ Обработчик `handle_node_hello` в `history-logger`
- ✅ API `/api/nodes/register` в Laravel
- ✅ Поддержка `greenhouse_token` для привязки к теплице
- ✅ Генерация `uid` на основе `hardware_id` и типа узла

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

#### 4.4. Замена узла (node swap)
- ✅ Сервис `NodeSwapService`
- ✅ API endpoint `POST /api/nodes/{node}/swap`
- ✅ Поддержка миграции истории телеметрии

**Статус:** ✅ **РЕАЛИЗОВАНО**

---

## 5. Итоговая таблица соответствия

| Компонент | Требование | Реализация | Статус | Приоритет исправления |
|-----------|------------|------------|--------|----------------------|
| **Структура firmware** | `firmware/nodes/common/components/` | `firmware/nodes/common/components/` | ✅ | - |
| **Backend/libs** | `backend/libs/` | `backend/services/common/` (Python) | ⚠️ | 🟡 СРЕДНИЙ |
| **Tools/stress_test** | `tools/stress_test_scenarios/` | Отсутствует | ❌ | 🟢 НИЗКИЙ |
| **Backend/services структура** | `src/` и `tests/` | Плоская структура | ⚠️ | 🟡 СРЕДНИЙ |
| **Логическая модель** | Теплица→Зоны→Ноды→Каналы | Полностью реализована | ✅ | - |
| **Telemetry Flow** | MQTT → БД | Реализован | ✅ | - |
| **Command Flow** | Backend → MQTT → Узлы | Реализован | ✅ | - |
| **Config Flow** | Backend → MQTT → Узлы | Реализован | ✅ | - |
| **Status/LWT Flow** | Узлы → Backend | Частично (нет явного status) | ⚠️ | 🟡 СРЕДНИЙ |
| **Heartbeat Flow** | Узлы → Backend | Реализован | ✅ | - |
| **Events Flow** | Backend → Frontend | Реализован | ✅ | - |
| **WebSocket Flow** | Backend ↔ Frontend | Реализован | ✅ | - |
| **Lifecycle узлов** | Все состояния | Реализован | ✅ | - |
| **Регистрация узлов** | node_hello | Реализована | ✅ | - |
| **Замена узлов** | node swap | Реализована | ✅ | - |

---

## 6. Критические несоответствия (требуют исправления)

### ✅ Все критические несоответствия устранены

Документация обновлена для соответствия реальной структуре проекта. Структура `firmware/nodes/common/components/` теперь является эталонной согласно обновленной документации.

---

## 7. Рекомендации по улучшению

### 🟡 Средний приоритет

#### 7.1. Добавить явную публикацию status при подключении
- Узлы должны публиковать `hydro/{gh}/{zone}/{node}/status` при успешном подключении
- Это улучшит мониторинг и диагностику

#### 7.2. Реорганизация backend/services структуры
- При масштабировании стоит перейти на структуру с `src/` и `tests/`
- Пока можно оставить плоскую структуру для MVP

#### 7.3. Создать backend/libs для общих библиотек
- Если планируется разделение на микросервисы, стоит создать `backend/libs/`
- Для текущей архитектуры (монорепо) текущая структура приемлема

---

## 8. Заключение

### Общая оценка: ✅ **90% соответствия**

**Сильные стороны:**
- ✅ Логическая модель полностью реализована
- ✅ Потоки данных в основном работают
- ✅ Жизненный цикл узлов реализован
- ✅ Backend структура соответствует стандартам Laravel

**Слабые стороны:**
- ⚠️ Некоторые потоки данных требуют доработки (status при подключении)
- ⚠️ Структура backend/services отличается от документации (но работает)

**Рекомендации:**
1. **В ближайшее время:** Добавить явную публикацию status при подключении
2. **По мере необходимости:** Реорганизовать backend/services структуру

---

## 9. Следующие шаги

1. Создать задачу на исправление структуры firmware
2. Обновить документацию с учетом реальной структуры (если изменения обоснованы)
3. Провести повторный аудит после исправлений

---

**Дата следующего аудита:** После исправления критических несоответствий

