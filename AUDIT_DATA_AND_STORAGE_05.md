# АУДИТ: DATA_AND_STORAGE (05)
## Полный детальный аудит проекта - Данные и хранилище

**Дата аудита:** 2025-01-15  
**Область:** `doc_ai/05_DATA_AND_STORAGE`  
**Статус:** ✅ Проверено с выявленными проблемами

---

## 1. ОБЗОР ДОКУМЕНТАЦИИ

### 1.1. Структура документации

Документация состоит из 3 файлов:

1. **DATA_MODEL_REFERENCE.md** (351 строка)
   - Справочник моделей данных
   - Описание таблиц, связей, индексов
   - Использование в Laravel и Python

2. **DATA_RETENTION_POLICY.md** (328 строк)
   - Политика хранения данных
   - Классификация данных (L0-L5)
   - Сроки хранения и архивирование

3. **TELEMETRY_PIPELINE.md** (105 строк)
   - Спецификация телеметрического конвейера
   - Путь данных от ESP32 до UI

---

## 2. КРИТИЧЕСКИЕ ПРОБЛЕМЫ

### 2.1. ❌ Отсутствующие таблицы в документации

**Проблема:** В реальной БД есть таблицы, которые не описаны в `DATA_MODEL_REFERENCE.md`:

1. **presets** - таблица пресетов (шаблонов конфигураций)
   - Найдена в миграции: `2025_11_16_000014_create_presets_table.php`
   - Используется в модели `Zone` (связь `preset_id`)
   - **Критичность:** Высокая - используется в API и моделях

2. **harvests** - таблица урожаев
   - Найдена в миграции: `2025_11_16_000016_create_harvests_table.php`
   - Используется в Reports API
   - **Критичность:** Средняя - используется в аналитике

3. **recipe_analytics** - аналитика по рецептам
   - Найдена в миграции: `2025_11_16_000017_create_recipe_analytics_table.php`
   - Используется в Reports API
   - **Критичность:** Средняя - используется в аналитике

4. **parameter_predictions** - прогнозы параметров (AI)
   - Найдена в миграции: `2025_11_17_042745_create_parameter_predictions_table.php`
   - Используется в AI API
   - **Критичность:** Средняя - используется в AI функционале

5. **zone_simulations** - симуляции Digital Twin
   - Найдена в миграции: `2025_11_17_043028_create_zone_simulations_table.php`
   - Используется в Simulations API
   - **Критичность:** Средняя - используется в Digital Twin

6. **zone_model_params** - параметры моделей для зон
   - Найдена в миграции: `2025_11_17_043030_create_zone_model_params_table.php`
   - Используется в AI/Simulations
   - **Критичность:** Низкая - внутренняя таблица

### 2.2. ❌ Несоответствие структуры таблиц

**Проблема:** Документация не соответствует реальной структуре БД:

#### 2.2.1. Таблица `zones`
- **В документации:** Нет поля `greenhouse_id` и `preset_id`
- **В реальности:** 
  - `greenhouse_id` - FK к `greenhouses` (nullable, cascadeOnDelete)
  - `preset_id` - FK к `presets` (nullable, nullOnDelete)
- **Критичность:** Высокая - критическая связь

#### 2.2.2. Таблица `telemetry_samples`
- **В документации:** Указано поле `channel_id`
- **В реальности:** Поле называется `channel` (VARCHAR, не FK)
- **Критичность:** Средняя - может привести к путанице

#### 2.2.3. Таблица `telemetry_last`
- **В документации:** Указан составной PK `(zone_id, metric_type)`
- **В реальности:** ✅ Соответствует
- **Дополнительно:** В реальности есть поля `node_id` и `channel` (nullable)

### 2.3. ❌ Отсутствующие таблицы в политике хранения

**Проблема:** В `DATA_RETENTION_POLICY.md` упоминаются таблицы, которых нет в БД:

1. **telemetry_raw** - упоминается в разделе 3.1 (Hot Telemetry)
   - В реальности используется `telemetry_samples`
   - **Критичность:** Высокая - несоответствие терминологии

2. **zone_events** - упоминается в разделе 5 (Events)
   - В реальности таблица НЕ СУЩЕСТВУЕТ
   - **Критичность:** Высокая - документация описывает несуществующую функциональность

3. **commands_archive** - упоминается в разделе 4
   - В реальности таблица НЕ СУЩЕСТВУЕТ
   - **Критичность:** Средняя - архивирование не реализовано

4. **zone_events_archive** - упоминается в разделе 5
   - В реальности таблица НЕ СУЩЕСТВУЕТ
   - **Критичность:** Средняя - архивирование не реализовано

5. **system_logs, node_logs, ai_logs, scheduler_logs** - упоминаются в разделе 7
   - В реальности таблицы НЕ СУЩЕСТВУЮТ
   - **Критичность:** Средняя - логирование в БД не реализовано

### 2.4. ❌ Отсутствующие таблицы в документации (OTA)

**Проблема:** В `DATA_MODEL_REFERENCE.md` описана таблица `firmware_files` (раздел 10.1), но:

- В реальности таблица НЕ СУЩЕСТВУЕТ в миграциях
- **Критичность:** Низкая - функционал OTA не реализован

---

## 3. СРЕДНИЕ ПРОБЛЕМЫ

### 3.1. ⚠️ Неполное описание агрегированных таблиц

**Проблема:** В `DATA_RETENTION_POLICY.md` упоминаются таблицы агрегации, но структура описана неполно:

- **telemetry_agg_1m** - упоминается, но структура не описана
- **telemetry_agg_1h** - упоминается, но структура не описана  
- **telemetry_daily** - упоминается, но структура не описана

**В реальности:**
- Все 3 таблицы существуют (миграция `2025_11_16_184939_create_telemetry_aggregated_tables.php`)
- Используется TimescaleDB hypertable
- Структура: `zone_id`, `node_id`, `channel`, `metric_type`, `value_avg`, `value_min`, `value_max`, `value_median`, `sample_count`, `ts`/`date`

**Критичность:** Средняя - важно для понимания архитектуры

### 3.2. ⚠️ Несоответствие в TELEMETRY_PIPELINE.md

**Проблема:** В разделе 4 указано поле `channel_id`, но в реальности используется `channel` (VARCHAR).

**Критичность:** Низкая - терминологическая ошибка

### 3.3. ⚠️ Отсутствие описания связи zones-greenhouses

**Проблема:** В `DATA_MODEL_REFERENCE.md` не описана связь `zones.greenhouse_id` → `greenhouses.id`.

**Критичность:** Средняя - важная связь для понимания структуры

### 3.4. ⚠️ Неполное описание команды очистки

**Проблема:** В `DATA_RETENTION_POLICY.md` упоминается очистка данных, но:

- Реализована только команда `telemetry:cleanup-raw` (Laravel)
- Нет описания автоматической агрегации
- Нет описания архивирования в S3

**Критичность:** Средняя - функционал частично реализован

---

## 4. НИЗКИЕ ПРОБЛЕМЫ / УЛУЧШЕНИЯ

### 4.1. 💡 Несоответствие имен моделей

**Проблема:** В документации упоминается модель `Node`, но в реальности используется `DeviceNode`.

**Критичность:** Низкая - косметическая проблема

### 4.2. 💡 Отсутствие описания TimescaleDB

**Проблема:** В документации не упоминается использование TimescaleDB для агрегированных таблиц.

**Критичность:** Низкая - техническая деталь

### 4.3. 💡 Неполное описание индексов

**Проблема:** В `DATA_MODEL_REFERENCE.md` описаны не все индексы:

- Отсутствуют индексы для `telemetry_agg_*` таблиц
- Отсутствуют индексы для `harvests`, `recipe_analytics`, `parameter_predictions`

**Критичность:** Низкая - важно для производительности

---

## 5. СООТВЕТСТВИЕ РЕАЛЬНОСТИ

### 5.1. ✅ Правильно описанные таблицы

1. **greenhouses** - структура соответствует
2. **nodes** - структура соответствует (кроме названия модели)
3. **node_channels** - структура соответствует
4. **telemetry_samples** - структура соответствует (кроме `channel_id` → `channel`)
5. **telemetry_last** - структура соответствует
6. **recipes** - структура соответствует
7. **recipe_phases** - структура соответствует
8. **zone_recipe_instances** - структура соответствует
9. **commands** - структура соответствует
10. **alerts** - структура соответствует
11. **users** - структура соответствует (базовая)

### 5.2. ✅ Правильно описанные связи

- `zone 1—N nodes` ✅
- `node 1—N channels` ✅
- `zone 1—N telemetry` ✅
- `zone 1—N alerts` ✅
- `recipe 1—N phases` ✅
- `zone 1—1 recipe_instance` ✅
- `zone 1—N commands` ✅

### 5.3. ✅ Правильно описанный pipeline

**TELEMETRY_PIPELINE.md:**
- Цепочка телеметрии описана правильно ✅
- Формат MQTT топика соответствует ✅
- Python Router → PostgreSQL описан правильно ✅
- Laravel API эндпоинты соответствуют ✅

---

## 6. РЕКОМЕНДАЦИИ

### 6.1. Критические исправления

1. **Добавить описание отсутствующих таблиц:**
   - `presets` - полное описание структуры и связей
   - `harvests` - описание структуры и использования
   - `recipe_analytics` - описание структуры
   - `parameter_predictions` - описание структуры
   - `zone_simulations` - описание структуры
   - `zone_model_params` - описание структуры

2. **Исправить несоответствия:**
   - В `zones` добавить `greenhouse_id` и `preset_id`
   - В `telemetry_samples` исправить `channel_id` → `channel`
   - В `TELEMETRY_PIPELINE.md` исправить `channel_id` → `channel`

3. **Удалить или исправить упоминания несуществующих таблиц:**
   - `telemetry_raw` → `telemetry_samples`
   - `zone_events` - удалить или реализовать
   - `commands_archive` - удалить или реализовать
   - `zone_events_archive` - удалить или реализовать
   - `system_logs`, `node_logs`, `ai_logs`, `scheduler_logs` - удалить или реализовать

4. **Добавить описание связи zones-greenhouses:**
   - В раздел 2.2 добавить `greenhouse_id` FK
   - В раздел 11 добавить связь `greenhouse 1—N zones`

### 6.2. Средние исправления

1. **Добавить описание агрегированных таблиц:**
   - Полная структура `telemetry_agg_1m`
   - Полная структура `telemetry_agg_1h`
   - Полная структура `telemetry_daily`
   - Описание TimescaleDB hypertable

2. **Уточнить политику хранения:**
   - Указать, что `telemetry_raw` = `telemetry_samples`
   - Описать реализованные команды очистки
   - Описать процесс агрегации (если реализован)

3. **Добавить описание индексов:**
   - Индексы для агрегированных таблиц
   - Индексы для новых таблиц (harvests, recipe_analytics и т.д.)

### 6.3. Улучшения

1. **Добавить описание TimescaleDB:**
   - Упоминание использования TimescaleDB
   - Описание hypertable для агрегированных данных

2. **Уточнить названия моделей:**
   - `Node` → `DeviceNode` в документации

3. **Добавить описание OTA (если планируется):**
   - Или удалить раздел 10.1 из документации

---

## 7. СТАТИСТИКА АУДИТА

### 7.1. Соответствие документации

- **Полностью соответствует:** 11 таблиц
- **Частично соответствует:** 3 таблицы (zones, telemetry_samples, telemetry_last)
- **Отсутствует в документации:** 6 таблиц (presets, harvests, recipe_analytics, parameter_predictions, zone_simulations, zone_model_params)
- **Упоминается, но не существует:** 7 таблиц (telemetry_raw, zone_events, commands_archive, zone_events_archive, system_logs, node_logs, ai_logs, scheduler_logs, firmware_files)

### 7.2. Критичность проблем

- **Критические:** 4 проблемы
- **Средние:** 4 проблемы
- **Низкие:** 3 проблемы

### 7.3. Охват документации

- **DATA_MODEL_REFERENCE.md:** ~65% соответствие реальности
- **DATA_RETENTION_POLICY.md:** ~40% соответствие реальности (много несуществующих таблиц)
- **TELEMETRY_PIPELINE.md:** ~95% соответствие реальности

---

## 8. ИТОГОВАЯ ОЦЕНКА

### 8.1. Общая оценка

**Оценка:** ⚠️ **ТРЕБУЕТ ДОРАБОТКИ**

Документация содержит существенные несоответствия с реальной структурой БД:
- Отсутствуют описания 6 важных таблиц
- Упоминаются 7 несуществующих таблиц
- Есть несоответствия в структуре существующих таблиц

### 8.2. Приоритеты исправления

1. **Высокий приоритет:**
   - Добавить описание `presets` (используется в API)
   - Исправить структуру `zones` (добавить `greenhouse_id`, `preset_id`)
   - Исправить `telemetry_raw` → `telemetry_samples` в политике хранения

2. **Средний приоритет:**
   - Добавить описание `harvests`, `recipe_analytics`
   - Добавить описание агрегированных таблиц
   - Удалить упоминания несуществующих таблиц

3. **Низкий приоритет:**
   - Уточнить названия моделей
   - Добавить описание TimescaleDB
   - Улучшить описание индексов

---

## 9. ЗАКЛЮЧЕНИЕ

Документация по данным и хранилищу требует серьезной доработки. Основные проблемы:

1. **Несоответствие реальной структуре БД** - отсутствуют описания важных таблиц
2. **Упоминание несуществующих таблиц** - создает путаницу
3. **Неполное описание связей** - отсутствует связь zones-greenhouses

**Рекомендация:** Провести полное обновление документации с синхронизацией всех таблиц, связей и политик хранения с реальной реализацией.

---

**Конец отчета аудита**

