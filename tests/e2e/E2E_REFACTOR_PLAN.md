# План рефакторинга E2E тестов под новую доменную архитектуру

**Дата создания:** 2025-12-25  
**Связано с:** MEGA_REFACTOR_PLAN.md (Этапы 0-6 завершены)

## 📊 Статус выполнения

- ✅ **Этап 0**: Анализ и планирование
- ✅ **Этап 1**: Исправление структуры БД (telemetry_last)
- ✅ **Этап 2**: Рефакторинг API helpers (частично - роль agronomist по умолчанию)
- ✅ **Этап 3**: Рефакторинг grow_cycle тестов (E50-E54)
  - ✅ E50_create_cycle_planned - 12 passed, 0 failed
  - ✅ E51_start_cycle_running - 18 passed, 0 failed
  - ✅ E52_stage_progress_timeline - 8 passed, 0 failed
  - ✅ E53_manual_advance_stage - 20 passed, 0 failed
  - ✅ E54_pause_resume_harvest - 23 passed, 0 failed
- ⏳ **Этап 4**: Рефакторинг automation_engine тестов (E60-E63)
- ⏳ **Этап 5**: Рефакторинг остальных тестов
- ⏳ **Этап 6**: Обновление документации

## Цель рефакторинга

Привести все E2E тесты в соответствие с новой доменной архитектурой:
- **GrowCycle** как центр истины (вместо `zone_recipe_instances`)
- **RecipeRevisions** с фазами по колонкам (вместо `recipe_phases.targets` JSONB)
- Новые API endpoints для управления циклами
- Удаление всех обращений к legacy таблицам и endpoints

## Ключевые изменения в архитектуре

### Удалены (legacy):
- ❌ `zone_recipe_instances` таблица
- ❌ `recipe_phases` таблица (legacy с JSONB targets)
- ❌ `zone_cycles`, `plant_cycles` таблицы
- ❌ `/api/zones/{id}/attach-recipe` endpoint
- ❌ `/api/zones/{id}/start`, `/api/zones/{id}/pause` endpoints (заменены на grow-cycle endpoints)

### Добавлены (новая модель):
- ✅ `grow_cycles` таблица (центр истины)
- ✅ `recipe_revisions` + `recipe_revision_phases` (структурированные targets)
- ✅ `grow_cycle_phases`, `grow_cycle_phase_steps` (снапшоты)
- ✅ `/api/zones/{zone}/grow-cycles` endpoint (создание цикла)
- ✅ `/api/grow-cycles/{id}/start`, `/api/grow-cycles/{id}/pause`, `/api/grow-cycles/{id}/resume`, `/api/grow-cycles/{id}/harvest` endpoints
- ✅ `/api/grow-cycles/{id}/advance-phase`, `/api/grow-cycles/{id}/set-phase` endpoints
- ✅ `/api/recipes/{id}/revisions`, `/api/recipe-revisions/{id}/phases` endpoints
- ✅ `/api/plants` endpoint (CRUD для растений)

## План работ по этапам

### ✅ Этап 1: Исправление структуры БД (telemetry_last)

**Проблема:** E2E БД использует старую структуру `telemetry_last` с `sensor_id`, но history-logger пишет в новую структуру с `zone_id`, `node_id`, `metric_type`.

**Решение:**
- ✅ Пересоздана таблица `telemetry_last` со структурой `(zone_id, node_id, metric_type, value, channel, updated_at)`
- ✅ Исправлен `ON CONFLICT` в `backend/services/common/db.py` и `backend/services/history-logger/main.py` для использования правильного primary key `(zone_id, node_id, metric_type)`
- ✅ Обновлен тест `E01_bootstrap.yaml` для работы с новой структурой

**Статус:** ✅ Завершен

---

### ⏳ Этап 2: Рефакторинг API helpers

**Файл:** `tests/e2e/browser/helpers/api.ts`

#### 2.1. Обновление методов создания рецептов

**Текущее состояние:**
- `createTestRecipe()` - создает рецепт и фазы через `/api/recipes/{id}/phases`

**Нужно изменить:**
```typescript
// Старый подход:
POST /api/recipes → POST /api/recipes/{id}/phases (legacy)

// Новый подход:
POST /api/recipes → POST /api/recipes/{id}/revisions → 
  POST /api/recipe-revisions/{rev_id}/phases → 
  POST /api/recipe-revisions/{rev_id}/publish
```

**Задачи:**
- [ ] Обновить `createTestRecipe()` для создания `RecipeRevision` и `RecipeRevisionPhase`
- [ ] Добавить метод `createRecipeRevision(recipeId, description?)`
- [ ] Добавить метод `addPhaseToRevision(revisionId, phaseData)`
- [ ] Добавить метод `publishRevision(revisionId)`

#### 2.2. Обновление методов управления циклами

**Текущее состояние:**
- `attachRecipeToZone()` - использует `/api/zones/{id}/attach-recipe` (legacy)
- `startZone()`, `pauseZone()`, `resumeZone()`, `harvestZone()` - используют `/api/zones/{id}/start|pause|resume|harvest` (legacy)

**Нужно изменить:**
```typescript
// Старый подход:
POST /api/zones/{id}/attach-recipe
POST /api/zones/{id}/start
POST /api/zones/{id}/pause

// Новый подход:
POST /api/zones/{id}/grow-cycles (создание цикла)
POST /api/grow-cycles/{cycle_id}/start
POST /api/grow-cycles/{cycle_id}/pause
POST /api/grow-cycles/{cycle_id}/resume
POST /api/grow-cycles/{cycle_id}/harvest
```

**Задачи:**
- [x] Обновить `attachRecipeToZone()` → `createGrowCycle()` (уже частично сделано)
- [ ] Обновить `startZone()` → `startGrowCycle(cycleId)`
- [ ] Обновить `pauseZone()` → `pauseGrowCycle(cycleId)`
- [ ] Обновить `resumeZone()` → `resumeGrowCycle(cycleId)`
- [ ] Обновить `harvestZone()` → `harvestGrowCycle(cycleId)`
- [ ] Добавить метод `getActiveGrowCycle(zoneId)` для получения активного цикла

#### 2.3. Добавление методов для работы с растениями

**Задачи:**
- [x] Добавить `createTestPlant()` (уже частично сделано)
- [ ] Добавить `getPlant(plantId)`
- [ ] Добавить `updatePlant(plantId, data)`
- [ ] Добавить `deletePlant(plantId)`

#### 2.4. Обновление интерфейсов

**Задачи:**
- [ ] Обновить `TestRecipePhase` для работы с новой структурой (колонки вместо JSONB targets)
- [ ] Добавить `TestRecipeRevision` интерфейс
- [ ] Добавить `TestGrowCycle` интерфейс

**Статус:** ⏳ В процессе (частично выполнено)

---

### ⏳ Этап 3: Рефакторинг grow_cycle тестов (E50-E54)

#### 3.1. E50_create_cycle_planned.yaml

**Текущие проблемы:**
- ❌ Использует `/api/plants` без POST endpoint (500 ошибка)
- ❌ Создает рецепт через старый API
- ❌ Проверяет `grow_cycles` через БД запросы

**Задачи:**
- [x] Добавить POST `/api/plants` endpoint в Laravel (выполнено)
- [x] Обновить `PlantController::store()` для возврата JSON (выполнено)
- [ ] Обновить создание рецепта через новый API (revisions)
- [ ] Обновить создание цикла через `/api/zones/{zone}/grow-cycles`
- [ ] Обновить проверки для использования правильных полей

**Статус:** ⏳ В процессе

#### 3.2. E51_start_cycle_running.yaml

**Текущие проблемы:**
- ❌ Использует старый endpoint `/api/zones/{id}/start`
- ❌ Проверяет `zone_recipe_instances` вместо `grow_cycles`

**Задачи:**
- [ ] Заменить `/api/zones/{id}/start` на `/api/grow-cycles/{id}/start`
- [ ] Обновить проверки для `grow_cycles.status = 'RUNNING'`
- [ ] Добавить проверку `current_phase_id` в снапшотах

**Статус:** ⏳ Не начат

#### 3.3. E52_stage_progress_timeline.yaml

**Текущие проблемы:**
- ❌ Использует `zone_recipe_instances` для отслеживания прогресса
- ❌ Проверяет `recipe_phases` вместо `recipe_revision_phases`

**Задачи:**
- [ ] Обновить запросы для использования `grow_cycles` и `grow_cycle_phases`
- [ ] Проверять прогресс через `grow_cycle_phases.phase_started_at` и `phase_due_at`
- [ ] Обновить проверки для работы с `recipe_revision_phases.duration_hours`

**Статус:** ⏳ Не начат

#### 3.4. E53_manual_advance_stage.yaml

**Текущие проблемы:**
- ❌ Использует `/api/zones/{id}/next-phase` (legacy)
- ❌ Проверяет `zone_recipe_instances.current_phase_index`

**Задачи:**
- [ ] Заменить на `/api/grow-cycles/{id}/advance-phase`
- [ ] Обновить проверки для `grow_cycles.current_phase_id` (снапшот)
- [ ] Добавить проверку `grow_cycle_transitions` для логирования перехода

**Статус:** ⏳ Не начат

#### 3.5. E54_pause_resume_harvest.yaml

**Текущие проблемы:**
- ❌ Использует `/api/zones/{id}/pause|resume|harvest` (legacy)
- ❌ Проверяет `zone_recipe_instances.status`

**Задачи:**
- [ ] Заменить на `/api/grow-cycles/{id}/pause|resume|harvest`
- [ ] Обновить проверки для `grow_cycles.status`
- [ ] Добавить проверку `grow_cycle_transitions` для каждого перехода

**Статус:** ⏳ Не начат

**Общий статус этапа:** ⏳ В процессе (E50 частично, остальные не начаты)

---

### ⏳ Этап 4: Рефакторинг automation_engine тестов (E60-E63)

#### 4.1. E60_climate_control_happy.yaml

**Текущие проблемы:**
- ❌ Использует `/api/zones/{id}/attach-recipe` (legacy)
- ❌ Проверяет `telemetry_last` через старую структуру (если используется)
- ❌ Использует `recipe_phases.targets` для получения targets

**Задачи:**
- [ ] Заменить создание цикла на новый API
- [ ] Обновить получение effective targets через `/api/zones/{zone}/grow-cycle` или `/api/internal/effective-targets/batch`
- [ ] Проверить использование `telemetry_last` (должна быть исправлена в Этапе 1)

**Статус:** ⏳ Не начат

#### 4.2. E61_fail_closed_corrections.yaml, E62_controller_fault_isolation.yaml, E63_backoff_on_errors.yaml

**Задачи:**
- [ ] Проверить использование legacy endpoints и таблиц
- [ ] Обновить для работы с `grow_cycles` и effective targets
- [ ] Убедиться, что automation-engine использует новый API (должен быть обновлен в Этапе 3 MEGA_REFACTOR_PLAN)

**Статус:** ⏳ Не начат

**Общий статус этапа:** ⏳ Не начат

---

### ⏳ Этап 5: Рефакторинг остальных тестов

#### 5.1. Core тесты (E01, E02)

**E01_bootstrap.yaml:**
- ✅ Исправлена структура `telemetry_last` (Этап 1)
- ✅ Тест проходит успешно

**E02_auth_ws_api.yaml:**
- [ ] Проверить использование legacy endpoints (если есть)
- [ ] Обновить при необходимости

**Статус:** ✅ E01 завершен, E02 требует проверки

#### 5.2. Commands тесты (E10-E14)

**Задачи:**
- [ ] Проверить использование `zone_recipe_instances` в контексте команд
- [ ] Обновить для работы с `grow_cycles` (если команды связаны с циклами)
- [ ] Убедиться, что команды содержат `cycle_id` вместо `recipe_instance_id`

**Статус:** ⏳ Не начат

#### 5.3. Alerts тесты (E20-E25)

**Задачи:**
- [ ] Проверить использование legacy таблиц в контексте алертов
- [ ] Обновить для работы с новой моделью (если алерты связаны с циклами)

**Статус:** ⏳ Не начат

#### 5.4. Snapshot тесты (E30-E32)

**Задачи:**
- [ ] Проверить использование legacy endpoints в snapshot
- [ ] Обновить для работы с новыми endpoints (если snapshot содержит данные о циклах)

**Статус:** ⏳ Не начат

#### 5.5. Infrastructure тесты (E40-E42)

**Задачи:**
- [ ] Проверить использование legacy таблиц (`zone_infrastructure`, `infrastructure_assets`)
- [ ] Обновить для работы с новой моделью `infrastructure_instances` (если применимо)

**Статус:** ⏳ Не начат

#### 5.6. Chaos тесты (E70-E72)

**Задачи:**
- [ ] Проверить использование legacy endpoints и таблиц
- [ ] Обновить при необходимости

**Статус:** ⏳ Не начат

**Общий статус этапа:** ⏳ Частично завершен (E01), остальные требуют проверки

---

### ⏳ Этап 6: Обновление документации

**Задачи:**
- [ ] Обновить `tests/e2e/README.md` с новыми API endpoints
- [ ] Обновить `tests/E2E_FULL_TEST_SUITE_TASK.md` с новой архитектурой
- [ ] Добавить примеры использования новых API helpers
- [ ] Обновить описание структуры данных в тестах

**Статус:** ⏳ Не начат

---

## Приоритеты выполнения

### Критичные (блокируют запуск тестов):
1. ✅ **Этап 1** - Исправление структуры БД (telemetry_last) - **ЗАВЕРШЕН**
2. ⏳ **Этап 2.2** - Обновление методов управления циклами в API helpers
3. ⏳ **Этап 3.1** - Исправление E50_create_cycle_planned (добавление POST /api/plants) - **В ПРОЦЕССЕ**

### Высокий приоритет (основные тесты циклов):
4. ⏳ **Этап 3** - Рефакторинг всех grow_cycle тестов (E50-E54)
5. ⏳ **Этап 4** - Рефакторинг automation_engine тестов (E60-E63)

### Средний приоритет (проверка остальных тестов):
6. ⏳ **Этап 5** - Рефакторинг остальных тестов (commands, alerts, snapshot, infrastructure, chaos)

### Низкий приоритет (документация):
7. ⏳ **Этап 6** - Обновление документации

## Чеклист для каждого теста

При рефакторинге каждого теста проверить:

- [ ] Используются ли legacy endpoints (`/api/zones/{id}/attach-recipe`, `/api/zones/{id}/start`, etc.)?
- [ ] Используются ли legacy таблицы (`zone_recipe_instances`, `recipe_phases`, `zone_cycles`, `plant_cycles`)?
- [ ] Правильно ли создаются рецепты (через revisions)?
- [ ] Правильно ли создаются циклы (через `/api/zones/{zone}/grow-cycles`)?
- [ ] Правильно ли проверяются данные (через `grow_cycles` вместо `zone_recipe_instances`)?
- [ ] Используются ли правильные поля для проверки (`current_phase_id` вместо `current_phase_index`)?
- [ ] Правильно ли работают проверки снапшотов (`grow_cycle_phases` вместо ссылок на шаблоны)?

## Метрики успеха

Рефакторинг считается завершенным, когда:

1. ✅ Все тесты проходят успешно (`./tools/testing/run_e2e.sh test` возвращает 0)
2. ✅ Нет обращений к legacy endpoints в логах тестов
3. ✅ Нет обращений к legacy таблицам в SQL запросах тестов
4. ✅ Все grow_cycle тесты используют новую модель
5. ✅ Документация обновлена

## Известные проблемы

1. **E50_create_cycle_planned**: Требует POST `/api/plants` endpoint - **РЕШЕНО** (добавлен в Laravel)
2. **telemetry_last структура**: История-logger использует новую структуру, но E2E БД была со старой - **РЕШЕНО** (пересоздана таблица)
3. **ON CONFLICT в telemetry_last**: Использовался неправильный primary key - **РЕШЕНО** (исправлен в `db.py` и `main.py`)

## Следующие шаги

1. Завершить исправление E50_create_cycle_planned (добавить создание revision и phases)
2. Обновить остальные grow_cycle тесты (E51-E54)
3. Проверить и обновить automation_engine тесты
4. Прогнать все тесты и исправить оставшиеся проблемы

