# E93: Scheduler -> start-cycle -> intent -> AE task executor
# DoD:
# - scheduler-подобный intent вставлен в zone_automation_intents
# - /zones/{id}/start-cycle успешно принят automation-engine
# - intent перешел в terminal
# - создан AE task с числовым task_id
# - internal task endpoint отдает terminal snapshot

name: E93_start_cycle_intent_executor_path
description: |
  Проверяет полный runtime путь запуска task из scheduler ingress:
  1) симулируем шаг scheduler (insert pending intent),
  2) вызываем AE endpoint /zones/{id}/start-cycle с scheduler security headers,
  3) проверяем lifecycle intent и AE task,
  4) убеждаемся, что task дошел до terminal status,
  5) проверяем детализацию через /internal/tasks/{task_id}.

actions:
  - step: set_test_context
    type: set
    test_zone_uid: ${TEST_NODE_ZONE_UID}
    idempotency_key: e93-start-cycle-${TIMESTAMP_MS}
    scheduler_source: laravel_scheduler
    scheduler_trace_id: e93-trace-${TIMESTAMP_MS}

  - step: get_zone_row
    type: database_query
    query: |
      SELECT id
      FROM zones
      WHERE uid = :zone_uid
      LIMIT 1
    params:
      zone_uid: ${test_zone_uid}
    save: zone_row
    expected_rows: 1

  - step: set_zone_id
    type: set
    zone_id: ${zone_row.0.id}

  - step: insert_scheduler_like_intent
    type: database_execute
    query: |
      INSERT INTO zone_automation_intents (
        zone_id,
        intent_type,
        payload,
        idempotency_key,
        status,
        not_before,
        retry_count,
        max_retries,
        created_at,
        updated_at
      )
      VALUES (
        :zone_id,
        'IRRIGATE_ONCE',
        jsonb_build_object(
          'source', CAST(:source AS text),
          'task_type', 'diagnostics',
          'workflow', 'cycle_start',
          'topology', 'two_tank_drip_substrate_trays'
        ),
        :idempotency_key,
        'pending',
        NOW() - INTERVAL '1 second',
        0,
        3,
        NOW(),
        NOW()
      )
    params:
      zone_id: ${zone_id}
      idempotency_key: ${idempotency_key}
      source: ${scheduler_source}

  - step: start_cycle_http
    type: http_request
    method: POST
    url: http://${AUTOMATION_ENGINE_HOST}:${AUTOMATION_ENGINE_API_PORT}/zones/${zone_id}/start-cycle
    headers:
      Accept: application/json
      Content-Type: application/json
      X-Trace-Id: ${scheduler_trace_id}
      X-Scheduler-Id: laravel-scheduler
      Authorization: Bearer dev-token-12345
    body:
      source: ${scheduler_source}
      idempotency_key: ${idempotency_key}
    retries: 2
    retry_delay_seconds: 1
    capture_response: start_cycle_response

  - step: wait_intent_terminal
    type: db.wait
    query: |
      SELECT
        id,
        status,
        retry_count,
        error_code,
        completed_at
      FROM zone_automation_intents
      WHERE zone_id = :zone_id
        AND idempotency_key = :idempotency_key
        AND status IN ('completed', 'failed', 'cancelled')
      ORDER BY id DESC
      LIMIT 1
    params:
      zone_id: ${zone_id}
      idempotency_key: ${idempotency_key}
    timeout: 120.0
    expected_rows: 1
    save: intent_terminal

  - step: set_task_id
    type: set
    task_id: ${start_cycle_response.json.data.task_id}

  - step: wait_ae_task_terminal
    type: db.wait
    query: |
      SELECT
        id,
        status,
        task_type,
        error_code,
        error_message
      FROM ae_tasks
      WHERE id = CAST(:task_id AS bigint)
        AND zone_id = :zone_id
        AND status IN ('completed', 'failed', 'cancelled')
      LIMIT 1
    params:
      zone_id: ${zone_id}
      task_id: ${task_id}
    timeout: 120.0
    expected_rows: 1
    save: ae_task_terminal

  - step: get_internal_task_detail
    type: http_request
    method: GET
    url: http://${AUTOMATION_ENGINE_HOST}:${AUTOMATION_ENGINE_API_PORT}/internal/tasks/${task_id}
    headers:
      Accept: application/json
      X-Trace-Id: ${scheduler_trace_id}
      X-Scheduler-Id: laravel-scheduler
      Authorization: Bearer dev-token-12345
    capture_response: internal_task_detail

assertions:
  - name: start_cycle_http_200
    type: custom_assert
    condition: "int(context.get('start_cycle_response', {}).get('status_code', 0)) == 200"

  - name: start_cycle_response_status_ok
    type: json_assertion
    source: start_cycle_response
    path: json
    expected:
      - field: status
        operator: equals
        value: ok
      - field: data.accepted
        operator: equals
        value: true
      - field: data.runner_state
        operator: equals
        value: active

  - name: start_cycle_returns_numeric_task_id
    type: custom_assert
    condition: "str(context.get('task_id', '')).isdigit()"

  - name: intent_terminal_status_valid
    type: custom_assert
    condition: "str(context.get('intent_terminal', [{}])[0].get('status', '')).lower() in {'completed', 'failed', 'cancelled'}"

  - name: ae_task_terminal_status_valid
    type: custom_assert
    condition: "str(context.get('ae_task_terminal', [{}])[0].get('status', '')).lower() in {'completed', 'failed', 'cancelled'}"

  - name: internal_task_detail_terminal
    type: json_assertion
    source: internal_task_detail
    path: json
    expected:
      - field: status
        operator: equals
        value: ok
      - field: data.status
        operator: in
        value: [completed, failed, cancelled]

  - name: internal_task_id_matches_start_cycle_task_id
    type: custom_assert
    condition: "str(context.get('internal_task_detail', {}).get('json', {}).get('data', {}).get('task_id', '')) == str(context.get('task_id', ''))"

  - name: intent_not_left_in_non_terminal
    type: database_query
    query: |
      SELECT COUNT(*)::int AS pending_cnt
      FROM zone_automation_intents
      WHERE zone_id = :zone_id
        AND idempotency_key = :idempotency_key
        AND status IN ('pending', 'claimed', 'running')
    params:
      zone_id: ${zone_id}
      idempotency_key: ${idempotency_key}
    expected_rows: 1
    expected:
      - field: pending_cnt
        operator: equals
        value: 0

cleanup:
  - step: delete_test_intent
    type: database_execute
    query: |
      DELETE FROM zone_automation_intents
      WHERE zone_id = :zone_id
        AND idempotency_key = :idempotency_key
    params:
      zone_id: ${zone_id}
      idempotency_key: ${idempotency_key}
    optional: true

  - step: delete_test_ae_commands
    type: database_execute
    query: |
      DELETE FROM ae_commands
      WHERE task_id = CAST(:task_id AS bigint)
    params:
      task_id: ${task_id}
    optional: true
    condition: ${task_id is not None and task_id != ''}

  - step: delete_test_ae_task
    type: database_execute
    query: |
      DELETE FROM ae_tasks
      WHERE id = CAST(:task_id AS bigint)
    params:
      task_id: ${task_id}
    optional: true
    condition: ${task_id is not None and task_id != ''}
