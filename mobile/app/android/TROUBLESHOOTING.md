# Решение проблем со сборкой

## Кнопки Build не активны

Если кнопки Build неактивны в Android Studio, выполните следующие шаги:

### 1. Синхронизация Gradle

**Способ 1: Через меню**
- **File → Sync Project with Gradle Files**
- Дождитесь завершения (может занять несколько минут)

**Способ 2: Через уведомление**
- В правом верхнем углу может быть уведомление "Gradle files have changed"
- Нажмите **Sync Now**

**Способ 3: Через панель Gradle**
- Откройте **View → Tool Windows → Gradle**
- Нажмите кнопку обновления (🔄) в панели Gradle

### 2. Проверка статуса синхронизации

В правом нижнем углу Android Studio должна быть иконка синхронизации:
- ✅ Зеленая галочка — синхронизация успешна
- ⏳ Крутящийся индикатор — синхронизация в процессе
- ❌ Красный крестик — ошибка синхронизации

### 3. Выбор правильного модуля

Убедитесь, что выбран правильный модуль:
- В левой панели **Project** выберите модуль **app**
- Или в верхней панели выберите **app** из выпадающего списка модулей

### 4. Выбор Build Variant

1. Откройте **Build → Select Build Variant**
2. Выберите нужный вариант:
   - `devDebug`
   - `stagingDebug`
   - `prodDebug`
   - `prodRelease`

### 5. Проверка ошибок

Если синхронизация завершилась с ошибками:

1. Откройте **View → Tool Windows → Build**
2. Проверьте ошибки в логах
3. Частые проблемы:
   - **SDK not found** → **File → Project Structure → SDK Location**
   - **JDK not found** → **File → Project Structure → Project → SDK**
   - **Dependencies failed** → проверьте интернет-соединение

### 6. Очистка и пересборка

Если ничего не помогает:

1. **Build → Clean Project**
2. **File → Invalidate Caches / Restart**
3. Выберите **Invalidate and Restart**
4. После перезапуска: **File → Sync Project with Gradle Files**

### 7. Проверка Gradle Wrapper

Если Gradle wrapper отсутствует, создайте его:

```bash
cd mobile/app/android
gradle wrapper --gradle-version 8.5
```

Или через Android Studio:
- **File → Settings → Build, Execution, Deployment → Build Tools → Gradle**
- Выберите **Gradle wrapper** в разделе **Gradle JDK**

### 8. Проверка local.properties

Убедитесь, что файл `local.properties` существует и содержит путь к SDK:

```properties
sdk.dir=/path/to/Android/Sdk
```

Если файла нет, Android Studio создаст его автоматически при первом открытии проекта.

### 9. Альтернативный способ сборки

Если кнопки не работают, используйте терминал:

```bash
cd mobile/app/android
./gradlew assembleDevDebug
```

Или через панель Gradle:
- **View → Tool Windows → Gradle**
- Разверните **Hydro → Tasks → build**
- Дважды кликните на нужную задачу (например, `assembleDevDebug`)

## Частые ошибки

### "SDK location not found"
1. **File → Project Structure → SDK Location**
2. Укажите путь к Android SDK (обычно `~/Android/Sdk`)

### "Gradle sync failed"
1. Проверьте подключение к интернету
2. **File → Invalidate Caches / Restart**
3. Проверьте версию JDK (должна быть 17+)

### "BuildConfig not found"
Убедитесь, что в `app/build.gradle.kts` есть:
```kotlin
buildFeatures {
    compose = true
    buildConfig = true
}
```

### "Config file not found"
Убедитесь, что конфиги находятся в:
```
app/src/main/assets/configs/
├── env.dev.json
├── env.staging.json
└── env.prod.json
```

## Проверка готовности к сборке

Перед сборкой убедитесь:
- ✅ Gradle sync завершен успешно
- ✅ Выбран модуль `app`
- ✅ Выбран Build Variant (например, `devDebug`)
- ✅ Нет ошибок в панели Build
- ✅ SDK и JDK настроены правильно

После выполнения этих шагов кнопки Build должны стать активными.

