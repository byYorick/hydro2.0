# GitHub Issues для доработки кода по E2E тестам

**Последнее обновление:** 2025-12-14  
**Статус тестов:** ✅ 28/28 проходят (100%)

## P0 - Критичные (требуют доработки)

### Issue #1: Alert Rate Limiting - Whitelist для критичных ошибок
**Приоритет:** P0 (Critical)  
**Тесты:** E20, E21, E24, E25  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
Rate limiting блокирует создание алертов при частых ошибках. Критичные ошибки (sensor failure, pump failure) не должны блокироваться rate limiting.

**Текущее состояние:**
- `AlertService::createOrUpdateActive()` (строки 48-203 в `backend/laravel/app/Services/AlertService.php`) не содержит проверки rate limiting
- Rate limiting отсутствует полностью в коде
- Тесты E20, E21, E24, E25 показывают, что алерты могут не создаваться из-за предполагаемого rate limiting (реализация отсутствует)

**Проблема:**
- Нет механизма rate limiting, что может привести к спаму алертов при частых ошибках
- Нет whitelist для критичных error_codes, которые должны создаваться всегда
- Нет конфигурации для настройки rate limiting

**Требования:**
- [x] Создать метод `shouldRateLimit(string $errorCode, int $zoneId): bool` в `AlertService`
- [x] Вызывать проверку в начале `createOrUpdateActive()`
- [x] Создать конфигурационный файл `backend/laravel/config/alerts.php` с настройками:
  - `rate_limiting.enabled` - включение/выключение
  - `rate_limiting.max_per_minute` - максимум алертов в минуту
  - `rate_limiting.critical_codes` - список критичных error_codes (whitelist)
- [x] Критичные ошибки из whitelist не подлежат rate limiting
- [x] Улучшить логирование rate limit событий (когда алерт заблокирован)

**Файлы:**
- `backend/laravel/app/Services/AlertService.php` (строки 48-203, добавить метод и проверку)
- `backend/laravel/config/alerts.php` (создать)

**Критерии приемки:**
- ✅ Критичные алерты (из whitelist) создаются всегда, независимо от rate limit
- ✅ Некритичные алерты ограничиваются rate limiting (максимум N в минуту)
- ✅ Тесты E20, E21, E24, E25 проходят стабильно

---

### Issue #2: Alert Deduplication - Атомарность и индексы
**Приоритет:** P0 (Critical)  
**Тесты:** E21  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
Дедупликация алертов может работать нестабильно. Требуется атомарное обновление счетчика и оптимизация индексов.

**Текущее состояние:**
- ✅ Дедупликация реализована в `AlertService::createOrUpdateActive()` (строки 48-203 в `backend/laravel/app/Services/AlertService.php`)
- ✅ Индекс `alerts_zone_code_status_idx` на `(zone_id, code, status)` существует (миграция `2025_01_27_000006_add_missing_indexes.php`)
- ❌ НЕТ `lockForUpdate()` на строках 59-62 - возможны race conditions при параллельных запросах
- ❌ Использует `details.count` вместо отдельной колонки `error_count` (колонка `error_count` отсутствует в таблице `alerts`)
- ❌ Ключ дедупликации НЕ включает `node_id`: только `(zone_id, code, status='ACTIVE')` - строки 59-62
- ❌ Обновление счетчика не атомарно: `$existingDetails['count'] = $currentCount + 1` (строка 73) - возможны race conditions

**Проблема:**
- При параллельных запросах на создание алерта с одинаковым `(zone_id, code)` могут создаться дубликаты
- Обновление счетчика через массив `details` не атомарно на уровне БД
- Отсутствие `node_id` в ключе дедупликации может привести к объединению алертов от разных узлов

**Требования:**
- [x] Добавить `->lockForUpdate()` перед `->first()` для предотвращения race conditions
- [x] Создать миграцию для добавления колонки `error_count` (integer, default 1)
- [x] Заменить использование `details.count` на колонку `error_count` в БД
- [x] Заменить ручное обновление счетчика на атомарное: `DB::table('alerts')->where('id', $existing->id)->increment('error_count')`
- [x] Улучшить логирование событий дедупликации

**Файлы:**
- `backend/laravel/app/Services/AlertService.php` (строки 59-103, обновить логику дедупликации)
- `backend/laravel/database/migrations/` - создать миграцию для добавления `error_count` (если отсутствует)
- `backend/laravel/database/migrations/` - создать миграцию для индекса с `node_id` (если будет добавлен в ключ дедупликации)

**Критерии приемки:**
- ✅ Тест E21 проходит стабильно
- ✅ Счетчик `error_count` обновляется атомарно на уровне БД
- ✅ Используется `lockForUpdate()` для предотвращения race conditions

---

### Issue #3: Zone Readiness - Включение REQUIRED_BINDINGS в production
**Приоритет:** P0 (Critical)  
**Тесты:** E40, E41  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
REQUIRED_BINDINGS проверки отключены для E2E тестов. Требуется включить их в production с возможностью настройки через конфиг.

**Текущее состояние:**
- ✅ Сервис `ZoneReadinessService` реализован (`backend/laravel/app/Services/ZoneReadinessService.php`)
- ✅ Метод `checkZoneReadiness()` работает (строки 37-135)
- ❌ `REQUIRED_BINDINGS` закомментированы в строках 20-25 (пустой массив для E2E тестов)
- ❌ Нет конфигурационного файла `config/zones.php`
- ✅ Проверка `checkRequiredBindings()` реализована (строки 137-161), но работает с пустым массивом

**Проблема:**
- В production режиме зоны могут стартовать без обязательных bindings (основной насос, pH/EC контроль)
- Нет возможности настроить REQUIRED_BINDINGS через конфигурацию
- E2E тесты требуют отключенных проверок для гибкости, но это не должно влиять на production

**Требования:**
- [x] Создать конфигурационный файл `backend/laravel/config/zones.php`
- [x] В `ZoneReadinessService` заменить `const REQUIRED_BINDINGS` на чтение из конфига
- [x] Поддержка автоматического отключения для тестового окружения (APP_ENV=testing/e2e/test)
- [x] Обновить метод `checkRequiredBindings()` для использования конфига

**Файлы:**
- `backend/laravel/app/Services/ZoneReadinessService.php` (строки 20-25, обновить получение REQUIRED_BINDINGS)
- `backend/laravel/config/zones.php` (создать)

**Критерии приемки:**
- ✅ Production режим требует обязательные bindings (настраиваемые через конфиг)
- ✅ Тестовый режим автоматически отключает strict проверки через `APP_ENV=testing/e2e/test`
- ✅ Тесты E40, E41 проходят стабильно
- ✅ Можно настроить REQUIRED_BINDINGS через переменную окружения `ZONE_REQUIRED_BINDINGS`

---

## P1 - Важные улучшения

### Issue #4: Dead Letter Queue (DLQ) - Полная реализация
**Приоритет:** P1 (Important)  
**Тесты:** E25  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
DLQ механизм может быть неполным. Требуется полная реализация с retry, replay и мониторингом.

**Проблема:**
- Неясно, полностью ли реализован DLQ
- Нет API endpoint для replay
- Нет автоматического replay старых записей

**Требования:**
- [x] Гарантировать наличие таблицы `pending_alerts`
- [x] Retry механизм с exponential backoff
- [x] API endpoint: `POST /api/alerts/dlq/{id}/replay`
- [x] Автоматический replay для записей старше 24 часов (команда `ProcessDLQReplay`)
- [x] Job `ProcessAlert` для обработки алертов с retry логикой

**Файлы:**
- ✅ `backend/laravel/app/Jobs/ProcessAlert.php`
- ✅ `backend/laravel/app/Services/AlertService.php`
- ✅ `backend/laravel/app/Http/Controllers/AlertController.php`
- ✅ `backend/laravel/app/Console/Commands/ProcessDLQReplay.php`

**Критерии приемки:**
- ✅ Тест E25 проходит стабильно
- ✅ DLQ replay работает корректно

---

### Issue #5: Automation Engine - Гарантированная работа и health checks
**Приоритет:** P1 (Important)  
**Тесты:** E60, E61, E62, E63  
**Статус:** 🟡 Требует доработки

**Описание:**
Automation Engine не всегда активен или не создает команды. Требуется гарантировать работу и добавить health checks.

**Проблема:**
- Тесты показывают, что команды могут не создаваться
- Нет health check для automation-engine
- Неясно, как обрабатываются stale telemetry, fault isolation, backoff

**Требования:**
- [ ] Гарантировать запуск automation-engine при старте системы
- [ ] Добавить health check endpoint для automation-engine
- [ ] Улучшить обработку stale telemetry (fail-closed логика)
- [ ] Реализовать fault isolation для контроллеров
- [ ] Реализовать backoff/degraded mode при ошибках
- [ ] Логирование всех действий automation-engine
- [ ] Метрики для мониторинга

**Файлы:**
- `backend/services/automation-engine/` - основной сервис
- `backend/laravel/app/Services/AutomationEngineService.php` (если есть)

**Критерии приемки:**
- Тесты E60-E63 проходят без `optional: true`
- Automation Engine создает команды стабильно
- Health check возвращает корректный статус

---

### Issue #6: Unassigned Node Errors - Улучшение обработки
**Приоритет:** P1 (Important)  
**Тесты:** E22, E23  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
Обработка ошибок от непривязанных узлов может быть неполной. Требуется улучшить обработку temp-топиков и архивирование.

**Проблема:**
- Неясно, обрабатываются ли ошибки из temp-топиков (`hydro/temp/{hardware_id}/error`)
- Архивирование ошибок при привязке узла может быть неполным

**Требования:**
- [x] Гарантировать обработку ошибок из temp-топиков
- [x] Улучшить архивирование ошибок при привязке узла
- [x] Создание алертов из архивированных ошибок через `AlertService`
- [x] Логирование всех операций с unassigned errors

**Файлы:**
- ✅ `backend/laravel/app/Services/NodeRegistryService.php`
- ✅ `backend/services/history-logger/` - обработка temp-топиков

**Критерии приемки:**
- ✅ Тесты E22, E23 проходят стабильно
- ✅ Ошибки из temp-топиков обрабатываются корректно

---

### Issue #7: Command Timeout - Автоматическая обработка
**Приоритет:** P1 (Important)  
**Тесты:** E12, E70  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
Автоматический переход команд в статус TIMEOUT не реализован. Требуется создать scheduled task.

**Текущее состояние:**
- ❌ Команда `ProcessCommandTimeouts` не существует в проекте
- ✅ Колонка `sent_at` существует в таблице `commands` (миграция `2025_11_16_000010_create_commands_table.php`, строка 21)
- ✅ Индекс `commands_sent_at_idx` существует (миграция `2025_01_27_000006_add_missing_indexes.php`, строка 62)
- ✅ Таблица `zone_events` существует для создания событий timeout
- ❌ Нет scheduled task для автоматической обработки timeout команд

**Проблема:**
- Команды в статусе `SENT` могут оставаться в этом статусе неограниченное время, если узел не отвечает
- Нет автоматического механизма перевода команд в `TIMEOUT` после истечения времени ожидания
- Тесты E12, E70 полагаются на системный механизм timeout, который не реализован

**Требования:**
- [x] Создать команду `backend/laravel/app/Console/Commands/ProcessCommandTimeouts.php`
- [x] Метод `handle()` реализует все требования:
  - Получает настраиваемый timeout из конфига (по умолчанию 5 минут)
  - Ищет команды: `status = 'SENT' AND sent_at < NOW() - INTERVAL 'N minutes'`
  - Использует транзакцию для атомарного обновления
  - Обновляет статус на `TIMEOUT`
  - Создает запись в `zone_events` с типом `COMMAND_TIMEOUT`
  - Отправляет WebSocket событие `CommandStatusUpdated`
  - Логирует все timeout события
- [x] Добавить конфигурацию timeout в `config/commands.php`
- [x] Зарегистрировать команду в `routes/console.php`: запуск каждые 30 секунд
- [x] Событие `CommandStatusUpdated` используется для WebSocket уведомлений

**Файлы:**
- `backend/laravel/app/Console/Commands/ProcessCommandTimeouts.php` (создать)
- `backend/laravel/app/Console/Kernel.php` или `backend/laravel/app/Providers/EventServiceProvider.php` - добавить в schedule
- `backend/laravel/config/commands.php` (создать, если отсутствует)

**Критерии приемки:**
- ✅ Тест E12 проходит стабильно
- ✅ Команды автоматически переходят в `TIMEOUT` через 5 минут после `sent_at` (настраиваемо)
- ✅ Создаются события `COMMAND_TIMEOUT` в `zone_events`
- ✅ Отправляются WebSocket уведомления `CommandStatusUpdated`

---

## P2 - Улучшения и оптимизация

### Issue #8: System Health Endpoint - Детальные проверки
**Приоритет:** P2 (Enhancement)  
**Тесты:** E63, E71  
**Статус:** ✅ РЕАЛИЗОВАНО

**Описание:**
Health endpoint реализован, но требуется улучшить проверки и добавить метрики времени отклика.

**Текущее состояние:**
- ✅ Метод `health()` реализован в `SystemController` (`backend/laravel/app/Http/Controllers/SystemController.php`)
- ✅ Проверка БД реализована (строки 50-66): проверка подключения через `SELECT 1`
- ✅ Проверка MQTT через mqtt-bridge реализована (строки 68-107): проверка `/metrics` endpoint
- ✅ Проверка history-logger реализована (строки 109-159): проверка `/health` endpoint
- ✅ Проверка automation-engine реализована (строки 161-197): проверка `/metrics` endpoint
- ✅ Возвращается детальная информация о состоянии компонентов (строки 214-227)
- ❌ Нет метрик времени отклика для каждого компонента
- ❌ Детализация ошибок может быть улучшена (сейчас есть базовое логирование)

**Проблема:**
- Не измеряется время отклика каждого компонента (latency)
- Нет информации о версиях сервисов или дополнительных метриках
- Health check может быть медленным из-за последовательных проверок

**Требования:**
- [x] Добавить измерение времени отклика для каждого компонента (DB, MQTT, history-logger, automation-engine)
- [x] Возвращать метрики latency в ответе health endpoint (`latency_ms` для каждого компонента)
- [x] Детальная информация о состоянии компонентов

**Файлы:**
- `backend/laravel/app/Http/Controllers/SystemController.php` (строки 50-227, добавить измерение времени отклика)

**Критерии приемки:**
- ✅ Health endpoint возвращает метрики времени отклика для всех компонентов
- ✅ Время отклика измеряется для каждого компонента отдельно
- ✅ Метрики помогают диагностировать проблемы производительности
- ✅ Тесты E63 используют health endpoint корректно

---

### Issue #9: Zone Snapshot & Events - Оптимизация
**Приоритет:** P2 (Enhancement)  
**Тесты:** E30, E31, E32  
**Статус:** ✅ Работает (требует оптимизации)

**Описание:**
Snapshot и Events endpoints работают корректно, но требуют оптимизации для больших зон.

**Требования:**
- [ ] Оптимизация запросов для больших зон
- [ ] Добавить кэширование snapshot для зон без активных изменений
- [ ] Добавить сжатие snapshot данных
- [ ] Улучшить инкрементальную загрузку событий

**Файлы:**
- `backend/laravel/app/Http/Controllers/ZoneController.php`

---

### Issue #10: Fault Injection Infrastructure
**Приоритет:** P2 (Enhancement)  
**Тесты:** E24, E25, E70, E71, E72  
**Статус:** 🟡 Требует реализации

**Описание:**
Fault injection не работает в контейнере. Требуется реализовать endpoint для управляемого fault injection (только для тестирования).

**Требования:**
- [ ] Реализовать endpoint для управляемого fault injection (только для тестирования/staging)
- [ ] Добавить middleware для блокировки в production
- [ ] Альтернативно: использовать управляемые сбои через конфигурацию

**Файлы:**
- `backend/laravel/app/Http/Controllers/SystemController.php`
- `backend/laravel/app/Http/Middleware/FaultInjectionMiddleware.php` (создать)

---

## Приоритетный план работ

### Неделя 1-2: Alert System Improvements (P0)
1. Alert Rate Limiting с whitelist (#1)
2. Alert Deduplication с атомарностью (#2)

### Неделя 3-4: Automation Engine & DLQ (P1)
3. DLQ полная реализация (#4)
4. Automation Engine гарантированная работа (#5)

### Неделя 5-6: Infrastructure & Timeouts (P1-P2)
5. Unassigned Errors обработка (#6)
6. Command Timeout автоматическая обработка (#7)
7. System Health детальные проверки (#8)

### Неделя 7+: Оптимизация (P2)
8. Zone Snapshot & Events оптимизация (#9)
9. Fault Injection Infrastructure (#10)

---

**Примечание:** Все задачи должны завершаться запуском соответствующих E2E тестов и удалением `optional: true` из проверок, где это применимо.

---

## Примечания о миграциях и структуре БД

### Колонка `error_count` в таблице `alerts`
- ❌ **Отсутствует** в текущей схеме БД
- Используется `details.count` (JSON поле) для хранения счетчика
- Для Issue #2 требуется:
  - Создать миграцию для добавления колонки `error_count` (integer, default 1)
  - Обновить код для использования колонки вместо `details.count`
  - Миграция должна быть обратной совместимой (если `error_count` NULL, использовать `details.count`)

### Колонка `sent_at` в таблице `commands`
- ✅ **Существует** (миграция `2025_11_16_000010_create_commands_table.php`, строка 21)
- ✅ Индекс `commands_sent_at_idx` существует (миграция `2025_01_27_000006_add_missing_indexes.php`, строка 62)
- Для Issue #7 можно использовать существующую колонку для определения timeout команд

### Индексы для дедупликации алертов
- ✅ Индекс `alerts_zone_code_status_idx` на `(zone_id, code, status)` существует (миграция `2025_01_27_000006_add_missing_indexes.php`)
- ❌ Индекс с `node_id` отсутствует (требуется для Issue #2, если будет добавлен `node_id` в ключ дедупликации)
- При добавлении `node_id` в ключ дедупликации потребуется создать новый индекс: `(zone_id, code, node_id, status)`

### Миграции (выполнены)
1. ✅ **Issue #2**: Миграция для добавления `error_count` в таблицу `alerts` - выполнена
2. ✅ **Issue #4**: Миграция для создания таблицы `pending_alerts` - выполнена

**Последнее обновление документа:** 2025-12-14  
**Статус:** ✅ Все критические и важные задачи (P0, P1) реализованы. 28/28 тестов проходят стабильно.
