# Аудит Backend Core (04_BACKEND_CORE)
## Дата: 2025-01-XX

## Обзор

Проведен аудит документации и реализации backend-ядра системы hydro2.0. Проверены:
- Соответствие документации реальной структуре проекта
- Соответствие API спецификации реализованным эндпоинтам
- Соответствие Docker-конфигураций документации
- Соответствие Python-сервисов архитектурной документации
- Выявлены несоответствия и проблемы

---

## 1. Соответствие документации и реализации

### ✅ Положительные моменты

1. **Структура проекта соответствует документации**
   - Laravel приложение находится в `backend/laravel/`
   - Python сервисы в `backend/services/`
   - Общая библиотека `common/` реализована
   - Docker-compose файлы присутствуют

2. **Python сервисы соответствуют архитектуре**
   - `mqtt-bridge` - реализован
   - `history-logger` - реализован
   - `automation-engine` - реализован
   - `scheduler` - реализован
   - `digital-twin` - реализован (дополнительно)
   - `device-registry` - помечен как PLANNED (соответствует документации)

3. **Технологический стек соответствует**
   - Laravel 11 (соответствует `^11.0` в composer.json)
   - PHP 8.2+ (соответствует `^8.2`)
   - Vue 3 + Inertia (соответствует package.json)
   - PostgreSQL (TimescaleDB в docker-compose)
   - Laravel Reverb для WebSocket

---

## 2. Критические несоответствия

### 🟡 СРЕДНЕ: Greenhouse API требует аутентификации

**Проблема:**
- В `API_SPEC_FRONTEND_BACKEND_FULL.md` (строки 77-90) описаны эндпоинты:
  - `GET /api/greenhouses`
  - `POST /api/greenhouses`
  - `GET /api/greenhouses/{id}`
- В `REST_API_REFERENCE.md` (строки 22-30) также описаны эти эндпоинты
- В реальном `routes/api.php` (строка 33) маршруты **есть**, но они находятся внутри `middleware('auth:sanctum')`
- В документации не указано, что эти эндпоинты требуют аутентификации

**Текущее состояние:**
- `GreenhouseController` существует в коде ✅
- Маршруты зарегистрированы в `api.php` ✅
- Но требуют аутентификации (что может быть правильно, но не документировано)

**Рекомендация:**
- Обновить документацию, указав, что эндпоинты Greenhouses требуют аутентификации
- Или, если это публичные эндпоинты, вынести их из middleware группы

---

### 🟡 СРЕДНЕ: Несоответствие в переменных окружения

**Проблема:**
- В `PYTHON_SERVICES_ARCH.md` (строка 345) указано: `LARAVEL_API_TOKEN`
- В `common/env.py` (строка 23) используется: `LARAVEL_API_TOKEN`
- В `docker-compose.dev.yml` (строка 121) используется: `LARAVEL_API_TOKEN` (пустое значение)
- Но в `FULL_STACK_DEPLOY_DOCKER.md` (строка 238) упоминается только `LARAVEL_API_TOKEN` без примеров

**Рекомендация:**
- Добавить в документацию примеры настройки токена
- Убедиться, что токен генерируется и передается правильно

---

### 🟡 СРЕДНЕ: Несоответствие в именах сервисов Docker

**Проблема:**
- В `FULL_STACK_DEPLOY_DOCKER.md` (строки 84-140) используются имена:
  - `postgres`, `mosquitto`, `laravel`, `python_service`
- В реальном `docker-compose.dev.yml` используются:
  - `db`, `mqtt`, `laravel`, `mqtt-bridge`, `history-logger`, `automation-engine`, `scheduler`

**Рекомендация:**
- Обновить документацию `FULL_STACK_DEPLOY_DOCKER.md` с актуальными именами сервисов
- Или привести docker-compose в соответствие с документацией (но это может сломать существующие интеграции)

---

### 🟡 СРЕДНЕ: Отсутствие Laravel в production docker-compose

**Проблема:**
- В `docker-compose.prod.yml` отсутствует сервис `laravel`
- В документации `FULL_STACK_DEPLOY_DOCKER.md` предполагается наличие Laravel сервиса

**Рекомендация:**
- Добавить Laravel сервис в production docker-compose
- Или документировать, что Laravel деплоится отдельно

---

## 3. Частичные несоответствия

### 🟢 НИЗКИЙ ПРИОРИТЕТ: Расширенный функционал API

**Наблюдение:**
- В реальном коде реализованы дополнительные эндпоинты, не описанные в документации:
  - `/api/ai/predict` - AI прогнозирование
  - `/api/ai/explain_zone` - объяснение состояния зоны
  - `/api/ai/recommend` - рекомендации AI
  - `/api/ai/diagnostics` - диагностика AI
  - `/api/simulations/zone/{zone}` - симуляции Digital Twin
  - `/api/presets` - пресеты
  - `/api/recipes/{recipe}/analytics` - аналитика рецептов
  - `/api/zones/{zone}/harvests` - урожаи

**Рекомендация:**
- Обновить `API_SPEC_FRONTEND_BACKEND_FULL.md` и `REST_API_REFERENCE.md` с описанием новых эндпоинтов
- Или явно пометить их как "экспериментальные" / "в разработке"

---

### 🟢 НИЗКИЙ ПРИОРИТЕТ: WebSocket реализация

**Наблюдение:**
- В `REALTIME_UPDATES_ARCH.md` описана архитектура WebSocket
- В `docker-compose.dev.yml` настроен Laravel Reverb (строки 33-64)
- Но в документации `FULL_STACK_DEPLOY_DOCKER.md` не упоминается Reverb

**Рекомендация:**
- Добавить описание Reverb в документацию по деплою
- Убедиться, что конфигурация Reverb документирована

---

## 4. Проблемы в документации

### 🟡 СРЕДНЕ: Устаревшие ссылки на структуру

**Проблема:**
- В `FULL_STACK_DEPLOY_DOCKER.md` (строки 22-38) описана структура:
  ```
  hydro2/
   backend-laravel/
   backend-python/
   frontend-laravel/
  ```
- В реальной структуре:
  ```
  hydro2.0/
   backend/
    laravel/
    services/
  ```

**Рекомендация:**
- Обновить документацию с актуальной структурой проекта

---

### 🟡 СРЕДНЕ: Несоответствие в портах

**Проблема:**
- В `FULL_STACK_DEPLOY_DOCKER.md` (строка 118): Laravel на порту `8000`
- В `docker-compose.dev.yml` (строка 68): Laravel на порту `8080:80`
- В документации также упоминается порт `5173` для Vite (строка 69)

**Рекомендация:**
- Унифицировать документацию с реальными портами
- Или явно указать различия между dev и prod окружениями

---

## 5. Проблемы в коде

### 🟡 СРЕДНЕ: Несоответствие в формате ответов API

**Проблема:**
- В `API_SPEC_FRONTEND_BACKEND_FULL.md` (строки 22-29) описан стандартный формат:
  ```json
  {
    "status": "ok",
    "data": { }
  }
  ```
- В реальных контроллерах используется этот формат ✅
- Но в некоторых местах может быть несоответствие

**Рекомендация:**
- Проверить все контроллеры на соответствие формату
- Добавить middleware для унификации формата ответов

---

### 🟢 НИЗКИЙ ПРИОРИТЕТ: Отсутствие версионирования API

**Наблюдение:**
- В документации (`API_SPEC_FRONTEND_BACKEND_FULL.md`, строка 284) упоминается версионирование `/api/v2/`
- В реальном коде версионирование не используется

**Рекомендация:**
- Решить, нужно ли версионирование на текущем этапе
- Если да - добавить версионирование в маршруты

---

## 6. Рекомендации по улучшению

### Приоритет 1 (Критично)

1. **Документировать требования аутентификации для Greenhouses API**
   - Указать в документации, что эндпоинты требуют `auth:sanctum`
   - Или вынести из middleware, если они должны быть публичными

2. **Обновить docker-compose.prod.yml**
   - Добавить Laravel сервис или документировать отдельный деплой

3. **Синхронизировать имена сервисов**
   - Привести документацию в соответствие с реальными именами в docker-compose

### Приоритет 2 (Важно)

4. **Обновить документацию по структуре проекта**
   - Исправить пути в `FULL_STACK_DEPLOY_DOCKER.md`

5. **Документировать новые API эндпоинты**
   - Добавить AI и Simulation эндпоинты в спецификацию

6. **Документировать Reverb/WebSocket**
   - Добавить описание в документацию по деплою

### Приоритет 3 (Желательно)

7. **Унифицировать порты в документации**
   - Указать различия между dev/prod

8. **Добавить примеры конфигурации токенов**
   - В документацию по переменным окружения

9. **Рассмотреть версионирование API**
   - Если планируется публичный API

---

## 7. Статистика аудита

- **Всего проверено документов:** 7
- **Критических проблем:** 0
- **Средних проблем:** 6
- **Низкоприоритетных:** 4
- **Положительных соответствий:** 3

---

## 8. Заключение

Документация в целом соответствует реализации, но есть несколько важных несоответствий, которые нужно исправить:

1. **Несоответствие имен сервисов Docker** - может привести к путанице при деплое
2. **Отсутствие Laravel в production docker-compose** - нужно уточнить стратегию деплоя
3. **Неполная документация по аутентификации API** - не указано, какие эндпоинты требуют токен

Большинство проблем связаны с устаревшей документацией, которая не была обновлена после изменений в коде. Рекомендуется провести синхронизацию документации с реальной реализацией.

---

## Приложение: Чек-лист для исправлений

- [ ] Документировать требования аутентификации для Greenhouses API
- [ ] Обновить `FULL_STACK_DEPLOY_DOCKER.md` с актуальными именами сервисов
- [ ] Добавить Laravel в `docker-compose.prod.yml` или документировать отдельный деплой
- [ ] Обновить структуру проекта в документации
- [ ] Добавить описание новых API эндпоинтов (AI, Simulations)
- [ ] Документировать Reverb/WebSocket конфигурацию
- [ ] Унифицировать порты в документации
- [ ] Добавить примеры конфигурации токенов

