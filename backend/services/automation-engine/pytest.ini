[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
markers =
    integration: marks tests that exercise cross-component integration paths
    crash_window: AE3 startup recovery crash-window contract (ae3lite.md §9.2)
