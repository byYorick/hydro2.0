# Детальный аудит WebSocket: Найденные баги

**Дата аудита:** 2024  
**Область:** WebSocket (Laravel Reverb + Laravel Echo + Pusher.js)  
**Критичность:** 🔴 Критично | 🟡 Важно | 🟢 Низкая

---

## 🔴 КРИТИЧЕСКИЕ БАГИ

### 1. Race Condition в `resubscribeAllChannels()`

**Файл:** `backend/laravel/resources/js/composables/useWebSocket.ts:342-458`

**Проблема:**
```typescript
// Строка 360: Очистка subscribedChannels перед переподпиской
subscribedChannels.clear()

// Строка 363-453: Переподписка на все каналы
activeSubscriptions.forEach(sub => {
  // ...
  if (subscribedChannels.has(channelName)) { // Проверка после очистки - всегда false!
    return
  }
  // ...
  subscribedChannels.add(channelName)
})
```

**Описание:**
- Функция очищает `subscribedChannels` перед переподпиской
- Но проверка `subscribedChannels.has()` происходит после очистки, что делает её бесполезной
- Если `resubscribeAllChannels()` вызывается несколько раз одновременно (например, при быстром reconnect), создаются дублирующие подписки
- Нет защиты от одновременного выполнения функции

**Последствия:**
- Множественные обработчики на одни и те же события
- Утечки памяти
- Дублирование событий в UI

**Исправление:**
```typescript
// Добавить флаг блокировки
let isResubscribing = false

export function resubscribeAllChannels(): void {
  if (isResubscribing) {
    logger.warn('[WebSocket] Resubscribe already in progress')
    return
  }
  
  isResubscribing = true
  try {
    // ... существующий код ...
  } finally {
    isResubscribing = false
  }
}
```

---

### 2. Утечка памяти: `activeSubscriptions` накапливает устаревшие подписки

**Файл:** `backend/laravel/resources/js/composables/useWebSocket.ts:67, 189-195, 288-294`

**Проблема:**
```typescript
// Глобальный массив, который никогда не очищается полностью
const activeSubscriptions: ActiveSubscription[] = []

// При подписке добавляется в массив
activeSubscriptions.push({
  type: 'zone_commands',
  id: zoneId,
  handler: onCommandUpdate
})

// При отписке удаляется только если вызван unsubscribe
// Но если компонент размонтирован без вызова unsubscribe, подписка остается!
```

**Описание:**
- `activeSubscriptions` - глобальный массив, который накапливает все подписки
- Если компонент размонтирован без явного вызова `unsubscribe()`, подписка остается в массиве
- При reconnect все эти "мертвые" подписки переподписываются
- Обработчики могут ссылаться на уже размонтированные компоненты

**Последствия:**
- Утечка памяти
- Вызовы обработчиков на несуществующие компоненты
- Накопление мусора при длительной работе приложения

**Исправление:**
- Добавить проверку валидности обработчиков перед переподпиской
- Очищать `activeSubscriptions` при размонтировании компонента (через `onUnmounted`)
- Добавить периодическую очистку неактивных подписок

---

### 3. Несоответствие типа канала: `events.global` объявлен как публичный, но используется как приватный

**Файл:** 
- `backend/laravel/routes/channels.php:20-21` (нет авторизации)
- `backend/laravel/resources/js/composables/useWebSocket.ts:243` (используется `channel()`, не `private()`)
- `backend/laravel/app/Events/EventCreated.php:38-40` (используется `Channel`, не `PrivateChannel`)

**Проблема:**
```php
// channels.php - канал объявлен как публичный (без авторизации)
// Канал для глобальных событий (публичный)
// Не требует авторизации, так как это публичный канал
```

```typescript
// useWebSocket.ts - используется публичный канал
const channel = window.Echo.channel(channelName) // ✅ Правильно для публичного канала
```

```php
// EventCreated.php - используется публичный канал
return new Channel('events.global'); // ✅ Правильно
```

**Описание:**
- На самом деле это НЕ баг, но потенциальная проблема безопасности
- Публичный канал `events.global` доступен всем без авторизации
- Любой может подключиться и слушать глобальные события системы

**Последствия:**
- Потенциальная утечка информации через публичный канал
- Нет контроля доступа к глобальным событиям

**Рекомендация:**
- Рассмотреть использование приватного канала с авторизацией
- Или добавить фильтрацию чувствительных данных в событиях

---

## 🟡 ВАЖНЫЕ БАГИ

### 4. Дублирование подписок при повторном вызове `subscribeToZoneCommands` с тем же `zoneId`

**Файл:** `backend/laravel/resources/js/composables/useWebSocket.ts:81-205`

**Проблема:**
```typescript
function subscribeToZoneCommands(zoneId: number, onCommandUpdate?: ...) {
  const channelName = `commands.${zoneId}`
  
  // Если уже подписаны, отписываемся
  if (subscriptions.value.has(channelName)) {
    const existing = subscriptions.value.get(channelName)!
    existing.unsubscribe() // ✅ Отписываемся
  }
  
  // Удаляем старую подписку из активных
  const index = activeSubscriptions.findIndex(...)
  if (index > -1) {
    activeSubscriptions.splice(index, 1) // ✅ Удаляем
  }
  
  // Но проверка subscribedChannels происходит ПОСЛЕ этого
  if (subscribedChannels.has(channelName)) {
    logger.warn(`[WebSocket] Already subscribed to ${channelName}, skipping`)
    // ❌ ПРОБЛЕМА: subscribedChannels НЕ очищается при unsubscribe!
    return existing.unsubscribe // Возвращаем старую функцию отписки
  }
  
  // Создаем новую подписку
  const channel = window.Echo.private(channelName)
  // ...
  subscribedChannels.add(channelName) // Добавляем в Set
}
```

**Описание:**
- При вызове `unsubscribe()` канал удаляется из `subscriptions.value`, но НЕ из `subscribedChannels`
- При повторной подписке проверка `subscribedChannels.has()` возвращает `true`, хотя старая подписка уже удалена
- Функция возвращает старую `unsubscribe`, которая может быть невалидной

**Последствия:**
- Некорректное поведение при повторных подписках
- Потенциальные ошибки при вызове старой функции отписки

**Исправление:**
- Убедиться, что `subscribedChannels.delete()` вызывается в функции `unsubscribe()`
- Или проверять `subscriptions.value.has()` вместо `subscribedChannels.has()`

---

### 5. Нет защиты от одновременных подписок на один канал из разных компонентов

**Файл:** `backend/laravel/resources/js/composables/useWebSocket.ts:109-117, 233-241`

**Проблема:**
```typescript
// Проверка subscribedChannels не является атомарной операцией
if (subscribedChannels.has(channelName)) {
  logger.warn(`[WebSocket] Already subscribed to ${channelName}, skipping`)
  const existing = subscriptions.value.get(channelName)
  if (existing) {
    return existing.unsubscribe // ❌ Может быть undefined, если другой компонент уже отписался
  }
}

// Между проверкой и созданием подписки может произойти другая подписка
const channel = window.Echo.private(channelName) // ❌ Создается дублирующая подписка
```

**Описание:**
- Если два компонента одновременно вызывают `subscribeToZoneCommands(1, ...)`, оба могут пройти проверку `subscribedChannels.has()`
- Оба создадут подписку на один канал
- События будут обрабатываться дважды

**Последствия:**
- Дублирование обработки событий
- Лишние подписки на сервере

**Исправление:**
- Использовать атомарную операцию (например, через Promise/Mutex)
- Или использовать общий канал с одним обработчиком, который распределяет события

---

### 6. Проблема с флагом `handlersSetup` в `bootstrap.js`

**Файл:** `backend/laravel/resources/js/bootstrap.js:91-213`

**Проблема:**
```typescript
let handlersSetup = false; // Флаг на уровне модуля

const setupWebSocketHandlers = () => {
  // ...
  if (handlersSetup) {
    return true; // ✅ Предотвращает дублирование
  }
  
  // Удаляем старые обработчики
  try {
    pusher.connection.unbind('connected');
    // ...
  } catch (e) {
    // Игнорируем ошибки
  }
  
  // Устанавливаем новые обработчики
  // ...
  
  handlersSetup = true; // ✅ Устанавливаем флаг
  return true;
};
```

**Описание:**
- Флаг `handlersSetup` устанавливается один раз и никогда не сбрасывается
- Если соединение разорвано и переподключено, обработчики не переустанавливаются
- Но при этом старые обработчики пытаются удаляться каждый раз

**Последствия:**
- Потенциальные проблемы при переподключении
- Обработчики могут не работать после reconnect

**Исправление:**
- Сбрасывать `handlersSetup = false` при отключении
- Или проверять наличие обработчиков перед установкой

---

### 7. Неправильная логика проверки состояния WebSocket в `useSystemStatus`

**Файл:** `backend/laravel/resources/js/composables/useSystemStatus.ts:289-301`

**Проблема:**
```typescript
if (state === 'connected') {
  wsStatus.value = 'connected'
} else if (state === 'connecting' || state === 'unavailable') {
  const wasConnected = wsStatus.value === 'connected'
  if (!wasConnected) {
    wsStatus.value = 'disconnected' // ❌ Показываем disconnected при connecting
  }
  // Если было подключение, сохраняем его (grace period)
  // ❌ Но это может скрыть реальные проблемы с подключением
}
```

**Описание:**
- При состоянии `connecting` статус остается `connected`, если ранее было подключение
- Это может скрыть проблемы с подключением
- Grace period не имеет таймаута

**Последствия:**
- Некорректное отображение статуса WebSocket
- Пользователь не видит, что соединение не устанавливается

**Исправление:**
- Добавить таймаут для grace period
- Показывать `connecting` как отдельное состояние
- Сбрасывать grace period после определенного времени

---

## 🟢 НИЗКАЯ КРИТИЧНОСТЬ / УЛУЧШЕНИЯ

### 8. Отсутствие обработки ошибок при создании канала

**Файл:** `backend/laravel/resources/js/composables/useWebSocket.ts:119, 243`

**Проблема:**
```typescript
const channel = window.Echo.private(channelName)
// ❌ Нет проверки, что channel создан успешно
// ❌ Нет обработки ошибок авторизации приватного канала
```

**Исправление:**
- Добавить обработку ошибок авторизации
- Проверять успешность создания канала

---

### 9. Нет логирования неудачных попыток переподключения

**Файл:** `backend/laravel/resources/js/bootstrap.js:141-160, 196-208`

**Проблема:**
- При неудачном переподключении ошибка логируется, но не накапливается статистика
- Нет ограничения на количество попыток переподключения

**Исправление:**
- Добавить счетчик попыток переподключения
- Реализовать экспоненциальный backoff
- Логировать статистику переподключений

---

### 10. Потенциальная проблема с CSRF токеном при авторизации каналов

**Файл:** `backend/laravel/resources/js/bootstrap.js:11-14, 71-77`

**Проблема:**
```typescript
const csrfToken = document.querySelector('meta[name="csrf-token"]')?.getAttribute('content');
// ...
auth: {
  headers: {
    'X-CSRF-TOKEN': csrfToken || '', // ❌ Может быть пустой строкой
  },
}
```

**Описание:**
- Если CSRF токен не найден, отправляется пустая строка
- Это может привести к ошибкам авторизации приватных каналов

**Исправление:**
- Проверять наличие CSRF токена перед инициализацией Echo
- Показывать предупреждение, если токен отсутствует

---

## 📊 СВОДКА

| Категория | Количество |
|-----------|------------|
| 🔴 Критичные | 3 |
| 🟡 Важные | 4 |
| 🟢 Низкая критичность | 3 |
| **Всего** | **10** |

---

## 🎯 РЕКОМЕНДАЦИИ ПО ПРИОРИТЕТАМ

1. **Немедленно исправить:**
   - Race condition в `resubscribeAllChannels()` (#1)
   - Утечка памяти в `activeSubscriptions` (#2)
   - Дублирование подписок (#4, #5)

2. **Исправить в ближайшее время:**
   - Проблема с флагом `handlersSetup` (#6)
   - Логика проверки состояния WebSocket (#7)
   - Несоответствие типа канала `events.global` (#3)

3. **Улучшения:**
   - Обработка ошибок при создании канала (#8)
   - Логирование переподключений (#9)
   - Проверка CSRF токена (#10)

---

## 🔧 ТЕСТИРОВАНИЕ

После исправления багов рекомендуется протестировать:

1. **Переподключение:**
   - Отключить сеть на 10 секунд
   - Проверить, что подписки восстанавливаются без дублирования
   - Проверить отсутствие утечек памяти

2. **Множественные компоненты:**
   - Открыть несколько страниц с одной зоной
   - Проверить, что события обрабатываются один раз
   - Проверить корректную отписку при закрытии страниц

3. **Долгая работа:**
   - Оставить приложение открытым на несколько часов
   - Проверить отсутствие накопления подписок в `activeSubscriptions`
   - Проверить стабильность соединения

---

**Автор аудита:** AI Assistant  
**Дата:** 2024

