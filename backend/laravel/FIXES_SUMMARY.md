# Сводка исправлений

## ✅ Исправление 1: WebSocket путь

**Проблема**: WebSocket подключался к `ws://localhost:6001/app/app/local` (дублирование пути) или напрямую к порту 6001.

**Исправление**:
- `wsPath` установлен как `/app` (вместо пустой строки)
- Добавлена логика переопределения порта 6001 на порт nginx (8080) в dev режиме
- Pusher теперь должен подключаться к `ws://localhost:8080/app/local` через nginx прокси

**Файлы**:
- `resources/js/utils/echoClient.ts` - исправлен `resolvePath()` и `resolvePort()`

## ✅ Исправление 2: Множественные 401 ошибки

**Проблема**: Компонент `HeaderStatusBar` делал множественные запросы к `/api/alerts` даже при 401 ошибках.

**Исправление**:
- Улучшена проверка аутентификации перед запуском интервала
- Интервал останавливается при обнаружении 401 ошибки
- Логирование для отладки

**Файлы**:
- `resources/js/Components/HeaderStatusBar.vue` - улучшена логика в `onMounted()`

## ✅ Исправление 3: Singleton для useSystemStatus

**Проблема**: Несколько компонентов создавали множественные интервалы для проверки здоровья.

**Исправление**:
- Реализован singleton паттерн
- Все компоненты используют общий экземпляр
- Интервалы запускаются только один раз

**Файлы**:
- `resources/js/composables/useSystemStatus.ts` - добавлен `sharedState`

## ⚠️ Проблема 4: 502 Bad Gateway

**Проблема**: 502 ошибка на `/users` - проблема с nginx/PHP-FPM.

**Статус**: Требует дополнительной проверки. Возможно:
- PHP-FPM не запущен или недоступен
- Nginx не может подключиться к PHP-FPM
- Проблема с конфигурацией nginx для PHP

**Рекомендации**:
1. Проверить статус PHP-FPM в контейнере
2. Проверить логи nginx и PHP-FPM
3. Проверить конфигурацию nginx для обработки PHP запросов

## 📝 Следующие шаги

1. **Перезапустить контейнер** для применения изменений nginx:
   ```bash
   docker-compose -f docker-compose.dev.yml restart laravel
   ```

2. **Очистить кеш браузера** и обновить страницу (Ctrl+Shift+R)

3. **Проверить WebSocket подключение** в консоли браузера:
   - Должен быть `ws://localhost:8080/app/local`
   - Не должно быть ошибок подключения

4. **Проверить 401 ошибки**:
   - Не должно быть множественных запросов к `/api/alerts`
   - Интервал должен останавливаться при 401

5. **Проверить 502 ошибку**:
   - Проверить логи PHP-FPM
   - Проверить статус PHP-FPM процесса

## 🔧 Измененные файлы

1. `resources/js/utils/echoClient.ts` - исправлен WebSocket путь и порт
2. `resources/js/composables/useSystemStatus.ts` - добавлен singleton
3. `resources/js/Components/HeaderStatusBar.vue` - улучшена обработка 401
4. `nginx-vite-proxy.conf` - уже настроен для проксирования WebSocket
5. Production assets пересобраны (`npm run build`)



