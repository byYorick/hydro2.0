# Чек-лист проверки исправлений

## ✅ Проверка 1: WebSocket путь
- [x] `wsPath` установлен как пустая строка в `echoClient.ts`
- [x] Pusher автоматически создаст путь `/app/{app_key}` (где app_key = 'local')
- [x] Nginx проксирует `/app/` на Reverb, сохраняя исходный путь
- [ ] **ПРОБЛЕМА**: Путь дублируется `/app/app/local` вместо `/app/local`
- **Решение**: Нужно проверить, что Pusher не добавляет `/app/` дважды

## ✅ Проверка 2: Singleton для useSystemStatus
- [x] Реализован singleton паттерн с `sharedState`
- [x] Все компоненты используют один экземпляр
- [x] Счетчик подписчиков отслеживает использование
- [x] Интервалы запускаются только один раз
- [x] Интервалы останавливаются только когда все подписчики отписались
- **Результат**: ✅ Работает корректно

## ✅ Проверка 3: Обработка ошибки 429
- [x] Флаг `isRateLimited` предотвращает повторные запросы
- [x] Интервал останавливается при получении 429
- [x] Toast уведомления не показываются для 429
- **Результат**: ✅ Работает корректно

## ⚠️ Проблема с WebSocket URL
**Текущая ошибка**: `ws://localhost:6001/app/app/local`

**Ожидаемый URL**: `ws://localhost:8080/app/local` (через nginx)

**Возможные причины**:
1. Pusher формирует путь неправильно - добавляет `/app/` к `wsPath`
2. Nginx неправильно проксирует путь
3. Конфигурация Reverb ожидает другой формат пути

**Решение**:
- Для Laravel Reverb, если `REVERB_SERVER_PATH` пустой, клиент должен использовать `wsPath = '/app'`
- Pusher тогда создаст путь `/app/{key}` = `/app/local`
- Nginx проксирует `/app/local` на `http://127.0.0.1:6001/app/local`

## 📝 Компоненты, использующие useSystemStatus
1. `HeaderStatusBar.vue` - использует для отображения статусов
2. `SystemMonitoringModal.vue` - использует для детального мониторинга

Оба компонента теперь используют общий singleton, что предотвращает множественные интервалы.

