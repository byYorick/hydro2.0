# Графики телеметрии на странице устройства

## Что добавлено

Теперь на странице отдельного устройства (`/devices/{id}`) отображаются **графики телеметрии**, привязанные к конкретной ноде.

### Backend изменения

#### 1. Новый API endpoint

**Файл**: `backend/laravel/app/Http/Controllers/TelemetryController.php`

Добавлен метод `nodeHistory()`:

```php
GET /api/nodes/{id}/telemetry/history
```

**Параметры:**
- `metric` (required) - Тип метрики (TEMP_AIR, HUMIDITY, CO2, PH, EC и т.д.)
- `channel` (optional) - Название канала для фильтрации
- `from` (optional) - Начало временного диапазона (ISO 8601)
- `to` (optional) - Конец временного диапазона (ISO 8601)

**Ответ:**
```json
{
  "status": "ok",
  "data": [
    {
      "ts": "2025-12-05T13:47:19.000000Z",
      "value": 21.015869140625,
      "channel": "temperature"
    },
    ...
  ]
}
```

**Особенности:**
- Проверка авторизации пользователя
- Проверка доступа к ноде через `ZoneAccessHelper`
- Лимит 5000 записей
- Данные берутся из таблицы `telemetry` (TimescaleDB)

#### 2. Роут

**Файл**: `backend/laravel/routes/api.php`

```php
Route::get('nodes/{id}/telemetry/history', [TelemetryController::class, 'nodeHistory']);
```

### Frontend изменения

**Файл**: `backend/laravel/resources/js/Pages/Devices/Show.vue`

#### Добавленные компоненты:

1. **MultiSeriesTelemetryChart** - мульти-серийный график для отображения всех сенсорных каналов

#### Функциональность:

- **Автоматическое определение сенсорных каналов**: показываются только каналы с `type === 'sensor'`
- **Мульти-серийный график**: все сенсорные каналы на одном графике с разными осями Y
- **Выбор временного диапазона**: 1H, 24H, 7D, 30D, ALL
- **Автоматические цвета**: каждая метрика имеет свой цвет
- **Автозагрузка**: данные загружаются при открытии страницы и при изменении устройства

#### Маппинг метрик:

```typescript
const metricColors = {
  'TEMP_AIR': '#f59e0b', // amber-500
  'HUMIDITY': '#3b82f6', // blue-500
  'CO2': '#10b981', // emerald-500
  'PH': '#8b5cf6', // violet-500
  'EC': '#06b6d4', // cyan-500
}

const metricLabels = {
  'TEMP_AIR': 'Температура',
  'HUMIDITY': 'Влажность',
  'CO2': 'CO₂',
  'PH': 'pH',
  'EC': 'EC',
}
```

## Как это работает

### Поток данных:

```
1. Firmware (ESP32)
   └─> Публикует телеметрию в MQTT
       └─> Топик: hydro/{gh_uid}/zn-{zone_id}/{node_uid}/{channel}/telemetry

2. History Logger
   └─> Слушает MQTT
   └─> Записывает в TimescaleDB (таблица telemetry)
       └─> Поля: ts, node_id, zone_id, channel, metric_type, value, unit

3. Laravel API
   └─> TelemetryController::nodeHistory()
   └─> SELECT FROM telemetry WHERE node_id = ? AND metric_type = ?
   └─> Возвращает JSON с данными

4. Frontend (Vue)
   └─> Загружает данные через API
   └─> Отображает на графике MultiSeriesTelemetryChart
```

## Примеры использования

### Для климатической ноды (`nd-clim-esp3278e`):

**URL**: `/devices/2`

**Каналы**:
- `temperature` (Температура, °C) - оранжевый
- `humidity` (Влажность, %) - синий
- `co2` (CO₂, ppm) - зелёный

### API запрос для загрузки данных:

```bash
curl -X GET "http://your-domain/api/nodes/2/telemetry/history?metric=TEMP_AIR&channel=temperature&from=2025-12-05T12:00:00Z" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## Отличие от графиков на странице зоны

| Особенность | Страница зоны (`/zones/{id}`) | Страница ноды (`/devices/{id}`) |
|-------------|-------------------------------|----------------------------------|
| **Данные** | Агрегированные по зоне | Конкретная нода |
| **Каналы** | Все ноды зоны вместе | Только эта нода |
| **Endpoint** | `/api/telemetry/aggregates` | `/api/nodes/{id}/telemetry/history` |
| **Фильтрация** | По zone_id + metric | По node_id + metric + channel |
| **Назначение** | Общий мониторинг зоны | Диагностика конкретного устройства |

## Скриншот структуры страницы

```
┌──────────────────────────────────────────────────────┐
│ Device Header (uid, status, zone link)              │
├──────────────────────────────────────────────────────┤
│ 📊 Telemetry Chart (MultiSeries)                    │
│  - Temperature (orange line)                         │
│  - Humidity (blue line)                              │
│  - CO₂ (green line)                                  │
│  [1H] [24H] [7D] [30D] [ALL]                        │
├────────────────────────────────┬─────────────────────┤
│ Channels Table                 │ NodeConfig (JSON)   │
│ - temperature (sensor)         │                     │
│ - humidity (sensor)            │                     │
│ - co2 (sensor)                 │                     │
│ - lighting (actuator)          │                     │
│ - ventilation (actuator)       │                     │
└────────────────────────────────┴─────────────────────┘
```

## Проверка работы

### 1. Откройте страницу устройства:
```
http://your-domain/devices/2
```

### 2. Проверьте, что:
- ✅ Отображается график с несколькими линиями (по количеству сенсорных каналов)
- ✅ Можно переключать временной диапазон (1H, 24H, 7D, 30D, ALL)
- ✅ Каждая линия имеет свой цвет и легенду
- ✅ При наведении показываются значения
- ✅ График поддерживает zoom (колесо мыши) и pan (перетаскивание)

### 3. Откройте DevTools и проверьте Network:
```
GET /api/nodes/2/telemetry/history?metric=TEMP_AIR&channel=temperature&from=...
Status: 200 OK
Response: { "status": "ok", "data": [...] }
```

## Troubleshooting

### График не отображается:

1. **Проверьте, что нода имеет сенсорные каналы**:
   ```bash
   docker compose -f docker-compose.dev.yml exec laravel \
     php artisan tinker --execute="echo json_encode(\App\Models\DeviceNode::find(2)->channels->toArray(), JSON_PRETTY_PRINT);"
   ```

2. **Проверьте, что есть данные в БД**:
   ```sql
   SELECT COUNT(*), channel, metric_type 
   FROM telemetry 
   WHERE node_id = 2 
   GROUP BY channel, metric_type;
   ```

3. **Проверьте логи History Logger**:
   ```bash
   docker compose -f docker-compose.dev.yml logs --tail=50 history-logger | grep "nd-clim-esp3278e"
   ```

4. **Проверьте Browser Console** на наличие ошибок JavaScript

### Данные не загружаются:

1. Проверьте авторизацию - нужен залогиненный пользователь
2. Проверьте доступ к ноде - пользователь должен иметь доступ к зоне, к которой привязана нода
3. Проверьте, что nода публикует телеметрию (см. логи MQTT)

## Дальнейшие улучшения

- [ ] Добавить экспорт данных в CSV/JSON
- [ ] Добавить сравнение с целевыми значениями (targets)
- [ ] Добавить алерты на графике (критические значения)
- [ ] Добавить возможность выбора конкретных каналов для отображения
- [ ] Добавить статистику (min/max/avg) под графиком

