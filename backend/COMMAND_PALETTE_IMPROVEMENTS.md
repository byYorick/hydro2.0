# Улучшения Command Palette (Ctrl+K)

## Реализованный функционал

### 1. ✅ Поиск через API
**Проблема**: API не поддерживал поиск зон/узлов/рецептов.

**Решение**: 
- Добавлен параметр `search` в `ZoneController`, `NodeController`, `RecipeController`
- Поиск работает по имени и описанию (case-insensitive)
- Результаты возвращаются через стандартные API endpoints

### 2. ✅ Быстрые действия для циклов
**Проблема**: Не было возможности быстро запускать циклы через Command Palette.

**Решение**: Добавлены быстрые действия для всех типов циклов:
- **IRRIGATION** (💧 Полив) - запуск полива зоны
- **PH_CONTROL** (🧪 Коррекция pH) - запуск коррекции pH
- **EC_CONTROL** (⚡ Коррекция EC) - запуск коррекции EC
- **CLIMATE** (🌡️ Управление климатом) - запуск управления климатом
- **LIGHTING** (💡 Управление освещением) - запуск управления освещением

Каждое действие:
- Показывается только для активных зон (RUNNING)
- Использует параметры по умолчанию (можно расширить для получения из API)
- Требует подтверждения перед выполнением
- Логируется для отладки

### 3. ✅ Переключение рецептов
**Проблема**: Не было возможности переключать режимы выращивания через Command Palette.

**Решение**: 
- Добавлено действие "Применить рецепт к зоне"
- Появляется при поиске рецепта, если в запросе упомянута зона
- Использует API endpoint `/api/zones/{zone}/attach-recipe`
- Требует подтверждения перед выполнением

### 4. ✅ Улучшенный UX
- **Категории**: Действия группируются по категориям (Зона, Устройство, Рецепт, Цикл: Полив, Цикл: pH, и т.д.)
- **Fuzzy search**: Поиск работает с неполными совпадениями
- **Подсветка совпадений**: Найденные слова выделяются в результатах
- **Горячие клавиши**: Ctrl+K (Cmd+K на Mac) для открытия, Escape для закрытия
- **Подтверждение**: Критичные действия требуют подтверждения

## Измененные файлы

### Backend
- `backend/laravel/app/Http/Controllers/ZoneController.php` - добавлен поиск
- `backend/laravel/app/Http/Controllers/NodeController.php` - добавлен поиск
- `backend/laravel/app/Http/Controllers/RecipeController.php` - добавлен поиск

### Frontend
- `backend/laravel/resources/js/Components/CommandPalette.vue` - расширен функционал

## Примеры использования

### Поиск и навигация
- `Ctrl+K` → `zone 5` → открывает зону 5
- `Ctrl+K` → `node ph-3` → открывает узел ph-3
- `Ctrl+K` → `recipe salad` → открывает рецепт салата

### Быстрые действия
- `Ctrl+K` → `zone 5 полив` → "Полить зону 5" → Enter → подтверждение → полив
- `Ctrl+K` → `zone 3 pH` → "Коррекция pH в зоне 3" → Enter → подтверждение → запуск цикла
- `Ctrl+K` → `zone 1 пауза` → "Приостановить зону 1" → Enter → подтверждение → пауза

### Переключение рецептов
- `Ctrl+K` → `recipe salad zone 5` → "Применить рецепт Salad к зоне 5" → Enter → подтверждение → применение

## Будущие улучшения

1. **Получение параметров из API**: Сейчас используются фиксированные значения по умолчанию, можно получать из targets/recipe через API
2. **Интеллектуальный поиск**: Понимание естественного языка ("полить зону 5 на 10 секунд")
3. **История команд**: Сохранение последних использованных команд
4. **Горячие клавиши для действий**: Прямые комбинации клавиш для часто используемых действий

## Результат

Теперь Command Palette полностью соответствует спецификации:
- ✅ Поиск зон/узлов/рецептов через API
- ✅ Быстрые действия для запуска циклов
- ✅ Переключение режимов выращивания (рецептов)
- ✅ Работает с клавиатуры (Ctrl+K)
- ✅ Удобный UX с категориями и подсветкой

