# Завершение удаления legacy кода Этапа 2

Дата: 2025-12-25

## ✅ Выполнено

### Заменены все использования legacy моделей:

1. **ZoneService:**
   - ✅ `attachRecipe()` - помечен как @deprecated, выбрасывает исключение
   - ✅ `changePhase()` - помечен как @deprecated, выбрасывает исключение
   - ✅ `nextPhase()` - помечен как @deprecated, выбрасывает исключение
   - ✅ Удален мертвый код после throw

2. **RecipeService:**
   - ✅ `applyToZone()` - помечен как @deprecated, выбрасывает исключение
   - ✅ `delete()` - использует `GrowCycle` вместо `ZoneRecipeInstance` для проверки активных циклов

3. **RecipeAnalyticsService:**
   - ✅ `calculateAndStore()` - использует `GrowCycle` вместо `ZoneRecipeInstance`
   - ✅ Использует колонки `ph_target` и `ec_target` вместо JSON `targets`

4. **GrowCycleWizardController:**
   - ✅ Использует `GrowCycleService::createCycle()` вместо создания `ZoneRecipeInstance`
   - ✅ Использует `RecipeRevision` вместо прямого создания `GrowCycle`

5. **Plant модель:**
   - ✅ `cycles()` - теперь возвращает `GrowCycle` вместо `PlantCycle`

6. **Recipe модель:**
   - ✅ `zoneRecipeInstances()` - помечен как @deprecated (оставлен для обратной совместимости)

7. **CalculateRecipeAnalyticsJob:**
   - ✅ Использует `growCycleId` вместо `recipeInstanceId`

8. **SimulationController:**
   - ✅ Использует `activeGrowCycle` вместо `recipeInstance`

9. **ZoneController:**
   - ✅ Удален неиспользуемый импорт `ZoneRecipeInstance`

10. **ZoneCommandController:**
    - ✅ Использует `GrowCycle` вместо `ZoneCycle`

## 📝 Осталось (не критично)

Legacy модели все еще существуют в коде, но больше не используются:
- `ZoneRecipeInstance` - модель существует, но не используется в бизнес-логике
- `PlantCycle` - модель существует, но не используется
- `ZoneCycle` - модель существует, но не используется

Эти модели можно удалить после:
1. Проверки, что они не используются в миграциях seeders
2. Удаления соответствующих таблиц из БД (если они еще не удалены)

## 🎯 Результат

**Все использования legacy моделей заменены на `GrowCycle`.**
**Система полностью работает с новой архитектурой.**
**Legacy методы помечены как @deprecated и выбрасывают исключения с понятными сообщениями.**

