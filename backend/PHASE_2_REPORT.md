# PHASE 2 - Отчет о выполнении

Дата: 2025-12-25

## ✅ PHASE 2: Сделать "канонический слой grow-cycle" полностью самодостаточным

### Выполнено

#### 2.1. Созданы таблицы снапшотов

**Созданные миграции:**
- `2025_12_25_151715_create_grow_cycle_phases_table.php`
- `2025_12_25_151716_create_grow_cycle_phase_steps_table.php`

**Таблица `grow_cycle_phases`:**
- Снапшот фаз ревизии рецепта для конкретного цикла
- Все уставки хранятся колонками (ph_target, ec_target, irrigation_mode, и т.д.)
- Обратная ссылка `recipe_revision_phase_id` для трассировки (nullable)
- Временные метки `started_at`, `ended_at` для выполнения фазы в цикле
- Уникальность: `unique(grow_cycle_id, phase_index)`
- Индексы: `(grow_cycle_id)`, `(recipe_revision_phase_id)`

**Таблица `grow_cycle_phase_steps`:**
- Снапшот шагов фазы для конкретного цикла
- Все уставки хранятся колонками (nullable)
- Обратная ссылка `recipe_revision_phase_step_id` для трассировки (nullable)
- Временные метки `started_at`, `ended_at` для выполнения шага в цикле
- Уникальность: `unique(grow_cycle_phase_id, step_index)`
- Индексы: `(grow_cycle_phase_id)`, `(recipe_revision_phase_step_id)`

**Преимущества снапшотов:**
- После старта цикла можно удалить/изменить шаблонные фазы — снапшот остаётся валиден
- История выполнения цикла сохраняется независимо от изменений рецепта
- Возможность отслеживания изменений через обратные ссылки на шаблоны

#### 2.2. Обновлены связи в grow_cycles

**Миграция:** `2025_12_25_151717_update_grow_cycles_for_snapshots.php`

**Изменения:**
- `current_phase_id` теперь ссылается на `grow_cycle_phases` вместо `recipe_revision_phases`
- `current_step_id` теперь ссылается на `grow_cycle_phase_steps` вместо `recipe_revision_phase_steps`
- Добавлены индексы для быстрого поиска:
  - `grow_cycles_current_phase_idx` на `current_phase_id`
  - `grow_cycles_current_step_idx` на `current_step_id`

**Валидность ссылок:**
- FK constraints обеспечивают, что `current_phase_id` ссылается на фазу того же цикла
- FK constraints обеспечивают, что `current_step_id` ссылается на шаг той же фазы цикла
- В БД невозможны ссылки `current_phase/step` на чужой цикл

#### 2.3. Созданы модели

**GrowCyclePhase:**
- Модель для снапшотов фаз
- Relationships: `growCycle()`, `recipeRevisionPhase()`, `steps()`
- Все уставки в fillable и casts

**GrowCyclePhaseStep:**
- Модель для снапшотов шагов
- Relationships: `growCyclePhase()`, `recipeRevisionPhaseStep()`
- Все уставки в fillable и casts

**GrowCycle (обновлена):**
- `currentPhase()` теперь возвращает `GrowCyclePhase` вместо `RecipeRevisionPhase`
- `currentStep()` теперь возвращает `GrowCyclePhaseStep` вместо `RecipeRevisionPhaseStep`
- Добавлен relationship `phases()` для получения всех снапшотов фаз цикла

## ✅ Acceptance критерии

- ✅ После старта цикла можно удалить/изменить шаблонные фазы — снапшот остаётся валиден
- ✅ В БД невозможны ссылки `current_phase/step` на чужой цикл (обеспечивается FK constraints)
- ✅ Все уставки хранятся колонками, не JSON
- ✅ Обратные ссылки на шаблоны для трассировки присутствуют

## 📋 Следующие шаги

PHASE 3: Сенсоры и телеметрия (единая модель и подготовка к росту)
- Создать таблицу `sensors` (zone_id nullable)
- Создать `telemetry_samples` с партиционированием
- Настроить retention политики

